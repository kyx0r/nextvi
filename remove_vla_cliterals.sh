#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0104 0112 0121 0130 0132 0136 0176-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Command handling readability line breaks
LB="0?"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: conf.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> #define CY1	14	/\\\\* bright cyan \\\\*/
#define WH1	15	/\\\\* bright white \\\\*/

${SEP}??!${DBG:-re p FAIL line 65\\${SEP}p FAIL line 65${INTR}${QF}}${SEP}${LB}
${SEP}+3${SEP}s/\\\\(int\\\\[\\\\]\\\\)//${SEP}??!${DBG:-re p FAIL line 65\\${SEP}p FAIL line 65${INTR}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'conf.c'

# Patch: ex.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 		return xserr;
	if \\\\(o1 >= 0 && dir > 0\\\\) \\\\{
		sbuf sb;${SEP}??!${DBG:-re p FAIL line 516\\${SEP}p FAIL line 516${INTR}${QF}}${SEP}${LB}
${SEP}+3c 		int *offs = emalloc(xkwdrs->nsubc * sizeof(int));
		int flg = 0, soff = 0;
.
${SEP}.,\$;f> 						soff \\\\+ offs\\\\[xgrp\\\\], &xrow, &xoff\\\\)\\\\)
			ret = xuerr;
		free\\\\(sb\\\\.s\\\\);${SEP}??!${DBG:-re p FAIL line 529\\${SEP}p FAIL line 529${INTR}${QF}}${SEP}${LB}
${SEP}+2a 		free(offs);
.
${SEP}.,\$;f> 		rep = re_read\\\\(&s, 0\\\\);
	\\\\}
	free\\\\(pat\\\\);${SEP}??!${DBG:-re p FAIL line 992\\${SEP}p FAIL line 992${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	int *offs = emalloc(rs->nsubc * sizeof(int));
.
${SEP}.,\$;f> 	if \\\\(rs != xkwdrs\\\\)
		rset_free\\\\(rs\\\\);
	free\\\\(rep\\\\);${SEP}??!${DBG:-re p FAIL line 1043\\${SEP}p FAIL line 1043${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(offs);
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

# Patch: lbuf.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 		int nskip, int \\\\*r, int \\\\*o\\\\)
\\\\{
	int r0 = \\\\*r, o0 = \\\\*o;${SEP}??!${DBG:-re p FAIL line 483\\${SEP}p FAIL line 483${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	int *offs = emalloc(re->nsubc * sizeof(int)), i = r0;
.
${SEP}.,\$;f> 				break;
			\\\\*o = _o;
			\\\\*r = i;${SEP}??!${DBG:-re p FAIL line 508\\${SEP}p FAIL line 508${INTR}${QF}}${SEP}${LB}
${SEP}+3c 			if (dir > 0) {
				free(offs);
.
${SEP}.,\$f> 				return 0;${SEP}??!${DBG:-re p FAIL line 509\\${SEP}p FAIL line 509${INTR}${QF}}${SEP}${LB}
${SEP}.a 			}
.
${SEP}.,\$;f> 		\\\\}
		off = 0;
	\\\\}${SEP}??!${DBG:-re p FAIL line 515\\${SEP}p FAIL line 515${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(offs);
.
${SEP}vis 2${SEP}wq" $VI -e 'lbuf.c'

# Patch: led.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 		xic \\\\? REG_ICASE \\\\| REG_NEWLINE : REG_NEWLINE\\\\);
	if \\\\(!rs\\\\)
		return;${SEP}??!${DBG:-re p FAIL line 48\\${SEP}p FAIL line 48${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	int *subs = emalloc(rs->nsubc * sizeof(int));
.
${SEP}.,\$;f> 	sbuf_null\\\\(acsb\\\\)
	free\\\\(ibuf->s\\\\);
	rset_free\\\\(rs\\\\);${SEP}??!${DBG:-re p FAIL line 80\\${SEP}p FAIL line 80${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(subs);
.
${SEP}.,\$;f> 	int att_old = 0, atti = 0, cterm = cend - cbeg;
	char \\\\*bound = NULL;
	char \\\\*\\\\*chrs = r->chrs;	/\\\\* chrs\\\\[i\\\\]: the i-th character in s0 \\\\*/${SEP}??!${DBG:-re p FAIL line 151\\${SEP}p FAIL line 151${INTR}${QF}}${SEP}${LB}
${SEP}+3,#+3c 	int *off = emalloc((cterm+1) * sizeof(int));	/* off[i]: the character at screen position i */
	int *att = emalloc((cterm+1) * sizeof(int));	/* att[i]: the attributes of i-th character */
	int *stt = NULL;	/* stt[i]: remap off indexes */
	int *ctt = NULL;	/* ctt[i]: cterm bound attrs */
.
${SEP}.,\$;f> 		if \\\\(o >= 0 && r->cmax > cterm && r->pos\\\\[o\\\\] \\\\+ r->wid\\\\[o\\\\] > cend\\\\)
			while \\\\(off\\\\[i\\\\] == o\\\\)
				off\\\\[ctx < 0 \\\\? i\\\\+\\\\+ : i--\\\\] = -1;${SEP}??!${DBG:-re p FAIL line 175\\${SEP}p FAIL line 175${INTR}${QF}}${SEP}${LB}
${SEP}+2a 		stt = emalloc((cterm+1) * sizeof(int));
		ctt = emalloc((cterm+1) * sizeof(int));
.
${SEP}.,\$;f> 		memcpy\\\\(chrs\\\\[n\\\\], r->nullhole, r->holelen\\\\);
		r->holelen = 0;
	\\\\}${SEP}??!${DBG:-re p FAIL line 257\\${SEP}p FAIL line 257${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(off);
	free(att);
	free(stt);
	free(ctt);
.
${SEP}vis 2${SEP}wq" $VI -e 'led.c'

# Patch: regex.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 	return 0;
\\\\}

${SEP}??!${DBG:-re p FAIL line 402\\${SEP}p FAIL line 402${INTR}${QF}}${SEP}${LB}
${SEP}+3c #define _return(state) { if (eol_ch) utf8_length[eol_ch] = 1; \\\\
free(pcs); \\\\
free(subs); \\\\
free(sdense); \\\\
free(_clist); \\\\
free(_nlist); \\\\
free(nsubs); \\\\
free(lb); \\\\
return state; } \\\\
.
${SEP}.,\$;f> 		return 0;
	flg = prog->flg \\\\| flg;
	const char \\\\*sp = s, \\\\*_sp = s, \\\\*s0, \\\\*s1;${SEP}??!${DBG:-re p FAIL line 628\\${SEP}p FAIL line 628${INTR}${QF}}${SEP}${LB}
${SEP}+3,#+1c 	int *npc, *pc, *insts = prog->insts;
	int **pcs = emalloc((prog->splits) * sizeof(int*));
	rsub **subs = emalloc(prog->splits * sizeof(rsub*));
	unsigned int *sdense = emalloc(prog->sparsesz * sizeof(unsigned int)), sparsesz = 0;
.
${SEP}.,\$f> 	rsub \\\\*nsub, \\\\*sub, \\\\*matched = NULL, \\\\*freesub = NULL;${SEP}??!${DBG:-re p FAIL line 631\\${SEP}p FAIL line 631${INTR}${QF}}${SEP}${LB}
${SEP}+1c 	rthread *_clist = emalloc(prog->len * sizeof(rthread)), *_nlist = emalloc(prog->len * sizeof(rthread));
.
${SEP}.,\$f> 	rthread \\\\*clist = _clist, \\\\*nlist = _nlist, \\\\*tmp;${SEP}??!${DBG:-re p FAIL line 633\\${SEP}p FAIL line 633${INTR}${QF}}${SEP}${LB}
${SEP}+1${SEP}s/lb\\\\[prog->laidx\\\\+1\\\\]/*lb = emalloc(prog->laidx * sizeof(lb[0]))/${SEP}??!${DBG:-re p FAIL line 633\\${SEP}p FAIL line 633${INTR}${QF}}${SEP}.,\$;f> 	int cnt, spc, i, c, j, osubp = nsubc \\\\* sizeof\\\\(char\\\\*\\\\);
	int si = 0, clistidx = 0, nlistidx, mcont = MATCH;
	int eol_ch = flg & REG_NEWLINE \\\\? '\\\\\\\\n' : 0;${SEP}??!${DBG:-re p FAIL line 638\\${SEP}p FAIL line 638${INTR}${QF}}${SEP}${LB}
${SEP}+3,#+1c 	char *nsubs = emalloc(prog->sub);
.
${SEP}.,\$;f> /\\\\* return the index of the matching regular expression or -1 if none matches \\\\*/
int rset_find\\\\(rset \\\\*rs, char \\\\*s, int \\\\*grps, int flg\\\\)
\\\\{${SEP}??!${DBG:-re p FAIL line 717\\${SEP}p FAIL line 717${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	const char **subs = emalloc((rs->nsubc+2) * sizeof(subs[0]));
.
${SEP}.,\$;f> 						grps\\\\[gi \\\\+ 1\\\\] = -1;
					\\\\}
				\\\\}${SEP}??!${DBG:-re p FAIL line 733\\${SEP}p FAIL line 733${INTR}${QF}}${SEP}${LB}
${SEP}+2a 				free(subs);
.
${SEP}.,\$;f> 			\\\\}
		\\\\}
	\\\\}${SEP}??!${DBG:-re p FAIL line 737\\${SEP}p FAIL line 737${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(subs);
.
${SEP}vis 2${SEP}wq" $VI -e 'regex.c'

# Patch: ren.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> static int syn_initft\\\\(int fti, int n, char \\\\*name, int flg\\\\)
\\\\{
	int i = n, set = hls\\\\[i\\\\]\\\\.set;${SEP}??!${DBG:-re p FAIL line 254\\${SEP}p FAIL line 254${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	char **pats = emalloc(hlslen * sizeof(char *));
.
${SEP}.,\$;f> 	ftmap\\\\[fti\\\\]\\\\.ft = name;
	ftmap\\\\[fti\\\\]\\\\.rs = rset_make\\\\(i - n, pats, flg\\\\);
	ftmap\\\\[fti\\\\]\\\\.seteidx = i;${SEP}??!${DBG:-re p FAIL line 260\\${SEP}p FAIL line 260${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(pats);
.
${SEP}.,\$;f> 	int fti = ftidx, blockhl = syn_blockhl, blockca = -1;
	re:;
	rset \\\\*rs = ftmap\\\\[fti\\\\]\\\\.rs;${SEP}??!${DBG:-re p FAIL line 310\\${SEP}p FAIL line 310${INTR}${QF}}${SEP}${LB}
${SEP}+3${SEP}s/subs\\\\[rs->nsubc\\\\]/*subs = emalloc(rs->nsubc * sizeof(int))/${SEP}??!${DBG:-re p FAIL line 310\\${SEP}p FAIL line 310${INTR}${QF}}${SEP}.,\$;f> 		flg = REG_NOTBOL;
	\\\\}
	fti\\\\+\\\\+;${SEP}??!${DBG:-re p FAIL line 353\\${SEP}p FAIL line 353${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	if (ftmidx > fti && ftmap[fti-1].ft == ftmap[fti].ft) {
		free(subs);
.
${SEP}.,\$f> 		goto re;${SEP}??!${DBG:-re p FAIL line 354\\${SEP}p FAIL line 354${INTR}${QF}}${SEP}${LB}
${SEP}.a 	}
.
${SEP}.,\$;f> 	if \\\\(blockca >= 0 && SYN_BSSET\\\\(blockca\\\\) && !SYN_BESET\\\\(blockca\\\\) && last_scdir > 0\\\\)
		syn_blockhl = -1;${SEP}??!${DBG:-re p FAIL line 357\\${SEP}p FAIL line 357${INTR}${QF}}${SEP}${LB}
${SEP}+2c 	if (syn_blockhl < 0 || blockhl < 0) {
		free(subs);
.
${SEP}.,\$f> 		return;${SEP}??!${DBG:-re p FAIL line 358\\${SEP}p FAIL line 358${INTR}${QF}}${SEP}${LB}
${SEP}.a 	}
.
${SEP}.,\$;f> 	for \\\\(j = 0; j < n; j\\\\+\\\\+\\\\)
		if \\\\(!att\\\\[j\\\\] \\\\|\\\\| !SYN_BPSET\\\\(blockatt\\\\)\\\\)
			att\\\\[j\\\\] = blockatt;${SEP}??!${DBG:-re p FAIL line 361\\${SEP}p FAIL line 361${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(subs);
.
${SEP}.,\$;f> 
void syn_init\\\\(void\\\\)
\\\\{${SEP}??!${DBG:-re p FAIL line 405\\${SEP}p FAIL line 405${INTR}${QF}}${SEP}${LB}
${SEP}+3c 	char **pats = emalloc(ftslen * sizeof(char *));
.
${SEP}.,\$;f> 	for \\\\(; i < ftslen; i\\\\+\\\\+\\\\)
		pats\\\\[i\\\\] = fts\\\\[i\\\\]\\\\.pat;
	syn_ftrs = rset_make\\\\(i, pats, 0\\\\);${SEP}??!${DBG:-re p FAIL line 409\\${SEP}p FAIL line 409${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	free(pats);
.
${SEP}vis 2${SEP}wq" $VI -e 'ren.c'

# Patch: vi.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 			s = lbuf_get\\\\(xb, row - vi_rshift\\\\);
			goto skip;
		\\\\}${SEP}??!${DBG:-re p FAIL line 143\\${SEP}p FAIL line 143${INTR}${QF}}${SEP}${LB}
${SEP}+3c 		char *tmp = emalloc(xcols+3);
		char *snum = emalloc(32);
.
${SEP}.,\$;f> 		restore\\\\(syn_blockhl\\\\)
		restore\\\\(xtd\\\\)
		restore\\\\(ftidx\\\\)${SEP}??!${DBG:-re p FAIL line 167\\${SEP}p FAIL line 167${INTR}${QF}}${SEP}${LB}
${SEP}+2a 		free(tmp);
		free(snum);
.
${SEP}.,\$;f> 				if \\\\(!\\\\(ln = lbuf_get\\\\(xb, xrow\\\\)\\\\)\\\\)
					break;
				ln \\\\+= xoff;${SEP}??!${DBG:-re p FAIL line 1290\\${SEP}p FAIL line 1290${INTR}${QF}}${SEP}${LB}
${SEP}+3c 				char *buf = emalloc(strlen(ln)+4);
.
${SEP}.,\$;f> 				strcpy\\\\(buf, \":e \"\\\\);
				strcpy\\\\(buf\\\\+3, ln\\\\);
				term_push\\\\(buf, strlen\\\\(ln\\\\)\\\\+3\\\\);${SEP}??!${DBG:-re p FAIL line 1293\\${SEP}p FAIL line 1293${INTR}${QF}}${SEP}${LB}
${SEP}+2a 				free(buf);
.
${SEP}.,\$;f> 					goto do_excmd;
				case '/': \\\\{
					cs = vi_curword\\\\(xb, xrow, xoff, vi_arg, 1\\\\);${SEP}??!${DBG:-re p FAIL line 1405\\${SEP}p FAIL line 1405${INTR}${QF}}${SEP}${LB}
${SEP}+3c 					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
.
${SEP}.,\$;f> 						strcat\\\\(buf, cs\\\\);
					free\\\\(cs\\\\);
					ln = vi_enprompt\\\\(\":\", buf, &k, &n\\\\);${SEP}??!${DBG:-re p FAIL line 1410\\${SEP}p FAIL line 1410${INTR}${QF}}${SEP}${LB}
${SEP}+2a 					free(buf);
.
${SEP}.,\$;f> 				case 't': \\\\{
					vi_drawmsg\\\\(\"arg2:\\\\(0\\\\|#\\\\)\"\\\\);
					cs = vi_curword\\\\(xb, xrow, xoff, vi_prefix\\\\(\\\\), 1\\\\);${SEP}??!${DBG:-re p FAIL line 1415\\${SEP}p FAIL line 1415${INTR}${QF}}${SEP}${LB}
${SEP}+3c 					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
.
${SEP}.,\$;f> 						free\\\\(cs\\\\);
					\\\\}
					ln = vi_enprompt\\\\(\":\", buf, &k, &n\\\\);${SEP}??!${DBG:-re p FAIL line 1424\\${SEP}p FAIL line 1424${INTR}${QF}}${SEP}${LB}
${SEP}+2a 					free(buf);
.
${SEP}.,\$;f> 					goto do_excmd; \\\\}
				case 'r': \\\\{
					cs = vi_curword\\\\(xb, xrow, xoff, vi_arg, 1\\\\);${SEP}??!${DBG:-re p FAIL line 1428\\${SEP}p FAIL line 1428${INTR}${QF}}${SEP}${LB}
${SEP}+3c 					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
.
${SEP}.,\$;f> 						free\\\\(cs\\\\);
					\\\\}
					ln = vi_enprompt\\\\(\":\", buf, &k, &n\\\\);${SEP}??!${DBG:-re p FAIL line 1435\\${SEP}p FAIL line 1435${INTR}${QF}}${SEP}${LB}
${SEP}+2a 					free(buf);
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

# Patch: vi.h
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> struct highlight \\\\{
	char \\\\*ft;		/\\\\* the filetype of this pattern \\\\*/
	char \\\\*pat;		/\\\\* regular expression \\\\*/${SEP}??!${DBG:-re p FAIL line 498\\${SEP}p FAIL line 498${INTR}${QF}}${SEP}${LB}
${SEP}+3${SEP}s/\\\\*att/att[16]/${SEP}??!${DBG:-re p FAIL line 498\\${SEP}p FAIL line 498${INTR}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'vi.h'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index d45d10a6..b22cdc46 100644
--- a/conf.c
+++ b/conf.c
@@ -62,7 +62,7 @@ const int ftslen = LEN(fts);
 #define CY1	14	/* bright cyan */
 #define WH1	15	/* bright white */
 
-#define A(...) (int[]){__VA_ARGS__}
+#define A(...) {__VA_ARGS__}
 
 /* At least 1 entry is required in this struct for fallback */
 /* lbuf lines are *always "\n\0" terminated, for $ to work one needs to account for '\n' too */
diff --git a/ex.c b/ex.c
index c195038b..c071ac3c 100644
--- a/ex.c
+++ b/ex.c
@@ -513,7 +513,8 @@ static void *ec_find(char *loc, char *cmd, char *arg)
 		return xserr;
 	if (o1 >= 0 && dir > 0) {
 		sbuf sb;
-		int offs[xkwdrs->nsubc], flg = 0, soff = 0;
+		int *offs = emalloc(xkwdrs->nsubc * sizeof(int));
+		int flg = 0, soff = 0;
 		int r2 = end - 1;
 		int skip = cmd[1] == '+' ? 1 : 0;
 		void *ret = NULL;
@@ -527,6 +528,7 @@ static void *ec_find(char *loc, char *cmd, char *arg)
 						soff + offs[xgrp], &xrow, &xoff))
 			ret = xuerr;
 		free(sb.s);
+		free(offs);
 		return ret;
 	}
 	off = xoff;
@@ -989,7 +991,7 @@ static void *ec_substitute(char *loc, char *cmd, char *arg)
 		rep = re_read(&s, 0);
 	}
 	free(pat);
-	int offs[rs->nsubc];
+	int *offs = emalloc(rs->nsubc * sizeof(int));
 	for (i = beg; i < end; i++) {
 		char *ln = lbuf_get(xb, i);
 		sbuf *r = NULL;
@@ -1041,6 +1043,7 @@ static void *ec_substitute(char *loc, char *cmd, char *arg)
 	if (rs != xkwdrs)
 		rset_free(rs);
 	free(rep);
+	free(offs);
 	return first < 0 ? xuerr : NULL;
 }
 
diff --git a/lbuf.c b/lbuf.c
index 09950361..5cb40929 100644
--- a/lbuf.c
+++ b/lbuf.c
@@ -480,7 +480,7 @@ int lbuf_search(struct lbuf *lb, rset *re, int dir, int beg, int end, int pskip,
 		int nskip, int *r, int *o)
 {
 	int r0 = *r, o0 = *o;
-	int offs[re->nsubc], i = r0;
+	int *offs = emalloc(re->nsubc * sizeof(int)), i = r0;
 	char *s = lbuf_get(lb, i);
 	int off, g1, g2, _o, step, flg;
 	if (pskip >= 0 && s)
@@ -505,14 +505,17 @@ int lbuf_search(struct lbuf *lb, rset *re, int dir, int beg, int end, int pskip,
 				break;
 			*o = _o;
 			*r = i;
-			if (dir > 0)
+			if (dir > 0) {
+				free(offs);
 				return 0;
+			}
 			step = off + g1;
 			off += g2 > 0 ? g2 : 1;
 			end = -1; /* break outer loop efficiently */
 		}
 		off = 0;
 	}
+	free(offs);
 	return end < 0 ? 0 : 1;
 }
 
diff --git a/led.c b/led.c
index 6a5e065f..de8ba321 100644
--- a/led.c
+++ b/led.c
@@ -45,7 +45,7 @@ static void file_index(struct lbuf *buf)
 		xic ? REG_ICASE | REG_NEWLINE : REG_NEWLINE);
 	if (!rs)
 		return;
-	int subs[rs->nsubc];
+	int *subs = emalloc(rs->nsubc * sizeof(int));
 	sbuf_smake(ibuf, 1024)
 	for (n = 1; n <= acsb->s_n; n++)
 		if (acsb->s[n - 1] == '\n')
@@ -78,6 +78,7 @@ static void file_index(struct lbuf *buf)
 	sbuf_null(acsb)
 	free(ibuf->s);
 	rset_free(rs);
+	free(subs);
 }
 
 static char *kmap_map(int kmap, int c)
@@ -148,10 +149,10 @@ void led_render(char *s0, int cbeg, int cend)
 	int att_old = 0, atti = 0, cterm = cend - cbeg;
 	char *bound = NULL;
 	char **chrs = r->chrs;	/* chrs[i]: the i-th character in s0 */
-	int off[cterm+1];	/* off[i]: the character at screen position i */
-	int att[cterm+1];	/* att[i]: the attributes of i-th character */
-	int stt[cterm+1];	/* stt[i]: remap off indexes */
-	int ctt[cterm+1];	/* ctt[i]: cterm bound attrs */
+	int *off = emalloc((cterm+1) * sizeof(int));	/* off[i]: the character at screen position i */
+	int *att = emalloc((cterm+1) * sizeof(int));	/* att[i]: the attributes of i-th character */
+	int *stt = NULL;	/* stt[i]: remap off indexes */
+	int *ctt = NULL;	/* ctt[i]: cterm bound attrs */
 	int ctx = r->ctx;
 	off[cterm] = -1;
 	if (ctx < 0) {
@@ -173,6 +174,8 @@ void led_render(char *s0, int cbeg, int cend)
 		if (o >= 0 && r->cmax > cterm && r->pos[o] + r->wid[o] > cend)
 			while (off[i] == o)
 				off[ctx < 0 ? i++ : i--] = -1;
+		stt = emalloc((cterm+1) * sizeof(int));
+		ctt = emalloc((cterm+1) * sizeof(int));
 		for (i = 0, c = 0; i < cterm;) {
 			if ((o = off[i++]) >= 0) {
 				att[c++] = o;
@@ -255,6 +258,10 @@ void led_render(char *s0, int cbeg, int cend)
 		memcpy(chrs[n], r->nullhole, r->holelen);
 		r->holelen = 0;
 	}
+	free(off);
+	free(att);
+	free(stt);
+	free(ctt);
 }
 
 static int led_lastchar(char *s)
diff --git a/regex.c b/regex.c
index ff88bb41..3ed5bf64 100644
--- a/regex.c
+++ b/regex.c
@@ -399,7 +399,15 @@ static int reg_comp(rcode *prog, char *re, int nsubc, int laidx, int flg)
 	return 0;
 }
 
-#define _return(state) { if (eol_ch) utf8_length[eol_ch] = 1; return state; } \
+#define _return(state) { if (eol_ch) utf8_length[eol_ch] = 1; \
+free(pcs); \
+free(subs); \
+free(sdense); \
+free(_clist); \
+free(_nlist); \
+free(nsubs); \
+free(lb); \
+return state; } \
 
 #define newsub(init, copy) \
 if (freesub) { \
@@ -625,18 +633,19 @@ static int re_pikevm(rcode *prog, const char *s, const char **subp, int nsubc, i
 		return 0;
 	flg = prog->flg | flg;
 	const char *sp = s, *_sp = s, *s0, *s1;
-	int *pcs[prog->splits], *npc, *pc, *insts = prog->insts;
-	rsub *subs[prog->splits];
+	int *npc, *pc, *insts = prog->insts;
+	int **pcs = emalloc((prog->splits) * sizeof(int*));
+	rsub **subs = emalloc(prog->splits * sizeof(rsub*));
+	unsigned int *sdense = emalloc(prog->sparsesz * sizeof(unsigned int)), sparsesz = 0;
 	rsub *nsub, *sub, *matched = NULL, *freesub = NULL;
-	rthread _clist[prog->len], _nlist[prog->len];
+	rthread *_clist = emalloc(prog->len * sizeof(rthread)), *_nlist = emalloc(prog->len * sizeof(rthread));
 	rthread *clist = _clist, *nlist = _nlist, *tmp;
-	const char *_subp[2], *lb[prog->laidx+1];
+	const char *_subp[2], **lb = emalloc(prog->laidx * sizeof(lb[0]));
 	int rsubsize = prog->presub, suboff = 0;
 	int cnt, spc, i, c, j, osubp = nsubc * sizeof(char*);
 	int si = 0, clistidx = 0, nlistidx, mcont = MATCH;
 	int eol_ch = flg & REG_NEWLINE ? '\n' : 0;
-	unsigned int sdense[prog->sparsesz], sparsesz = 0;
-	char nsubs[prog->sub];
+	char *nsubs = emalloc(prog->sub);
 	for (i = 0; i < prog->laidx; i++)
 		lb[i] = NULL;
 	if (eol_ch)
@@ -714,7 +723,7 @@ rset *rset_make(int n, char **re, int flg)
 /* return the index of the matching regular expression or -1 if none matches */
 int rset_find(rset *rs, char *s, int *grps, int flg)
 {
-	const char *subs[rs->nsubc+2];
+	const char **subs = emalloc((rs->nsubc+2) * sizeof(subs[0]));
 	const char **sub = subs+2;
 	if (re_pikevm(rs->regex, s, sub, rs->nsubc, flg)) {
 		subs[1] = NULL; /* make sure sub[-1] never matches */
@@ -731,10 +740,12 @@ int rset_find(rset *rs, char *s, int *grps, int flg)
 						grps[gi + 1] = -1;
 					}
 				}
+				free(subs);
 				return i;
 			}
 		}
 	}
+	free(subs);
 	return -1;
 }
 
diff --git a/ren.c b/ren.c
index a98fc3fa..0d972388 100644
--- a/ren.c
+++ b/ren.c
@@ -251,13 +251,14 @@ int syn_blockhl;
 static int syn_initft(int fti, int n, char *name, int flg)
 {
 	int i = n, set = hls[i].set;
-	char *pats[hlslen];
+	char **pats = emalloc(hlslen * sizeof(char *));
 	for (; i < hlslen && hls[i].ft == name && hls[i].set == set; i++)
 		pats[i - n] = hls[i].pat;
 	ftmap[fti].setbidx = n;
 	ftmap[fti].ft = name;
 	ftmap[fti].rs = rset_make(i - n, pats, flg);
 	ftmap[fti].seteidx = i;
+	free(pats);
 	return i < hlslen && hls[i].ft == name && hls[i].set != set;
 }
 
@@ -307,7 +308,7 @@ void syn_highlight(int *att, char *s, int n)
 	int fti = ftidx, blockhl = syn_blockhl, blockca = -1;
 	re:;
 	rset *rs = ftmap[fti].rs;
-	int subs[rs->nsubc], *catt, *iatt, sl, c;
+	int *subs = emalloc(rs->nsubc * sizeof(int)), *catt, *iatt, sl, c;
 	int cend, sidx = 0, flg = 0, hl, j, i, ii;
 	while ((sl = rset_find(rs, s + sidx, subs, flg)) >= 0) {
 		cend = 1;
@@ -350,15 +351,20 @@ void syn_highlight(int *att, char *s, int n)
 		flg = REG_NOTBOL;
 	}
 	fti++;
-	if (ftmidx > fti && ftmap[fti-1].ft == ftmap[fti].ft)
+	if (ftmidx > fti && ftmap[fti-1].ft == ftmap[fti].ft) {
+		free(subs);
 		goto re;
+	}
 	if (blockca >= 0 && SYN_BSSET(blockca) && !SYN_BESET(blockca) && last_scdir > 0)
 		syn_blockhl = -1;
-	if (syn_blockhl < 0 || blockhl < 0)
+	if (syn_blockhl < 0 || blockhl < 0) {
+		free(subs);
 		return;
+	}
 	for (j = 0; j < n; j++)
 		if (!att[j] || !SYN_BPSET(blockatt))
 			att[j] = blockatt;
+	free(subs);
 }
 
 char *syn_filetype(char *path)
@@ -402,9 +408,10 @@ int syn_addhl(char *reg, int id)
 
 void syn_init(void)
 {
-	char *pats[ftslen];
+	char **pats = emalloc(ftslen * sizeof(char *));
 	int i = 0;
 	for (; i < ftslen; i++)
 		pats[i] = fts[i].pat;
 	syn_ftrs = rset_make(i, pats, 0);
+	free(pats);
 }
diff --git a/vi.c b/vi.c
index f909fe0d..1c0ad2d5 100644
--- a/vi.c
+++ b/vi.c
@@ -140,7 +140,8 @@ static void vi_drawrow(int row)
 			s = lbuf_get(xb, row - vi_rshift);
 			goto skip;
 		}
-		char tmp[xcols+3], snum[32];
+		char *tmp = emalloc(xcols+3);
+		char *snum = emalloc(32);
 		memset(tmp, ' ', xcols+1);
 		tmp[xcols+1] = '\n';
 		tmp[xcols+2] = '\0';
@@ -165,6 +166,8 @@ static void vi_drawrow(int row)
 		restore(syn_blockhl)
 		restore(xtd)
 		restore(ftidx)
+		free(tmp);
+		free(snum);
 		return;
 	}
 	s = lbuf_get(xb, row);
@@ -1287,10 +1290,11 @@ void vi(int init)
 				if (!(ln = lbuf_get(xb, xrow)))
 					break;
 				ln += xoff;
-				char buf[strlen(ln)+4];
+				char *buf = emalloc(strlen(ln)+4);
 				strcpy(buf, ":e ");
 				strcpy(buf+3, ln);
 				term_push(buf, strlen(ln)+3);
+				free(buf);
 				break; }
 			case TK_CTL('n'):
 				vi_cndir = vi_arg ? -vi_cndir : vi_cndir;
@@ -1402,17 +1406,18 @@ void vi(int init)
 					goto do_excmd;
 				case '/': {
 					cs = vi_curword(xb, xrow, xoff, vi_arg, 1);
-					char buf[cs ? strlen(cs)+30 : 30];
+					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
 					strcpy(buf, "re ");
 					if (cs)
 						strcat(buf, cs);
 					free(cs);
 					ln = vi_enprompt(":", buf, &k, &n);
+					free(buf);
 					goto do_excmd; }
 				case 't': {
 					vi_drawmsg("arg2:(0|#)");
 					cs = vi_curword(xb, xrow, xoff, vi_prefix(), 1);
-					char buf[cs ? strlen(cs)+30 : 30];
+					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
 					strcpy(buf, ".,.+");
 					char *buf1 = itoa(vi_arg, buf+4);
 					strcat(buf1, "s/");
@@ -1422,10 +1427,11 @@ void vi(int init)
 						free(cs);
 					}
 					ln = vi_enprompt(":", buf, &k, &n);
+					free(buf);
 					goto do_excmd; }
 				case 'r': {
 					cs = vi_curword(xb, xrow, xoff, vi_arg, 1);
-					char buf[cs ? strlen(cs)+30 : 30];
+					char *buf = emalloc(cs ? strlen(cs)+30 : 30);
 					strcpy(buf, "%s/");
 					if (cs) {
 						strcat(buf, cs);
@@ -1433,6 +1439,7 @@ void vi(int init)
 						free(cs);
 					}
 					ln = vi_enprompt(":", buf, &k, &n);
+					free(buf);
 					goto do_excmd; }
 				default:
 					term_dec()
diff --git a/vi.h b/vi.h
index bd944301..115bc62e 100644
--- a/vi.h
+++ b/vi.h
@@ -495,7 +495,7 @@ extern const int ftslen;
 struct highlight {
 	char *ft;		/* the filetype of this pattern */
 	char *pat;		/* regular expression */
-	int *att;		/* attributes of the matched groups */
+	int att[16];		/* attributes of the matched groups */
 	unsigned char set;	/* subset index */
 	unsigned char id;	/* id of this hl */
 };
