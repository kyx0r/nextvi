#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0105 0107-0110 0112-0113 0117-0127 0131-0132 0136 0172 0176-0205 0207-0217 0224-0341 0343-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Command that handles readability line breaks
LB="0?"
# Disable errors
[ "$DBG" = "1" ] && DBG="0\?" || DBG=
# Ignore errors
[ "$QF" = "1" ] && QF= || QF="\\${SEP}vis 2\\${SEP}q!1"
# Enters vi at failing code line in this script
# Designed for state inspection mid execution
[ "$INTR" = "1" ] && INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:83reg %@/:%f> %@p:&Q:b0:|sc! \\\\\\${SEP}|:vis 3\\${SEP}q1" || INTR=

# Patch: vi.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}b0${SEP}%;f> 
	mv = .*\\\\(.*\\\\);
	switch \\\\(mv\\\\) \\\\{${SEP}??!${DBG:-ya!p\\${SEP}prp\\${SEP}p FAIL vi.c:523\\${SEP}pr${INTR}${QF}}${SEP}${LB}
${SEP}+2a 	case '\\\\033':	/* Arrow keys */
		mv = term_read(0);
		if (mv == '[') {
			mv = term_read(0);
			switch (mv) {
			case 'A':	/* ↑ */
				*row = MAX(*row - cnt, 0);
				mv = 'k';
				break;
			case 'B':	/* ↓ */
				*row = MIN(*row + cnt, lbuf_len(xb) - 1);
				mv = 'j';
				break;
			case 'D':	/* ← */
			case 'C':	/* → */
				if (!(cs = lbuf_get(xb, *row)))
					return -1;
				dir = dir_context(lbuf_get(xb, *row));
				if (mv == 'D')
					dir = -dir;
				for (i = 0; i < cnt; i++)
					if (vi_nextcol(cs, dir, off))
						break;
				mv = mv == 'D' ? 'h' : 'l';
				break;
			default:
				return 0;
			}
		} else	/* Not a 033[X command so we abort */
			return 0;
		break;
${SEP}vis 2${SEP}b0${SEP}w${SEP}q" $VI -e 'vi.c'

exit 0
=== PATCH2VI DELTA ===
=== DELTA vi.c ===
=== GROUP 1 ===
+	case '\033':	/* Arrow keys */
+		mv = term_read(0);
+		if (mv == '[') {
+			mv = term_read(0);
+			switch (mv) {
+			case 'A':	/* ↑ */
+				*row = MAX(*row - cnt, 0);
+				mv = 'k';
+				break;
+			case 'B':	/* ↓ */
+				*row = MIN(*row + cnt, lbuf_len(xb) - 1);
+				mv = 'j';
+				break;
+			case 'D':	/* ← */
+			case 'C':	/* → */
+				if (!(cs = lbuf_get(xb, *row)))
+					return -1;
+				dir = dir_context(lbuf_get(xb, *row));
+				if (mv == 'D')
+					dir = -dir;
+				for (i = 0; i < cnt; i++)
+					if (vi_nextcol(cs, dir, off))
+						break;
+				mv = mv == 'D' ? 'h' : 'l';
+				break;
+			default:
+				return 0;
+			}
+		} else	/* Not a 033[X command so we abort */
+			return 0;
+		break;
=== END ===
=== LEVEL 2 ===
=== pre_ctx ===

	mv = term_read(0);
	switch (mv) {
=== END ===
=== post_ctx ===
	case ',':
	case ';':
		if (!vi_charlast[0])
=== END ===
=== pattern ===

	mv = .*\(.*\);
	switch \(mv\) \{
=== END ===
=== END ===
=== PATCH2VI PATCH ===
diff --git a/vi.c b/vi.c
index 74ffc2d3..92e6ea87 100644
--- a/vi.c
+++ b/vi.c
@@ -521,6 +521,37 @@ static int vi_region(int cmd, int *row, int *off)
 
 	mv = term_read(0);
 	switch (mv) {
+	case '\033':	/* Arrow keys */
+		mv = term_read(0);
+		if (mv == '[') {
+			mv = term_read(0);
+			switch (mv) {
+			case 'A':	/* ↑ */
+				*row = MAX(*row - cnt, 0);
+				mv = 'k';
+				break;
+			case 'B':	/* ↓ */
+				*row = MIN(*row + cnt, lbuf_len(xb) - 1);
+				mv = 'j';
+				break;
+			case 'D':	/* ← */
+			case 'C':	/* → */
+				if (!(cs = lbuf_get(xb, *row)))
+					return -1;
+				dir = dir_context(lbuf_get(xb, *row));
+				if (mv == 'D')
+					dir = -dir;
+				for (i = 0; i < cnt; i++)
+					if (vi_nextcol(cs, dir, off))
+						break;
+				mv = mv == 'D' ? 'h' : 'l';
+				break;
+			default:
+				return 0;
+			}
+		} else	/* Not a 033[X command so we abort */
+			return 0;
+		break;
 	case ',':
 	case ';':
 		if (!vi_charlast[0])
