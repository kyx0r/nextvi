#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0105 0107-0110 0112 0117-0123 0125-0127 0131-0132 0136 0172 0176-0205 0207-0217 0224-0341 0343-0377
set -e

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"
# Uncomment to skip errors (. = silent nop)
#DBG="."
# Set QF=. to continue despite errors (errors are still printed)
#QF=.

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: vi.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> \\\\{
	int var, c = term_read\\\\(TK_CTL\\\\('l'\\\\)\\\\);
	switch \\\\(c\\\\) \\\\{${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 348\\${SEP}${QF}}${SEP};=
${SEP}+2a 	case '\\\\033':	/* Arrow keys */
		c = term_read(0);
		if (c == '[') {
			c = term_read(0);
			switch (c) {
			case 'A':	/* ↑ */
				*row = MAX(*row - cnt, 0);
				c = 'k';
				break;
			case 'B':	/* ↓ */
				*row = MIN(*row + cnt, lbuf_len(xb) - 1);
				c = 'j';
				break;
			default:	/* Not a line motion so we put back all the arrow characters */
				term_back('\\\\033');
				term_back('[');
				term_back(c);
				return 0;
			}
		} else	/* Not an arrow sequence so we abort */
			return 0;
		break;
.
${SEP}.,\$;f> 	\\\\}
	mv = term_read\\\\(TK_CTL\\\\('l'\\\\)\\\\);
	switch \\\\(mv\\\\) \\\\{${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 586\\${SEP}${QF}}${SEP};=
${SEP}+2a 	case '\\\\033':	/* Arrow keys */
		mv = term_read(0);
		if (mv == '[') {
			if (!(cs = lbuf_get(xb, *row)))
				return -1;
			dir = dir_context(lbuf_get(xb, *row));
			mv = term_read(0);
			switch (mv) {
			case 'D':	/* ← */
				dir = -dir;
			case 'C':	/* → */
				for (i = 0; i < cnt; i++)
					if (vi_nextcol(cs, dir, off))
						break;
				break;
			default:	/* Not a motion managed by this function so we abort */
				return 0;
			}
		} else	/* Not a 033[X command so we abort */
			return 0;
		break;
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

exit 0
=== PATCH2VI DELTA ===
=== DELTA vi.c ===
--- patch2vi_rwPZr7_vi.c.diff.orig	2026-04-21 21:29:43.573875830 -0100
+++ patch2vi_rwPZr7_vi.c.diff	2026-04-21 21:30:10.330931632 -0100
@@ -27,7 +27,7 @@
 %;f>
 === SEARCH PATTERN ===
 \{
-	int mark, c = term_read\(TK_CTL\('l'\)\);
+	int var, c = term_read\(TK_CTL\('l'\)\);
 	switch \(c\) \{
 --- extra (delete to include) ---
 	case '\\n':
=== PATCH2VI PATCH ===
diff --git a/vi.c b/vi.c
index a3d3876c..920c01e6 100644
--- a/vi.c
+++ b/vi.c
@@ -346,6 +346,28 @@ static int vi_motionln(int *row, int cmd, int cnt)
 {
 	int mark, c = term_read(TK_CTL('l'));
 	switch (c) {
+	case '\033':	/* Arrow keys */
+		c = term_read(0);
+		if (c == '[') {
+			c = term_read(0);
+			switch (c) {
+			case 'A':	/* ↑ */
+				*row = MAX(*row - cnt, 0);
+				c = 'k';
+				break;
+			case 'B':	/* ↓ */
+				*row = MIN(*row + cnt, lbuf_len(xb) - 1);
+				c = 'j';
+				break;
+			default:	/* Not a line motion so we put back all the arrow characters */
+				term_back('\033');
+				term_back('[');
+				term_back(c);
+				return 0;
+			}
+		} else	/* Not an arrow sequence so we abort */
+			return 0;
+		break;
 	case '\n':
 	case '+':
 	case 'j':
@@ -584,6 +606,27 @@ static int vi_motion(int vc, int *row, int *off)
 	}
 	mv = term_read(TK_CTL('l'));
 	switch (mv) {
+	case '\033':	/* Arrow keys */
+		mv = term_read(0);
+		if (mv == '[') {
+			if (!(cs = lbuf_get(xb, *row)))
+				return -1;
+			dir = dir_context(lbuf_get(xb, *row));
+			mv = term_read(0);
+			switch (mv) {
+			case 'D':	/* ← */
+				dir = -dir;
+			case 'C':	/* → */
+				for (i = 0; i < cnt; i++)
+					if (vi_nextcol(cs, dir, off))
+						break;
+				break;
+			default:	/* Not a motion managed by this function so we abort */
+				return 0;
+			}
+		} else	/* Not a 033[X command so we abort */
+			return 0;
+		break;
 	case ',':
 	case ';':
 		if (!vi_charlast[0])
