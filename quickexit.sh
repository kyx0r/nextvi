#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0032 0034-0037 0112 0120-0121 0126 0130 0132 0176 0200-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: conf.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%f> \\\\(\\\\?:'\\\\[a-z'\`\\\\[\\\\\\\\\\\\\\\\\\\\]\\\\*\\\\]\\\\)\\\\|\\\\(\\\\[\\\\.%\\\\\$\\\\]\\\\|\\\\[0-9 \\\\\\\\t\\\\]\\\\*\\\\)\\\\?\\\\)\\\\)\\\\(\\\\?:\\\\(\\\\[-\\\\*-\\\\+/%\\\\]\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\[0-9\\\\]\\\\+\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\(\\\\?:\\\\[ \\\\\\\\t\\\\]\\\\*\\\\\\\\\\\\\\\\\\\\|\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)\\\\\\\\\\\\\\\\\\\\|\\\\|\\\\\$\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\\\\\${SEP}??!${DBG:-re p FAIL line 289\\${SEP}p FAIL line 289${INTR}${QF}}${SEP};=
${SEP}+3${SEP}s/\\\\(p/(qe|p/${SEP}??!${DBG:-re p FAIL line 289\\${SEP}p FAIL line 289${INTR}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'conf.c'

# Patch: ex.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}i int xqe = 1000;			/* exit insert via kj (delay in ms) */
.
${SEP}%f> EO\\\\(pac\\\\)${SEP}??!${DBG:-re p FAIL line 1437\\${SEP}p FAIL line 1437${INTR}${QF}}${SEP};=
${SEP}+2a EO(qe)
.
${SEP}.,\$f> 	\\\\{\"m\", ec_mark\\\\},${SEP}??!${DBG:-re p FAIL line 1500\\${SEP}p FAIL line 1500${INTR}${QF}}${SEP};=
${SEP}.a 	EO(qe),
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

# Patch: led.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}1a static int gettime_ms(void)
{
	struct timespec t;
	if (clock_gettime(CLOCK_MONOTONIC, &t) < 0)
		return 0;
	return t.tv_sec * 1000 + t.tv_nsec / 1000000;
}

.
${SEP}%;f> 				exbuf_load\\\\(ex_buf\\\\)
			\\\\}
			continue; \\\\}${SEP}??!${DBG:-re p FAIL line 638\\${SEP}p FAIL line 638${INTR}${QF}}${SEP};=
${SEP}+2a 		case 'j':
			if (xqe && (gettime_ms() - is->quickexit) < xqe) {
				if (len - pre > 0 && sb->s[led_lastchar(sb->s)] == 'k') {
					term_push(\"\", 2);
					continue;
				}
			}
			goto _default;
		case 'k':
			is->quickexit = gettime_ms();
_default:
.
${SEP}vis 2${SEP}wq" $VI -e 'led.c'

# Patch: vi.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}9a #include <time.h>
.
${SEP}%;f> 				k = vc_insert\\\\(c\\\\);
				ins:${SEP}??!${DBG:-re p FAIL line 1532\\${SEP}p FAIL line 1532${INTR}${QF}}${SEP};=
${SEP}+2a 				if (xqe)
					vi_mod |= 2;
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

# Patch: vi.h
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 	int p_reg;
	int lsug;
	int sug_pt;${SEP}??!${DBG:-re p FAIL line 363\\${SEP}p FAIL line 363${INTR}${QF}}${SEP};=
${SEP}+2a 	int quickexit;
.
${SEP}.,\$;f> is\\\\.p_reg = 0; \\\\\\\\
is\\\\.lsug = 0; \\\\\\\\
is\\\\.sug_pt = -1; \\\\\\\\${SEP}??!${DBG:-re p FAIL line 371\\${SEP}p FAIL line 371${INTR}${QF}}${SEP};=
${SEP}+2a is.quickexit = 0; \\\\
.
${SEP}.,\$f> extern int xshape;${SEP}??!${DBG:-re p FAIL line 418\\${SEP}p FAIL line 418${INTR}${QF}}${SEP};=
${SEP}+2a extern int xqe;
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.h'

exit 0
=== PATCH2VI DELTA ===
=== DELTA conf.c ===
--- /tmp/patch2vi_DnFeGC_conf.c.diff.orig
+++ /tmp/patch2vi_DnFeGC_conf.c.diff
@@ -9,8 +9,6 @@
 %;f>
 === SEARCH PATTERN ===
 \(\?:'\[a-z'`\[\\\\\]\*\]\)\|\(\[\.%\$\]\|\[0-9 \\t\]\*\)\?\)\)\(\?:\(\[-\*-\+/%\]\)\[ \\t\]\*\[0-9\]\+\[ \\t\]\*\)\*\(\?:\[ \\t\]\*\\\\\|\.\*\?\(\?:\(\?<\^\\\\\\\\\)\\\\\|\|\$\)\[ \\t\]\*\)\*\)\[ \\t\]\*\\
-\(\?:\(\[,;\]#\?\)\[ \\t\]\*\(\(\?:\\\\\|\.\*\?\(\?:\(\?<\^\\\\\\\\\)\\\\\|\|\$\)\[ \\t\]\*\)\*\(\?:\(\?:<\.\*\?\(\?:\(\?<\^\\\\\\\\\)<\|\$\)\|>\.\*\?\(\?:\(\?<\^\\\\\\\\\)>\|\$\)\)\|\\
-\(\?:'\[a-z'`\[\\\\\]\*\]\)\|\(\[\.\$\]\|\[0-9 \\t\]\*\)\?\)\)\(\?:\(\[-\*-\+/%\]\)\[ \\t\]\*\(\[0-9\]\+\)\[ \\t\]\*\)\*\(\?:\[ \\t\]\*\\\\\|\.\*\?\(\?:\(\?<\^\\\\\\\\\)\\\\\|\|\$\)\)\*\[ \\t\]\*\)\*\)\\
 --- extra (delete to include) ---
 \(\(pac\|pr\|ai\|ish\|err\|ic\|grp\|mpt\|rcm\|shape\|seq\|ts\|td\|order\|hl\[lwpr\]\?\|left\|lim\|led\|vis\)\\
 \|\[@&!=dmj\]\|\\\\\?\\\\\?\\\?!\?\|\\\\\?!\|b\[psx\]\?\|p\[uh\]\?\|ac\?\|e\[f!\]\?!\?\|f\[-\+><tdp\]\?\|inc\|i\|sc!\?\|\\
=== DELTA ex.c ===
--- patch2vi_L0zf1a_ex.c.diff.orig	2026-04-23 13:13:49.229885745 -0100
+++ patch2vi_L0zf1a_ex.c.diff	2026-04-23 13:13:59.800576659 -0100
@@ -1,7 +1,7 @@
 === GROUP 1/3 (line 0) ===
 +int xqe = 1000;			/* exit insert via kj (delay in ms) */
 === COMMAND STRATEGY (default: rel) ===
-#abs
+abs
 #rel
 %f>
 === SEARCH PATTERN ===
@@ -21,9 +21,7 @@
 #rel
 .,\$;f>
 === SEARCH PATTERN ===
-EO\(pac\) EO\(pr\) EO\(ai\) EO\(err\) EO\(ish\) EO\(ic\) EO\(mpt\)
-EO\(shape\) EO\(seq\) EO\(ts\) EO\(td\) EO\(order\) EO\(hll\) EO\(hlw\)
-EO\(hlp\) EO\(hlr\) EO\(hl\) EO\(lim\) EO\(led\) EO\(vis\)
+EO\(pac\)
 --- extra (delete to include) ---
 
 _EO\(grp, xgrp = \(!\*arg \? !xgrp : eo_val\(arg\)\) \* 2; return NULL;\)
@@ -41,8 +39,6 @@
 #rel
 .,\$;f>
 === SEARCH PATTERN ===
-	\{"g", ec_glob\},
-	EO\(mpt\),
 	\{"m", ec_mark\},
 --- extra (delete to include) ---
 	\{"q!", ec_quit\},
@@ -51,6 +47,6 @@
 === EDIT COMMAND (abs) ===
 1500a 	EO(qe),
 === EDIT COMMAND (rel) ===
-+2a 	EO(qe),
+a 	EO(qe),
 === END GROUP ===
 
=== DELTA led.c ===
--- /tmp/patch2vi_cCIldL_led.c.diff.orig
+++ /tmp/patch2vi_cCIldL_led.c.diff
@@ -8,7 +8,7 @@
 +}
 +
 === COMMAND STRATEGY (default: rel) ===
-#abs
+abs
 #rel
 %f>
 === SEARCH PATTERN ===
=== DELTA vi.c ===
--- /tmp/patch2vi_DNCbak_vi.c.diff.orig
+++ /tmp/patch2vi_DNCbak_vi.c.diff
@@ -1,7 +1,7 @@
 === GROUP 1/2 (line 9) ===
 +#include <time.h>
 === COMMAND STRATEGY (default: rel) ===
-#abs
+abs
 #rel
 %;f>
 === SEARCH PATTERN ===
@@ -29,7 +29,6 @@
 === SEARCH PATTERN ===
 				k = vc_insert\(c\);
 				ins:
-				vi_mod \|= !xpac && xrow == orow \? 8 : 1;
 --- extra (delete to include) ---
 				if \(k == 127\) \{
 					if \(xrow && !\(xoff > 0 && lbuf_eol\(xb, xrow, 1\)\)\) \{
=== DELTA vi.h ===
--- /tmp/patch2vi_lKDyRX_vi.h.diff.orig	2026-02-17 19:15:16.482674421 -0100
+++ /tmp/patch2vi_lKDyRX_vi.h.diff	2026-02-17 19:16:02.897965195 -0100
@@ -40,8 +40,6 @@
 .,\$;f>
 === SEARCH PATTERN (offset: 3) ===
 extern int xshape;
-extern int xorder;
-extern int xts;
 --- extra (delete to include) ---
 extern int xish;
 extern int xgrp;
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index d45d10a6..352facd4 100644
--- a/conf.c
+++ b/conf.c
@@ -286,7 +286,7 @@ return|select|switch|type|var))\\>", A(GR1, BL1 | SYN_BD, YE1)},
 (?:'[a-z'`[\\]*])|([.%$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*[0-9]+[ \t]*)*(?:[ \t]*\\|.*?(?:(?<^\\\\)\\||$)[ \t]*)*)[ \t]*\
 (?:([,;]#?)[ \t]*((?:\\|.*?(?:(?<^\\\\)\\||$)[ \t]*)*(?:(?:<.*?(?:(?<^\\\\)<|$)|>.*?(?:(?<^\\\\)>|$))|\
 (?:'[a-z'`[\\]*])|([.$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*([0-9]+)[ \t]*)*(?:[ \t]*\\|.*?(?:(?<^\\\\)\\||$))*[ \t]*)*)\
-((pac|pr|ai|ish|err|ic|grp|mpt|shape|seq|ts|td|order|hl[lwpr]?|left|lim|led|vis)\
+((qe|pac|pr|ai|ish|err|ic|grp|mpt|shape|seq|ts|td|order|hl[lwpr]?|left|lim|led|vis)\
 |[@&!=dmj]|\\?\\?\?!?|\\?!|b[psx]?|p[uh]?|ac?|e[f!]?!?|f[-+><tdp]?|inc|i|sc!?|\
 (?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czbd]|x!?|ya!?|cm!?|cd?)?",
 		A(BL1 | SYN_BD, RE, RE, RE, RE, WH1, MA1, RE, RE, WH1, RE, GR1, CY1, MA1)},
diff --git a/ex.c b/ex.c
index c195038b..fe5988a8 100644
--- a/ex.c
+++ b/ex.c
@@ -1,3 +1,4 @@
+int xqe = 1000;			/* exit insert via kj (delay in ms) */
 int xleft;			/* the first visible column */
 int xvis;			/* startup flags */
 int xai = 1;			/* autoindent option */
@@ -1435,6 +1436,7 @@ static void *eo_##opt(char *loc, char *cmd, char *arg) { inner }
 EO(pac) EO(pr) EO(ai) EO(err) EO(ish) EO(ic) EO(mpt)
 EO(shape) EO(seq) EO(ts) EO(td) EO(order) EO(hll) EO(hlw)
 EO(hlp) EO(hlr) EO(hl) EO(lim) EO(led) EO(vis)
+EO(qe)
 
 _EO(grp, xgrp = (!*arg ? !xgrp : eo_val(arg)) * 2; return NULL;)
 
@@ -1498,6 +1500,7 @@ static struct excmd {
 	{"g", ec_glob},
 	EO(mpt),
 	{"m", ec_mark},
+	EO(qe),
 	{"q!", ec_quit},
 	{"q", ec_quit},
 	{"reg+", ec_regprint},
diff --git a/led.c b/led.c
index 6a5e065f..1b43d40d 100644
--- a/led.c
+++ b/led.c
@@ -1,4 +1,12 @@
 static sbuf *suggestsb;
+static int gettime_ms(void)
+{
+	struct timespec t;
+	if (clock_gettime(CLOCK_MONOTONIC, &t) < 0)
+		return 0;
+	return t.tv_sec * 1000 + t.tv_nsec / 1000000;
+}
+
 static sbuf *acsb;
 sbuf *led_attsb;
 
@@ -636,6 +644,17 @@ static int led_line(sbuf *sb, int ps, int pre, char **post, int postn, char **po
 				exbuf_load(ex_buf)
 			}
 			continue; }
+		case 'j':
+			if (xqe && (gettime_ms() - is->quickexit) < xqe) {
+				if (len - pre > 0 && sb->s[led_lastchar(sb->s)] == 'k') {
+					term_push("", 2);
+					continue;
+				}
+			}
+			goto _default;
+		case 'k':
+			is->quickexit = gettime_ms();
+_default:
 		default:
 			if (c == '\n' || TK_INT(c))
 				return c;
diff --git a/vi.c b/vi.c
index 628bb946..72e26883 100644
--- a/vi.c
+++ b/vi.c
@@ -7,6 +7,7 @@
 #include <dirent.h>
 #include <signal.h>
 #include <unistd.h>
+#include <time.h>
 #include <poll.h>
 #include <termios.h>
 #include <limits.h>
@@ -1530,6 +1531,8 @@ void vi(int init)
 				k = vc_insert(c);
 				ins:
 				vi_mod |= !xpac && xrow == orow ? 8 : 1;
+				if (xqe)
+					vi_mod |= 2;
 				if (k == 127) {
 					if (xrow && !(xoff > 0 && lbuf_eol(xb, xrow, 1))) {
 						xrow--;
diff --git a/vi.h b/vi.h
index bd944301..9b4ef8bf 100644
--- a/vi.h
+++ b/vi.h
@@ -361,6 +361,7 @@ typedef struct {
 	int p_reg;
 	int lsug;
 	int sug_pt;
+	int quickexit;
 	char *sug;
 	char *_sug;
 } ins_state;
@@ -369,6 +370,7 @@ is.t_row = -2; \
 is.p_reg = 0; \
 is.lsug = 0; \
 is.sug_pt = -1; \
+is.quickexit = 0; \
 is.sug = NULL; \
 is._sug = NULL; \
 
@@ -416,6 +418,7 @@ extern int xtd;
 extern int xshape;
 extern int xorder;
 extern int xts;
+extern int xqe;
 extern int xish;
 extern int xgrp;
 extern int xpac;
