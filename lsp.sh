#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0102-0103 0107-0110 0112 0121-0122 0126-0127 0131-0132 0136 0177-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: ex.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 	if \\\\(cd == 3 \\\\|\\\\| \\\\(!rd && fd >= 0\\\\)\\\\) \\\\{
		ex_bufpostfix\\\\(ex_buf, arg\\\\[0\\\\]\\\\);
		syn_setft\\\\(xb_ft\\\\);${SEP}??!${DBG:-re p FAIL line 375\\${SEP}p FAIL line 375${INTR}${QF}}${SEP};=
${SEP}+2a 		if (*xb_path && xb_ft)
			lsp_open(xb_path, xb_ft);
.
${SEP}.,\$;f> 			ec_setpath\\\\(NULL, NULL, path\\\\);
		lbuf_saved\\\\(xb, 0\\\\);
		ex_buf->mtime = mtime\\\\(path\\\\);${SEP}??!${DBG:-re p FAIL line 714\\${SEP}p FAIL line 714${INTR}${QF}}${SEP};=
${SEP}+2a 		if (*xb_path)
			lsp_save(xb_path);
.
${SEP}.,\$;f> 	return NULL;
\\\\)

${SEP}??!${DBG:-re p FAIL line 1451\\${SEP}p FAIL line 1451${INTR}${QF}}${SEP};=
${SEP}+2a static void *ec_lsp(char *loc, char *cmd, char *arg)
{
	char ft[32];
	int n = 0;
	if (!*arg) { lsp_list(); return NULL; }
	while (arg[n] && arg[n] != ' ' && n < (int)sizeof(ft)-1) {
		ft[n] = arg[n];
		n++;
	}
	ft[n] = '\\\\0';
	while (arg[n] == ' ') n++;
	if (!arg[n]) return \"lsp: missing server command\";
	lsp_register(ft, arg+n);
	return NULL;
	(void)loc; (void)cmd;
}

.
${SEP}.,\$;f> 	EO\\\\(hlp\\\\),
	EO\\\\(hlr\\\\),
	EO\\\\(hl\\\\),${SEP}??!${DBG:-re p FAIL line 1539\\${SEP}p FAIL line 1539${INTR}${QF}}${SEP};=
${SEP}+2a 	{\"lsp\", ec_lsp},
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

# Patch: term.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 
int term_read\\\\(int winch\\\\)
\\\\{${SEP}??!${DBG:-re p FAIL line 152\\${SEP}p FAIL line 152${INTR}${QF}}${SEP};=
${SEP}+3d${SEP}.,\$;f> 			goto ret;
		\\\\}
		cw = 0;${SEP}??!${DBG:-re p FAIL line 165\\${SEP}p FAIL line 165${INTR}${QF}}${SEP};=
${SEP}+3,#+9c 		re:;
		{
			struct pollfd pfds[1 + LSP_NFDS_MAX];
			int _nr, _i;
			pfds[0].fd = STDIN_FILENO;
			pfds[0].events = POLLIN;
			for (_i = 0; _i < lsp_nfds; _i++) {
				pfds[1+_i].fd = lsp_fds[_i];
				pfds[1+_i].events = POLLIN;
			}
			if (xquit < 0 || (_nr = poll(pfds, 1+lsp_nfds, -1)) <= 0) {
				xquit = !isatty(STDIN_FILENO) ? -1 : xquit;
				if (term_winch && winch && xquit >= 0) {
					*ibuf = winch;
					goto ret;
				} else if (term_winch != cw && !winch && xquit >= 0) {
					cw = term_winch;
					goto re;
				}
				goto err;
			}
			if (lsp_fd_ready)
				for (_i = 0; _i < lsp_nfds; _i++)
					if (pfds[1+_i].revents & POLLIN)
						lsp_fd_ready(lsp_fds[_i]);
			if (!(pfds[0].revents & POLLIN)) {
				if (term_winch && winch) {
					*ibuf = winch;
					goto ret;
				}
.
${SEP}.,\$;f> 				goto re;
			\\\\}${SEP}??!${DBG:-re p FAIL line 177\\${SEP}p FAIL line 177${INTR}${QF}}${SEP};=
${SEP}+2,#+1c 			if (read(STDIN_FILENO, ibuf, 1) <= 0) {
				xquit = !isatty(STDIN_FILENO) ? -1 : xquit;
				if (term_winch && winch && xquit >= 0) {
					*ibuf = winch;
					goto ret;
				} else if (term_winch != cw && !winch && xquit >= 0) {
					cw = term_winch;
					goto re;
				}
				goto err;
			}
			goto ret;
.
${SEP}.,\$f> 		\\\\}${SEP}??!${DBG:-re p FAIL line 179\\${SEP}p FAIL line 179${INTR}${QF}}${SEP};=
${SEP}.a 		err:
		*ibuf = 0;
.
${SEP}vis 2${SEP}wq" $VI -e 'term.c'

# Patch: vi.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> #include \"ren\\\\.c\"
#include \"term\\\\.c\"
#include \"uc\\\\.c\"${SEP}??!${DBG:-re p FAIL line 24\\${SEP}p FAIL line 24${INTR}${QF}}${SEP};=
${SEP}+2a #include \"lsp.c\"
.
${SEP}.,\$;f> \\\\}
#define vi_drawmsg_mpt\\\\(msg\\\\) \\\\{ vi_drawmsg\\\\(msg\\\\); if \\\\(!xmpt\\\\) xmpt = 1; \\\\}

${SEP}??!${DBG:-re p FAIL line 104\\${SEP}p FAIL line 104${INTR}${QF}}${SEP};=
${SEP}+2a void lsp_show_msg(char *msg) { vi_drawmsg_mpt(msg) }

.
${SEP}.,\$;f> 				\\\\} else if \\\\(k == '~' \\\\|\\\\| k == 'u' \\\\|\\\\| k == 'U'\\\\) \\\\{
					vc_motion\\\\(k\\\\);
					goto rep;${SEP}??!${DBG:-re p FAIL line 1637\\${SEP}p FAIL line 1637${INTR}${QF}}${SEP};=
${SEP}+2a 				} else if (k == 'K') {
					if (xb_path && xb_path[0])
						lsp_hover(xb_path, xrow, xoff);
				} else if (k == 'd') {
					if (xb_path && xb_path[0])
						lsp_definition(xb_path, xrow, xoff);
.
${SEP}.,\$;f> 			syn_blockhl = -1;
			vi_drawrow\\\\(xrow\\\\);
		\\\\}${SEP}??!${DBG:-re p FAIL line 1800\\${SEP}p FAIL line 1800${INTR}${QF}}${SEP};=
${SEP}+2a 		if (!xmpt && xb_path && xb_path[0]) {
			const char *_ldiag = lsp_diag_for_line(xb_path, xrow);
			if (_ldiag)
				vi_drawmsg((char *)_ldiag);
		}
.
${SEP}.,\$;f> 	setup_signals\\\\(\\\\);
	dir_init\\\\(\\\\);
	syn_init\\\\(\\\\);${SEP}??!${DBG:-re p FAIL line 1839\\${SEP}p FAIL line 1839${INTR}${QF}}${SEP};=
${SEP}+2a 	lsp_init();
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

# Patch: vi.h
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> /\\\\* filesystem \\\\*/
extern rset \\\\*fsincl;
void dir_calc\\\\(char \\\\*path\\\\);${SEP}??!${DBG:-re p FAIL line 540\\${SEP}p FAIL line 540${INTR}${QF}}${SEP};=
${SEP}+2a 
/* lsp.c */
#define LSP_NFDS_MAX	8
extern int lsp_nfds;
extern int lsp_fds[LSP_NFDS_MAX];
extern void (*lsp_fd_ready)(int fd);
void lsp_init(void);
void lsp_register(const char *ft, const char *cmd);
void lsp_open(const char *path, const char *ft);
void lsp_save(const char *path);
void lsp_hover(const char *path, int row, int off);
void lsp_definition(const char *path, int row, int off);
const char *lsp_diag_for_line(const char *path, int line);
void lsp_list(void);
void lsp_show_msg(char *msg);
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.h'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/ex.c b/ex.c
index 36b8a6d6..a98f6bf2 100644
--- a/ex.c
+++ b/ex.c
@@ -373,6 +373,8 @@ static void *ec_edit(char *loc, char *cmd, char *arg)
 	if (cd == 3 || (!rd && fd >= 0)) {
 		ex_bufpostfix(ex_buf, arg[0]);
 		syn_setft(xb_ft);
+		if (*xb_path && xb_ft)
+			lsp_open(xb_path, xb_ft);
 	}
 	snprintf(msg, sizeof(msg), "\"%s\" %dL [%c]",
 			*xb_path ? xb_path : "unnamed", lbuf_len(xb),
@@ -712,6 +714,8 @@ static void *ec_write(char *loc, char *cmd, char *arg)
 			ec_setpath(NULL, NULL, path);
 		lbuf_saved(xb, 0);
 		ex_buf->mtime = mtime(path);
+		if (*xb_path)
+			lsp_save(xb_path);
 	}
 	if (cmd[0] == 'x' || (cmd[0] == 'w' && cmd[1] == 'q'))
 		ec_quit("", cmd, "");
@@ -1449,6 +1453,23 @@ _EO(left,
 	return NULL;
 )
 
+static void *ec_lsp(char *loc, char *cmd, char *arg)
+{
+	char ft[32];
+	int n = 0;
+	if (!*arg) { lsp_list(); return NULL; }
+	while (arg[n] && arg[n] != ' ' && n < (int)sizeof(ft)-1) {
+		ft[n] = arg[n];
+		n++;
+	}
+	ft[n] = '\0';
+	while (arg[n] == ' ') n++;
+	if (!arg[n]) return "lsp: missing server command";
+	lsp_register(ft, arg+n);
+	return NULL;
+	(void)loc; (void)cmd;
+}
+
 #undef EO
 #define EO(opt) {#opt, eo_##opt}
 
@@ -1537,6 +1558,7 @@ static struct excmd {
 	EO(hlp),
 	EO(hlr),
 	EO(hl),
+	{"lsp", ec_lsp},
 	EO(left),
 	EO(lim),
 	EO(led),
diff --git a/term.c b/term.c
index 68990b78..8b893398 100644
--- a/term.c
+++ b/term.c
@@ -149,7 +149,6 @@ void term_back(int c)
 
 int term_read(int winch)
 {
-	static struct pollfd ufd = {STDIN_FILENO, POLLIN};
 	int cw;
 	if (ibuf_pos >= ibuf_cnt) {
 		if (texec) {
@@ -162,21 +161,53 @@ int term_read(int winch)
 			goto ret;
 		}
 		cw = 0;
-		re:
-		/* read a single input character */
-		if (xquit < 0 || poll(&ufd, 1, -1) <= 0 ||
-				read(STDIN_FILENO, ibuf, 1) <= 0) {
-			xquit = !isatty(STDIN_FILENO) ? -1 : xquit;
-			if (term_winch && winch && xquit >= 0) {
-				*ibuf = winch;
-				goto ret;
-			} else if (term_winch != cw && !winch && xquit >= 0) {
-				cw = term_winch;
+		re:;
+		{
+			struct pollfd pfds[1 + LSP_NFDS_MAX];
+			int _nr, _i;
+			pfds[0].fd = STDIN_FILENO;
+			pfds[0].events = POLLIN;
+			for (_i = 0; _i < lsp_nfds; _i++) {
+				pfds[1+_i].fd = lsp_fds[_i];
+				pfds[1+_i].events = POLLIN;
+			}
+			if (xquit < 0 || (_nr = poll(pfds, 1+lsp_nfds, -1)) <= 0) {
+				xquit = !isatty(STDIN_FILENO) ? -1 : xquit;
+				if (term_winch && winch && xquit >= 0) {
+					*ibuf = winch;
+					goto ret;
+				} else if (term_winch != cw && !winch && xquit >= 0) {
+					cw = term_winch;
+					goto re;
+				}
+				goto err;
+			}
+			if (lsp_fd_ready)
+				for (_i = 0; _i < lsp_nfds; _i++)
+					if (pfds[1+_i].revents & POLLIN)
+						lsp_fd_ready(lsp_fds[_i]);
+			if (!(pfds[0].revents & POLLIN)) {
+				if (term_winch && winch) {
+					*ibuf = winch;
+					goto ret;
+				}
 				goto re;
 			}
-			err:
-			*ibuf = 0;
+			if (read(STDIN_FILENO, ibuf, 1) <= 0) {
+				xquit = !isatty(STDIN_FILENO) ? -1 : xquit;
+				if (term_winch && winch && xquit >= 0) {
+					*ibuf = winch;
+					goto ret;
+				} else if (term_winch != cw && !winch && xquit >= 0) {
+					cw = term_winch;
+					goto re;
+				}
+				goto err;
+			}
+			goto ret;
 		}
+		err:
+		*ibuf = 0;
 		ret:
 		ibuf_cnt = 1;
 		ibuf_pos = 0;
diff --git a/vi.c b/vi.c
index 628bb946..136895f6 100644
--- a/vi.c
+++ b/vi.c
@@ -22,6 +22,7 @@
 #include "ren.c"
 #include "term.c"
 #include "uc.c"
+#include "lsp.c"
 
 int vi_hidch;			/* show hidden chars */
 int vi_lncol;			/* line numbers cursor offset */
@@ -102,6 +103,8 @@ static void vi_drawmsg(char *msg)
 }
 #define vi_drawmsg_mpt(msg) { vi_drawmsg(msg); if (!xmpt) xmpt = 1; }
 
+void lsp_show_msg(char *msg) { vi_drawmsg_mpt(msg) }
+
 static int vi_nextcol(char *ln, int dir, int *off)
 {
 	int o = ren_off(ln, ren_next(ln, ren_pos(ln, *off), dir));
@@ -1635,6 +1638,12 @@ void vi(int init)
 				} else if (k == '~' || k == 'u' || k == 'U') {
 					vc_motion(k);
 					goto rep;
+				} else if (k == 'K') {
+					if (xb_path && xb_path[0])
+						lsp_hover(xb_path, xrow, xoff);
+				} else if (k == 'd') {
+					if (xb_path && xb_path[0])
+						lsp_definition(xb_path, xrow, xoff);
 				}
 				break;
 			case 'x':
@@ -1798,6 +1807,11 @@ void vi(int init)
 			syn_blockhl = -1;
 			vi_drawrow(xrow);
 		}
+		if (!xmpt && xb_path && xb_path[0]) {
+			const char *_ldiag = lsp_diag_for_line(xb_path, xrow);
+			if (_ldiag)
+				vi_drawmsg((char *)_ldiag);
+		}
 		if (vi_status && xmpt < 1) {
 			xrows -= term_resized != vi_status;
 			vi_status = term_resized;
@@ -1837,6 +1851,7 @@ int main(int argc, char *argv[])
 	setup_signals();
 	dir_init();
 	syn_init();
+	lsp_init();
 	temp_open(0, "/hist/", _ft);
 	temp_open(1, "/fm/", fm_ft);
 	temp_open(2, "/sc/", _ft);
diff --git a/vi.h b/vi.h
index bd944301..b5b60318 100644
--- a/vi.h
+++ b/vi.h
@@ -538,3 +538,18 @@ extern int vi_lncol;
 /* filesystem */
 extern rset *fsincl;
 void dir_calc(char *path);
+
+/* lsp.c */
+#define LSP_NFDS_MAX	8
+extern int lsp_nfds;
+extern int lsp_fds[LSP_NFDS_MAX];
+extern void (*lsp_fd_ready)(int fd);
+void lsp_init(void);
+void lsp_register(const char *ft, const char *cmd);
+void lsp_open(const char *path, const char *ft);
+void lsp_save(const char *path);
+void lsp_hover(const char *path, int row, int off);
+void lsp_definition(const char *path, int row, int off);
+const char *lsp_diag_for_line(const char *path, int line);
+void lsp_list(void);
+void lsp_show_msg(char *msg);
