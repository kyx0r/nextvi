diff --git a/vi.c b/vi.c
index 7845c06..5cd9904 100644
--- a/vi.c
+++ b/vi.c
@@ -809,6 +809,7 @@ static int vi_motion(int *row, int *off)
 			return -1;
 		break;
 	case TK_CTL('a'):
+	case '*':
 		if (!(cs = vi_curword(xb, *row, *off, cnt, 0)))
 			return -1;
 		ex_krsset(cs, +1);
