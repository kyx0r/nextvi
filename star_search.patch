diff --git a/vi.c b/vi.c
index 32f68bc..f012005 100644
--- a/vi.c
+++ b/vi.c
@@ -891,6 +891,7 @@ static int vi_motion(int *row, int *off)
 			return -1;
 		break;
 	case TK_CTL('a'):
+	case '*':
 		if (!(cs = vi_curword(xb, *row, *off, cnt, 0)))
 			return -1;
 		ex_krsset(cs, +1);
