#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0112-0113 0121 0126 0131-0132 0136 0177-0377
set -e

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"
# Uncomment to skip errors (. = silent nop)
#DBG="."
# Set QF=. to continue despite errors (errors are still printed)
#QF=.

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: ex.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> sbuf \\\\*xacreg;			/\\\\* autocomplete db filter regex \\\\*/
rset \\\\*xkwdrs;			/\\\\* the last searched keyword rset \\\\*/
sbuf \\\\*xregs\\\\[256\\\\];		/\\\\* string registers \\\\*/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 40\\${SEP}${QF}}${SEP};=
${SEP}+2a int xexrc = 0;			/* read .exrc from the current directory */
.
${SEP}.,\$f> EO\\\\(pac\\\\)${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1382\\${SEP}${QF}}${SEP};=
${SEP}+2a EO(exrc)
.
${SEP}.,\$;f> 	EO\\\\(ai\\\\),
	\\\\{\"ac\", ec_setacreg\\\\},
	\\\\{\"a\", ec_insert\\\\},${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1420\\${SEP}${QF}}${SEP};=
${SEP}+2a 	EO(exrc),
.
${SEP}.,\$;f> 	xgrec--;
\\\\}

${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1645\\${SEP}${QF}}${SEP};=
${SEP}+2a void ex_script(FILE *fp)
{
	char done = 0;
	do {
		size_t n = 128, i = 0;
		int c;
		char *ln = malloc(128);
		while ((c = fgetc(fp)) != EOF && c != '\\\\n') {
			if (i >= n - 2) {
				n += 128;
				ln = erealloc(ln, n);
			}
			ln[i++] = c;
		}
		if (!i) {
			free(ln);
			done = 1;
			break;
		}
		ln[i] = '\\\\0';
		ex_command(ln);
		free(ln);
	} while(!done);
}

void load_exrc(char *exrc)
{
	struct stat st;
	if (stat(exrc, &st) == 0) {
		if (st.st_uid == getuid() && !(st.st_mode & S_IWGRP) && !(st.st_mode & S_IWOTH)) {
			FILE *fp = fopen(exrc, \"r\");
			if (fp) {
				ex_script(fp);
				fclose(fp);
			} else {
				fprintf(stderr, \"Cannot open %s\\\\n\", exrc);
				exit(EXIT_FAILURE);
			}
		} else {
			fprintf(stderr, \"Bad permissions on %s\\\\n\", exrc);
			exit(EXIT_FAILURE);
		}
	}
}

.
${SEP}.,\$;f> 		s = \\\\*\\\\(\\\\+\\\\+files\\\\);
	\\\\} while \\\\(--n > 0\\\\);
	xvis &= ~4;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1657\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/\\\\)\\\\)\\\\)/))) {/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1657\\${SEP}${QF}}${SEP}.,\$f> 		ex_command\\\\(s\\\\)${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1658\\${SEP}${QF}}${SEP};=
${SEP}.a 	} else {
		char *homeenv = getenv(\"HOME\");
		char *xdgconfighomeenv = getenv(\"XDG_CONFIG_HOME\");
		if (xdgconfighomeenv) {
			char exrc[PATH_MAX];
			snprintf(exrc, sizeof(exrc), \"%s/nextvi/exrc\", xdgconfighomeenv);
			load_exrc(exrc);
		} else if (homeenv) {
			char exrc[PATH_MAX];
			snprintf(exrc, sizeof(exrc), \"%s/.config/nextvi/exrc\", homeenv);
			load_exrc(exrc);
		}
		if (homeenv) {
			char exrc[PATH_MAX];
			snprintf(exrc, sizeof(exrc), \"%s/.exrc\", homeenv);
			load_exrc(exrc);
		}
	}
	if (xexrc) {
		char buf[PATH_MAX];
		getcwd(buf, PATH_MAX);
		if (strcmp(buf, getenv(\"HOME\")) != 0)
			load_exrc(\".exrc\");
	}
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

exit 0
=== PATCH2VI DELTA ===
=== DELTA ex.c ===
--- /tmp/patch2vi_yU53j4_ex.c.diff.orig	2026-04-14 10:26:35.567577335 -0100
+++ /tmp/patch2vi_yU53j4_ex.c.diff	2026-04-14 10:26:42.533997148 -0100
@@ -26,9 +26,7 @@
 #rel
 .,\$;f>
 === SEARCH PATTERN ===
-EO\(pac\) EO\(pr\) EO\(ai\) EO\(err\) EO\(ish\) EO\(ic\) EO\(grp\) EO\(mpt\) EO\(rcm\)
-EO\(shape\) EO\(seq\) EO\(ts\) EO\(td\) EO\(order\) EO\(hll\) EO\(hlw\)
-EO\(hlp\) EO\(hlr\) EO\(hl\) EO\(lim\) EO\(led\) EO\(vis\)
+EO\(pac\)
 --- extra (delete to include) ---
 
 _EO\(left,
=== PATCH2VI PATCH ===
diff --git a/ex.c b/ex.c
index 81878d89..d0bb0162 100644
--- a/ex.c
+++ b/ex.c
@@ -38,6 +38,7 @@ int xesc = '\\';		/* ex command arg escape character */
 sbuf *xacreg;			/* autocomplete db filter regex */
 rset *xkwdrs;			/* the last searched keyword rset */
 sbuf *xregs[256];		/* string registers */
+int xexrc = 0;			/* read .exrc from the current directory */
 struct buf *bufs;		/* main buffers */
 struct buf tempbufs[2];		/* temporary buffers, for internal use */
 struct buf *ex_buf;		/* current buffer */
@@ -1380,6 +1381,7 @@ static void *eo_##opt(char *loc, char *cmd, char *arg) { inner }
 EO(pac) EO(pr) EO(ai) EO(err) EO(ish) EO(ic) EO(grp) EO(mpt) EO(rcm)
 EO(shape) EO(seq) EO(ts) EO(td) EO(order) EO(hll) EO(hlw)
 EO(hlp) EO(hlr) EO(hl) EO(lim) EO(led) EO(vis)
+EO(exrc)
 
 _EO(left,
 	if (*loc)
@@ -1418,6 +1420,7 @@ static struct excmd {
 	EO(ai),
 	{"ac", ec_setacreg},
 	{"a", ec_insert},
+	EO(exrc),
 	EO(err),
 	{"ef!", ec_fuzz},
 	{"ef", ec_fuzz},
@@ -1643,6 +1646,51 @@ void ex(void)
 	xgrec--;
 }
 
+void ex_script(FILE *fp)
+{
+	char done = 0;
+	do {
+		size_t n = 128, i = 0;
+		int c;
+		char *ln = malloc(128);
+		while ((c = fgetc(fp)) != EOF && c != '\n') {
+			if (i >= n - 2) {
+				n += 128;
+				ln = erealloc(ln, n);
+			}
+			ln[i++] = c;
+		}
+		if (!i) {
+			free(ln);
+			done = 1;
+			break;
+		}
+		ln[i] = '\0';
+		ex_command(ln);
+		free(ln);
+	} while(!done);
+}
+
+void load_exrc(char *exrc)
+{
+	struct stat st;
+	if (stat(exrc, &st) == 0) {
+		if (st.st_uid == getuid() && !(st.st_mode & S_IWGRP) && !(st.st_mode & S_IWOTH)) {
+			FILE *fp = fopen(exrc, "r");
+			if (fp) {
+				ex_script(fp);
+				fclose(fp);
+			} else {
+				fprintf(stderr, "Cannot open %s\n", exrc);
+				exit(EXIT_FAILURE);
+			}
+		} else {
+			fprintf(stderr, "Bad permissions on %s\n", exrc);
+			exit(EXIT_FAILURE);
+		}
+	}
+}
+
 void ex_init(char **files, int n)
 {
 	xbufsalloc = MAX(n, xbufsalloc);
@@ -1654,6 +1702,30 @@ void ex_init(char **files, int n)
 		s = *(++files);
 	} while (--n > 0);
 	xvis &= ~4;
-	if ((s = getenv("EXINIT")))
+	if ((s = getenv("EXINIT"))) {
 		ex_command(s)
+	} else {
+		char *homeenv = getenv("HOME");
+		char *xdgconfighomeenv = getenv("XDG_CONFIG_HOME");
+		if (xdgconfighomeenv) {
+			char exrc[PATH_MAX];
+			snprintf(exrc, sizeof(exrc), "%s/nextvi/exrc", xdgconfighomeenv);
+			load_exrc(exrc);
+		} else if (homeenv) {
+			char exrc[PATH_MAX];
+			snprintf(exrc, sizeof(exrc), "%s/.config/nextvi/exrc", homeenv);
+			load_exrc(exrc);
+		}
+		if (homeenv) {
+			char exrc[PATH_MAX];
+			snprintf(exrc, sizeof(exrc), "%s/.exrc", homeenv);
+			load_exrc(exrc);
+		}
+	}
+	if (xexrc) {
+		char buf[PATH_MAX];
+		getcwd(buf, PATH_MAX);
+		if (strcmp(buf, getenv("HOME")) != 0)
+			load_exrc(".exrc");
+	}
 }
