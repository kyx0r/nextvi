#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 
set -e

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: ex.c
SEP="$(printf '\x01')"
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 				bit 1: print errors, bit 2: early return, bit 3: ignore errors \\\\*/
int xrcm = 1;			/\\\\* range command model -
				0: exec at command parse 1: exec at command \\\\*/${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 25\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a int xlw;			/* soft linewrap col */
.
${SEP}.,$;f+ 
static void \\\\*ec_null\\\\(char \\\\*loc, char \\\\*cmd, char \\\\*arg\\\\) \\\\{ return NULL; \\\\}
${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 1331\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a static void *ec_linewrap(char *loc, char *cmd, char *arg)
{
	int fd;
	if (xb->modified)
		return \"unsaved changes present\";
	if (*arg)
		xlw = atoi(arg);
	else
		xlw = xcols;
	lbuf_free(xb);
	xb = lbuf_make();
	readfile()
	ex_bufpostfix(ex_buf, 1);
	syn_setft(xb_ft);
	return NULL;
}

.
${SEP}.,$;f+ 	EO\\\\(left\\\\),
	EO\\\\(lim\\\\),
	EO\\\\(led\\\\),${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 1448\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 	{\"lw\", ec_linewrap},
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

# Patch: lbuf.c
SEP="$(printf '\x01')"
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> \\\\{
	int i, pos = lo->pos;
	if \\\\(s\\\\) \\\\{${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 84\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 		char *lwp = NULL;
.
${SEP}.,$;f+ 			struct linfo \\\\*n = emalloc\\\\(l_nonl \\\\+ 5 \\\\+ sizeof\\\\(struct linfo\\\\)\\\\);
			n->len = l_nonl;
			n->grec = 0;${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 90\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 			n->lw_prev = NULL;
			n->lw_next = NULL;
.
${SEP}.,$;f+ 			memcpy\\\\(ln, s, l_nonl\\\\);
			memset\\\\(&ln\\\\[l_nonl \\\\+ 1\\\\], 0, 4\\\\);	/\\\\* fault tolerance pad \\\\*/
			ln\\\\[l_nonl\\\\] = '\\\\\\\\n';${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 94\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 			if (xlw) {
				rstate->s = NULL;
				ren_state *r = ren_position(ln);
				if (r->cmax > xlw) {
					l_nonl = l;
					l = uc_chr(r->s, r->col[xlw]) - r->s;
					if (l <= 0) {
						l = l_nonl;
						goto too_small;
					}
					l_nonl = l - (ln[l - !!l] == '\\\\n');
					n = erealloc(n, l_nonl + 5 + sizeof(struct linfo));
					n->len = l_nonl;
					n->grec = 0;
					n->lw_prev = lwp;
					n->lw_next = NULL;
					ln = (char*)(n + 1);
					if (lwp)
						lbuf_s(lwp)->lw_next = ln;
					lwp = ln;
					memcpy(ln, s, l_nonl);
					memset(&ln[l_nonl + 1], 0, 4);	/* fault tolerance pad */
					ln[l_nonl] = '\\\\n';
				} else {
					if (lwp)
						lbuf_s(lwp)->lw_next = ln;
					n->lw_prev = lwp;
					lwp = NULL;
				}
			}
			too_small:
.
${SEP}.,$;f+ 		end = lb->ln_n;
	if \\\\(beg == end && !buf\\\\)
		return;${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 190\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 	/* save chain boundary pointers before edit */
	char *chain_pred = NULL, *chain_succ = NULL;
	if (xlw) {
		if (beg < end) {
			/* deletion case: save the chain links of deleted range */
			chain_pred = lbuf_s(lb->ln[beg])->lw_prev;
			chain_succ = lbuf_s(lb->ln[end - 1])->lw_next;
		} else if (beg > 0 && beg < lb->ln_n) {
			/* pure insertion: check if inserting into middle of a chain */
			char *prev_ln = lb->ln[beg - 1];
			char *next_ln = lb->ln[beg];
			if (lbuf_s(prev_ln)->lw_next == next_ln) {
				/* inserting into a chain - need to break/relink */
				chain_pred = prev_ln;
				chain_succ = next_ln;
			}
		}
	}
.
${SEP}.,$;f+ 	struct lopt \\\\*lo = lbuf_opt\\\\(lb, beg, o1, end - beg\\\\);
	sbuf_smake\\\\(sb, sizeof\\\\(lo->ins\\\\[0\\\\]\\\\)\\\\+1\\\\)
	lo->n_ins = lbuf_replace\\\\(lb, sb, buf, lo, lo->n_del, 0\\\\);${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 193\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 	/* relink the chain after edit */
	if (xlw && (chain_pred || chain_succ)) {
		if (lo->n_ins > 0) {
			char *first_new = lb->ln[beg];
			char *last_new = lb->ln[beg + lo->n_ins - 1];
			if (chain_pred) {
				lbuf_s(chain_pred)->lw_next = first_new;
				lbuf_s(first_new)->lw_prev = chain_pred;
			}
			if (chain_succ) {
				lbuf_s(last_new)->lw_next = chain_succ;
				lbuf_s(chain_succ)->lw_prev = last_new;
			}
		} else {
			/* no new lines inserted, link predecessor to successor */
			if (chain_pred)
				lbuf_s(chain_pred)->lw_next = chain_succ;
			if (chain_succ)
				lbuf_s(chain_succ)->lw_prev = chain_pred;
		}
	}
.
${SEP}.,$;f+ 	for \\\\(int i = beg; i < end; i\\\\+\\\\+\\\\) \\\\{
		char \\\\*ln = lb->ln\\\\[i\\\\];
		long nw = 0;${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 240\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+3;30;31c (!lbuf_s(ln)->lw_next)
.
${SEP}.,$;f+ 		lo->ref = 1;
		sb\\\\.s = \\\\(char\\\\*\\\\)lo->del;
		lbuf_replace\\\\(lb, &sb, NULL, lo, lo->n_ins, lo->n_del\\\\);${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 399\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 		/* relink chain: restored lines have original pointers */
		if (xlw) {
			for (int i = 0; i < lo->n_del; i++) {
				char *ln = lo->del[i];
				char *prev = lbuf_s(ln)->lw_prev;
				char *next = lbuf_s(ln)->lw_next;
				if (prev)
					lbuf_s(prev)->lw_next = ln;
				if (next)
					lbuf_s(next)->lw_prev = ln;
			}
			/* pure insertion undo: link the chain around removed lines */
			if (lo->n_ins > 0 && lo->n_del == 0) {
				char *pred = lbuf_s(lo->ins[0])->lw_prev;
				char *succ = lbuf_s(lo->ins[lo->n_ins - 1])->lw_next;
				if (pred)
					lbuf_s(pred)->lw_next = succ;
				if (succ)
					lbuf_s(succ)->lw_prev = pred;
			}
		}
.
${SEP}.,$;f+ 		lo->ref = 2;
		sb\\\\.s = \\\\(char\\\\*\\\\)lo->ins;
		lbuf_replace\\\\(lb, &sb, NULL, lo, lo->n_del, lo->n_ins\\\\);${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 421\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 		/* relink chain: restored lines have their pointers from edit time */
		if (xlw) {
			for (int i = 0; i < lo->n_ins; i++) {
				char *ln = lo->ins[i];
				char *prev = lbuf_s(ln)->lw_prev;
				char *next = lbuf_s(ln)->lw_next;
				if (prev)
					lbuf_s(prev)->lw_next = ln;
				if (next)
					lbuf_s(next)->lw_prev = ln;
			}
			/* pure deletion redo: link the chain around removed lines */
			if (lo->n_del > 0 && lo->n_ins == 0) {
				char *pred = lbuf_s(lo->del[0])->lw_prev;
				char *succ = lbuf_s(lo->del[lo->n_del - 1])->lw_next;
				if (pred)
					lbuf_s(pred)->lw_next = succ;
				if (succ)
					lbuf_s(succ)->lw_prev = pred;
			}
		}
.
${SEP}vis 2${SEP}wq" $VI -e 'lbuf.c'

# Patch: vi.c
SEP="$(printf '\x01')"
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 		return;
	\\\\}
	s = lbuf_get\\\\(xb, row\\\\);${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 171\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 	if (xlw && s) {
		led_att la;
		if (!led_attsb)
			sbuf_make(led_attsb, sizeof(la))
		if (lbuf_s(s)->lw_next && !lbuf_s(s)->lw_prev) {
			la.s = s;
			la.off = 0;
			la.att = SYN_BGMK(8);
			sbuf_mem(led_attsb, &la, (int)sizeof(la))
		} else if (!lbuf_s(s)->lw_next && lbuf_s(s)->lw_prev) {
			la.s = s;
			la.off = ren_position(s)->n-1;
			la.att = SYN_BGMK(9);
			sbuf_mem(led_attsb, &la, (int)sizeof(la))
		}
	}
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

# Patch: vi.h
SEP="$(printf '\x01')"
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> struct linfo \\\\{
	int len;
	int grec;${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 148\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a 	char *lw_prev;
	char *lw_next;
.
${SEP}.,$;f+ extern int xlim;
extern int xseq;
extern int xerr;${SEP}??!${DBG:-.-5,.+5p\\${SEP}p FAIL line 435\\${SEP}vis 2\\${SEP}q! 1}${SEP};=
${SEP}.+2a extern int xlw;
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.h'
