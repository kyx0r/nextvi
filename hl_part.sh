#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0107-0110 0112 0120-0121 0123-0124 0126 0130-0132 0136 0176-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: led.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%f> static sbuf \\\\*suggestsb;${SEP}??!${DBG:-re p FAIL line 1\\${SEP}p FAIL line 1${INTR}${QF}}${SEP};=
${SEP}.a #define LEDBACK 300
#define LEDFORW 300

.
${SEP}.,\$;f> 	if \\\\(!xled\\\\)
		return;
	ren_state \\\\*r = ren_position\\\\(s0\\\\);${SEP}??!${DBG:-re p FAIL line 146\\${SEP}p FAIL line 146${INTR}${QF}}${SEP};=
${SEP}+2a 	int fcbeg = cbeg - LEDBACK < 0 ? 0 : cbeg - LEDBACK;
	int fcend = cend + LEDFORW;
	int fcterm = fcend - fcbeg; /* fake the render dimensions */
.
${SEP}.,\$;f> 	char \\\\*bound = NULL;
	char \\\\*\\\\*chrs = r->chrs;	/\\\\* chrs\\\\[i\\\\]: the i-th character in s0 \\\\*/
	int off\\\\[cterm\\\\+1\\\\];	/\\\\* off\\\\[i\\\\]: the character at screen position i \\\\*/${SEP}??!${DBG:-re p FAIL line 152\\${SEP}p FAIL line 152${INTR}${QF}}${SEP};=
${SEP}+3,#+2c 	int att[fcterm+1];	/* att[i]: the attributes of i-th character */
	int stt[fcterm+1];	/* stt[i]: remap off indexes */
	int ctt[fcterm+1];	/* ctt[i]: cterm bound attrs */
.
${SEP}.,\$;f> 			off\\\\[c - cbeg\\\\] = c <= r->cmax \\\\? r->col\\\\[c\\\\] : -1;
	\\\\}
	if \\\\(r->cmax > cterm \\\\|\\\\| cbeg\\\\) \\\\{${SEP}??!${DBG:-re p FAIL line 165\\${SEP}p FAIL line 165${INTR}${QF}}${SEP};=
${SEP}+2a 		c = 0;
.
${SEP}.,\$;f> 		if \\\\(o >= 0 && r->cmax > cterm && r->pos\\\\[o\\\\] \\\\+ r->wid\\\\[o\\\\] > cend\\\\)
			while \\\\(off\\\\[i\\\\] == o\\\\)
				off\\\\[ctx < 0 \\\\? i\\\\+\\\\+ : i--\\\\] = -1;${SEP}??!${DBG:-re p FAIL line 176\\${SEP}p FAIL line 176${INTR}${QF}}${SEP};=
${SEP}+3c 		l = cbeg <= r->cmax ? r->col[cbeg] : -1;
		for (o = 0; l > 0 && l <= n && o < LEDBACK; o++) {
			if (r->pos[l] < cbeg && c < fcterm) {
				atti++;
				att[c++] = l;
			}
			l -= ctx;
		}
		for (i = 0; i < cterm;) {
.
${SEP}.,\$;f> 				for \\\\(; off\\\\[i\\\\] == o; i\\\\+\\\\+\\\\);
			\\\\}
		\\\\}${SEP}??!${DBG:-re p FAIL line 181\\${SEP}p FAIL line 181${INTR}${QF}}${SEP};=
${SEP}+2a 		l = cend <= r->cmax ? r->col[cend] : -1;
		for (o = 0; l > 0 && l <= n && o < LEDFORW; o++) {
			if (r->pos[l] >= cend && c < fcterm) {
				att[c++] = l;
			}
			l += ctx;
		}
.
${SEP}.,\$;f> 		sbufn_null\\\\(bsb\\\\)
		bound = bsb->s;
	\\\\}${SEP}??!${DBG:-re p FAIL line 203\\${SEP}p FAIL line 203${INTR}${QF}}${SEP};=
${SEP}+3${SEP}s/ c/ fc/${SEP}??!${DBG:-re p FAIL line 203\\${SEP}p FAIL line 203${INTR}${QF}}${SEP}.,\$f> 	if \\\\(xhl\\\\)${SEP}??!${DBG:-re p FAIL line 205\\${SEP}p FAIL line 205${INTR}${QF}}${SEP};=
${SEP}+1${SEP}s/ c/ fc/${SEP}??!${DBG:-re p FAIL line 205\\${SEP}p FAIL line 205${INTR}${QF}}${SEP}.,\$;f> 			if \\\\(!bound\\\\)
				att\\\\[p->off\\\\] = syn_merge\\\\(att\\\\[p->off\\\\], p->att\\\\);
			else if \\\\(c && stt\\\\[0\\\\] <= p->off && stt\\\\[c-1\\\\] >= p->off\\\\) \\\\{${SEP}??!${DBG:-re p FAIL line 215\\${SEP}p FAIL line 215${INTR}${QF}}${SEP};=
${SEP}+3${SEP}s/0/atti/${SEP}??!${DBG:-re p FAIL line 215\\${SEP}p FAIL line 215${INTR}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'led.c'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/led.c b/led.c
index 6a5e065f..e1a1ddca 100644
--- a/led.c
+++ b/led.c
@@ -1,4 +1,7 @@
 static sbuf *suggestsb;
+#define LEDBACK 300
+#define LEDFORW 300
+
 static sbuf *acsb;
 sbuf *led_attsb;
 
@@ -144,14 +147,17 @@ void led_render(char *s0, int cbeg, int cend)
 	if (!xled)
 		return;
 	ren_state *r = ren_position(s0);
+	int fcbeg = cbeg - LEDBACK < 0 ? 0 : cbeg - LEDBACK;
+	int fcend = cend + LEDFORW;
+	int fcterm = fcend - fcbeg; /* fake the render dimensions */
 	int j, c, l, i, o, n = r->n;
 	int att_old = 0, atti = 0, cterm = cend - cbeg;
 	char *bound = NULL;
 	char **chrs = r->chrs;	/* chrs[i]: the i-th character in s0 */
 	int off[cterm+1];	/* off[i]: the character at screen position i */
-	int att[cterm+1];	/* att[i]: the attributes of i-th character */
-	int stt[cterm+1];	/* stt[i]: remap off indexes */
-	int ctt[cterm+1];	/* ctt[i]: cterm bound attrs */
+	int att[fcterm+1];	/* att[i]: the attributes of i-th character */
+	int stt[fcterm+1];	/* stt[i]: remap off indexes */
+	int ctt[fcterm+1];	/* ctt[i]: cterm bound attrs */
 	int ctx = r->ctx;
 	off[cterm] = -1;
 	if (ctx < 0) {
@@ -163,6 +169,7 @@ void led_render(char *s0, int cbeg, int cend)
 			off[c - cbeg] = c <= r->cmax ? r->col[c] : -1;
 	}
 	if (r->cmax > cterm || cbeg) {
+		c = 0;
 		i = ctx < 0 ? cterm-1 : 0;
 		o = off[i];
 		if (o >= 0 && cbeg && r->pos[o] < cbeg)
@@ -173,12 +180,27 @@ void led_render(char *s0, int cbeg, int cend)
 		if (o >= 0 && r->cmax > cterm && r->pos[o] + r->wid[o] > cend)
 			while (off[i] == o)
 				off[ctx < 0 ? i++ : i--] = -1;
-		for (i = 0, c = 0; i < cterm;) {
+		l = cbeg <= r->cmax ? r->col[cbeg] : -1;
+		for (o = 0; l > 0 && l <= n && o < LEDBACK; o++) {
+			if (r->pos[l] < cbeg && c < fcterm) {
+				atti++;
+				att[c++] = l;
+			}
+			l -= ctx;
+		}
+		for (i = 0; i < cterm;) {
 			if ((o = off[i++]) >= 0) {
 				att[c++] = o;
 				for (; off[i] == o; i++);
 			}
 		}
+		l = cend <= r->cmax ? r->col[cend] : -1;
+		for (o = 0; l > 0 && l <= n && o < LEDFORW; o++) {
+			if (r->pos[l] >= cend && c < fcterm) {
+				att[c++] = l;
+			}
+			l += ctx;
+		}
 		stt[0] = 0;
 		for (i = 1; i < c; i++) {
 			int key0 = att[i];
@@ -200,9 +222,9 @@ void led_render(char *s0, int cbeg, int cend)
 		sbufn_null(bsb)
 		bound = bsb->s;
 	}
-	memset(att, 0, MIN(n, cterm+1) * sizeof(att[0]));
+	memset(att, 0, MIN(n, fcterm+1) * sizeof(att[0]));
 	if (xhl)
-		syn_highlight(att, bound ? bound : s0, MIN(n, cterm));
+		syn_highlight(att, bound ? bound : s0, MIN(n, fcterm));
 	free(bound);
 	if (led_attsb && xhl) {
 		led_att *p = (led_att*)led_attsb->s;
@@ -212,7 +234,7 @@ void led_render(char *s0, int cbeg, int cend)
 			if (!bound)
 				att[p->off] = syn_merge(att[p->off], p->att);
 			else if (c && stt[0] <= p->off && stt[c-1] >= p->off) {
-				i = p->off - stt[0];
+				i = p->off - stt[atti];
 				if (i < c && stt[i] == p->off) {
 					att[i] = syn_merge(att[i], p->att);
 					continue; /* text not reordered */
