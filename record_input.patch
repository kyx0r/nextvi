diff --git a/ex.c b/ex.c
index 115d4ecb..690e4f75 100644
--- a/ex.c
+++ b/ex.c
@@ -23,6 +23,7 @@ int xlim = -1;			/* rendering cutoff for non cursor lines */
 int xseq = 1;			/* undo/redo sequence */
 int xerr = 1;			/* error handling -
 				bit 1: print errors, bit 2: early return, bit 3: ignore errors */
+int xrec;			/* input recoding register */
 
 int xrow, xoff, xtop;		/* current row, column, and top row */
 int xbufcur;			/* number of active buffers */
@@ -1279,6 +1280,7 @@ static void *eo_##opt(char *loc, char *cmd, char *arg) { inner }
 EO(pac) EO(pr) EO(ai) EO(ish) EO(ic) EO(grp) EO(shape) EO(seq)
 EO(sep) EO(tbs) EO(td) EO(order) EO(hll) EO(hlw) EO(hlp) EO(hlr)
 EO(hl) EO(lim) EO(led) EO(vis) EO(mpt) EO(err)
+EO(rec)
 
 _EO(left,
 	if (*loc)
@@ -1334,6 +1336,7 @@ static struct excmd {
 	{"m", ec_mark},
 	{"q!", ec_quit},
 	{"q", ec_quit},
+	EO(rec),
 	{"reg", ec_regprint},
 	{"rd", ec_undoredo},
 	{"r", ec_read},
diff --git a/term.c b/term.c
index 757c26eb..5655fd17 100644
--- a/term.c
+++ b/term.c
@@ -160,6 +160,12 @@ int term_read(void)
 		}
 		ibuf_cnt = 1;
 		ibuf_pos = 0;
+		if (xrec && *ibuf) {
+			char buf[2];
+			buf[0] = *ibuf;
+			buf[1] = '\0';
+			ex_regput(xrec, buf, 1);
+		}
 	}
 	if (icmd_pos < sizeof(icmd))
 		icmd[icmd_pos++] = ibuf[ibuf_pos];
diff --git a/vi.h b/vi.h
index 1f1a6fdc..0a7d96ac 100644
--- a/vi.h
+++ b/vi.h
@@ -417,6 +417,7 @@ extern int xsep;
 extern int xlim;
 extern int xseq;
 extern int xerr;
+extern int xrec;
 /* global variables */
 extern int xrow, xoff, xtop;
 extern int xbufcur;
