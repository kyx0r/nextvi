#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 
set -e

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"
# Uncomment to skip errors (. = silent nop)
#DBG="."
# Set QF=. to continue despite errors (errors are still printed)
#QF=.

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: conf.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> \\\\(\\\\?:\\\\(\\\\[,;\\\\]#\\\\?\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\(\\\\(\\\\?:\\\\\\\\\\\\\\\\\\\\|\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)\\\\\\\\\\\\\\\\\\\\|\\\\|\\\\\$\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\(\\\\?:\\\\(\\\\?:<\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)<\\\\|\\\\\$\\\\)\\\\|>\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)>\\\\|\\\\\$\\\\)\\\\)\\\\|\\\\\\\\
\\\\(\\\\?:'\\\\[a-z'\`\\\\[\\\\\\\\\\\\\\\\\\\\]\\\\*\\\\]\\\\)\\\\|\\\\(\\\\[\\\\.\\\\\$\\\\]\\\\|\\\\[0-9 \\\\\\\\t\\\\]\\\\*\\\\)\\\\?\\\\)\\\\)\\\\(\\\\?:\\\\(\\\\[-\\\\*-\\\\+/%\\\\]\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\(\\\\[0-9\\\\]\\\\+\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\(\\\\?:\\\\[ \\\\\\\\t\\\\]\\\\*\\\\\\\\\\\\\\\\\\\\|\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)\\\\\\\\\\\\\\\\\\\\|\\\\|\\\\\$\\\\)\\\\)\\\\*\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\)\\\\\\\\
\\\\(\\\\(pac\\\\|pr\\\\|ai\\\\|ish\\\\|err\\\\|ic\\\\|grp\\\\|mpt\\\\|rcm\\\\|shape\\\\|seq\\\\|ts\\\\|td\\\\|order\\\\|hl\\\\[lwpr\\\\]\\\\?\\\\|left\\\\|lim\\\\|led\\\\|vis\\\\)\\\\\\\\${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 261\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/mj\\\\]/j]|md?/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 261\\${SEP}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'conf.c'

# Patch: ex.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 	return NULL;
\\\\)

${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1445\\${SEP}${QF}}${SEP};=
${SEP}+2a static void modal_dispatch(char *tape)
{
	char *s = tape;
	sbuf_smake(out, 64)
	while (*s) {
		while (*s == ' ') s++;
		if (!*s) break;
		char *end = walk(s);
		if (*s == '(' && s[1] == '\$') {
			char cmd[512];
			char *p = s + 2;
			char *cap = end - 1;
			int len = (int)(cap - p);
			if (len > 0 && len < (int)sizeof(cmd) - 1) {
				memcpy(cmd, p, len);
				cmd[len] = '\\\\0';
				ex_exec(cmd);
			}
		} else {
			sbuf_mem(out, s, end - s)
			sbuf_chr(out, ' ')
		}
		s = end;
	}
	if (out->s_n > 0) {
		sbufn_null(out)
		ex_print(out->s, NULL)
	}
	free(out->s);
}

static void *ec_modal(char *loc, char *cmd, char *arg)
{
	/* reset modal state */
	flip = 0;
	src_ = bank_a; dst_ = bank_b;
	rules_ = rules; dict_ = dict;
	stack_ = stack;
	memset(regs, 0, sizeof(regs));
	bank_a[0] = bank_b[0] = 0;
	quiet = 1;

	/* load rules from lbuf range into src_ */
	char *ptr = src_;
	if (loc[0] && lbuf_len(xb)) {
		int beg = 0, end = 0, o1 = 0, o2 = -1;
		if (ex_region(loc, &beg, &end, &o1, &o2))
			return xrerr;
		for (int i = beg; i < end; i++) {
			char *ln = lbuf_get(xb, i);
			if (ln) {
				ptr = mem_import(ln, ptr);
				if (ptr < src_ + sizeof(bank_a) - 1)
					*ptr++ = ' ';
			}
		}
	}

	/* append arg (initial tape) to src_ */
	if (*arg)
		ptr = mem_import(arg, ptr);

	/* trim trailing whitespace and null-terminate */
	while (ptr > src_ && *(ptr - 1) <= ' ') ptr--;
	*ptr = '\\\\0';

	/* set up output capture */
	sbuf_smake(out, 64)
	modal_output = out;

	/* rewrite loop */
	int rw = 0, lim = cycles;
	cycles = 0x200000;
	while (rewrite() && ++rw)
		if (!--cycles) {
			cycles = lim;
			modal_output = NULL;
			free(out->s);
			return \"modal: rewrites exceeded\";
		}
	cycles = lim;
	modal_output = NULL;

	/* flush string output captured via device_write */
	if (out->s_n > 0) {
		sbufn_null(out)
		ex_print(out->s, NULL)
	}
	free(out->s);

	/* dispatch (\$cmd...) forms and print residue */
	modal_dispatch(src_);
	return NULL;
}

.
${SEP}.,\$;f> 	\\\\{\"g!\", ec_glob\\\\},
	\\\\{\"g\", ec_glob\\\\},
	EO\\\\(mpt\\\\),${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1494\\${SEP}${QF}}${SEP};=
${SEP}+2a 	{\"md\", ec_modal},
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

# Patch: modal.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}i #include <stdio.h>

static sbuf *modal_output;

typedef struct {
	unsigned int id, refs;
	char *a, *b;
} rule;

static int flip, quiet, debug, cycles = 0x200000;
static rule rules[0x1000], *rules_ = rules;
static char dict[0x8000], *dict_ = dict;
static char bank_a[0x4000], *src_ = bank_a;
static char bank_b[0x4000], *dst_ = bank_b;
static char *regs[0x100], stack[0x10], *stack_ = stack;

#define spacer(c) (c <= ' ' || c == '(' || c == ')')
#define chex(c) (0xf & (c - (c <= '9' ? '0' : 0x57)))

static char *copy(char *src, char *dst, int length)
{
	while (length--) *dst++ = *src++;
	return dst;
}

static char *walk(char *s)
{
	char c;
	int depth = 0;
	if (*s == '(') {
		while ((c = *s++)) {
			if (c == '(') depth++;
			if (c == ')') --depth;
			if (!depth) return s;
		}
	}
	while ((c = *s) && !spacer(c)) s++;
	return s;
}

static int sint(char *s)
{
	char c = *s, *cap = walk(s);
	int r = 0, n = 1;
	if (c == '#') {
		s++;
		while ((c = *s) && s++ < cap) r = (r << 4) | chex(c);
		return r;
	}
	if (c == '-') { n = -1, s++; }
	while ((c = *s) && s++ < cap) r = r * 10 + c - '0';
	return r * n;
}

static void device_write(char *s)
{
	char **reg = regs + '0';
	/* phase: ALU */
	if (*reg) {
		int hex = **reg == '#', acc = sint(*reg++);
		/* clang-format off */
		switch(*s) {
		case '+': while (*reg) acc += sint(*reg++); break;
		case '-': while (*reg) acc -= sint(*reg++); break;
		case '*': while (*reg) acc *= sint(*reg++); break;
		case '/': while (*reg) acc /= sint(*reg++); break;
		case '%': while (*reg) acc %= sint(*reg++); break;
		case '&': while (*reg) acc &= sint(*reg++); break;
		case '^': while (*reg) acc ^= sint(*reg++); break;
		case '|': while (*reg) acc |= sint(*reg++); break;
		case '=': while (*reg) acc = acc == sint(*reg++); break;
		case '!': while (*reg) acc = acc != sint(*reg++); break;
		case '>': while (*reg) acc = acc > sint(*reg++); break;
		case '<': while (*reg) acc = acc < sint(*reg++); break;
		}
		/* clang-format on */
		dst_ += snprintf(dst_, 0x10, hex ? \"#%x\" : \"%d\", acc);
		return;
	} else {
		/* phase: string */
		char *cap = walk(s);
		if (*s == '(') s++, --cap;
		while (s < cap) {
			char c = *s++, hb, lb;
			if (c == '\\\\\\\\') {
				unsigned char wc = 0;
				switch(*s++) {
				case 't': wc = 0x09; break;
				case 'n': wc = 0x0a; break;
				case 's': wc = 0x20; break;
				case '#': hb = *s++, lb = *s++; wc = (chex(hb) << 4) | chex(lb); break;
				}
				if (wc) {
					if (modal_output) { sbuf_chr(modal_output, wc) } else putc(wc, stdout);
				}
			} else {
				if (modal_output) { sbuf_chr(modal_output, c) } else putc(c, stdout);
			}
		}
	}
}

static char *file_import(char *path, char *ptr)
{
	FILE *f;
	int pr = 0;
	if ((f = fopen(path, \"r\"))) {
		unsigned char c, last = 0;
		while (fread(&c, 1, 1, f)) {
			c = c <= 0x20 ? 0x20 : c;
			if (c == '(') pr++;
			if (c == ')') pr--;
			if (c == ' ' && last == '(') continue;
			if (c == ')' && last == ' ') ptr--;
			if (c == ' ' && last == ' ') ptr--;
			if (c == '(' && last != '?' && !spacer(last)) *ptr++ = ' ';
			if (last == ')' && !spacer(c)) *ptr++ = ' ';
			*ptr++ = last = c;
		}
		while (*(--ptr) <= ' ') *ptr = 0;
		fclose(f);
		if (pr) fprintf(stderr, \"Modal program imbalanced.\\\\n\");
		return ptr;
	}
	return copy(\"NAF\", ptr, 3);
}

static void write_reg(char r, char *reg)
{
	switch(r) {
	case ':': device_write(reg); return;
	case '~': {
		unsigned char c;
		while (fread(&c, 1, 1, stdin) && c >= ' ') *dst_++ = c;
		if (feof(stdin)) dst_ = copy(\"EOF\", dst_, 3);
		return;
	}
	case '_': {
		char filepath[0x80];
		copy(reg, filepath, walk(reg) - reg);
		dst_ = file_import(filepath, dst_);
		return;
	}
	case '^': { /* op: join */
		char c, *cap = walk(reg);
		if (*reg == '(') reg++, --cap;
		while (reg < cap && (c = *reg++))
			if (!spacer(c)) *dst_++ = c;
		return;
	}
	case '.': { /* op: unwrap */
		char *cap = walk(reg);
		if (*reg == '(') reg++, --cap;
		dst_ = copy(reg, dst_, cap - reg);
		return;
	}
	case '*': { /* op: explode */
		int i, depth = 0;
		char c, *cap = walk(reg);
		if (*reg == '(' && reg[1] != ')') { /* tuple */
			reg++;
			while (reg < cap - 1) {
				while ((c = *reg) && !spacer(c))
					*dst_++ = c, reg++;
				*dst_++ = ' ', *dst_++ = '(', reg++, depth++;
			}
		} else /* token */
			while ((c = *reg++) && !spacer(c))
				*dst_++ = c, *dst_++ = ' ', *dst_++ = '(', depth++;
		for(i = 0; i < depth; i++) *dst_++ = ')';
		return;
	}
	default: {
		dst_ = copy(reg, dst_, walk(reg) - reg);
		return;
	}
	}
}

static int write_tail(char *s, rule *r)
{
	while ((*dst_++ = *s++))
		;
	*dst_ = 0;
	if ((flip = !flip))
		src_ = bank_b, dst_ = bank_a;
	else
		src_ = bank_a, dst_ = bank_b;
	if (r && !quiet) fprintf(stderr, \"%02d %s\\\\n\", r->id, src_), ++r->refs;
	return 1;
}

static int apply_rule(rule *r, char *s)
{
	unsigned char rid;
	char c, *a = r->a, *b = r->b, *origin = dst_, *reg;
	/* phase: clean regs */
	while (stack_ != stack) regs[(int)*(--stack_)] = 0;
	/* phase: match rule */
	while ((c = *a++)) {
		if (c == '?') {
			char *pcap = walk(s);
			rid = *a++;
			if ((reg = regs[rid])) { /* reg cmp */
				char *rcap = walk(reg), *pp = s;
				while (reg < rcap || pp < pcap)
					if (*reg++ != *pp++) return 0;
			} else /* reg set */
				regs[rid] = s, *stack_++ = rid;
			s = pcap;
		} else if (c != *s++)
			return 0;
	}
	c = *s;
	if (!spacer(c)) return 0;
	/* phase: write rule */
	while ((c = *b++))
		if (c == '?' && (rid = *b) && (reg = regs[rid]))
			write_reg(rid, reg), b++;
		else
			*dst_++ = c;
	if (dst_ == origin) {
		while (*s == ' ') s++;
		if (*s == ')' && *(dst_ - 1) == ' ') dst_--;
	}
	return write_tail(s, r);
}

static char *parse_frag(char **side, char *s)
{
	char c, *cap;
	while ((c = *s) && c == ' ') s++;
	if (c == ')' || (c == '<' && s[1] == '>') || (c == '>' && s[1] == '<')) {
		*side = dict_, *dict_++ = 0;
		return s;
	} else {
		cap = walk(s), *side = dict_;
		if (c == '(')
			dict_ = copy(s + 1, dict_, cap - s - 2);
		else
			dict_ = copy(s, dict_, cap - s);
		s = cap, *dict_++ = 0;
	}
	return s;
}

static rule *find_rule(char *s, char *cap)
{
	rule *r = rules;
	if (*s == '(') s++, cap--;
	while (r < rules_) {
		char *ss = s, *a = r->a;
		while (*ss++ == *a++)
			if (!*a && ss == cap) return r;
		r++;
	}
	return NULL;
}

static void remove_rule(rule *r)
{
	if (r < rules_ - 1) {
		char *memsrc = (r + 1)->a;
		int distance = (r + 1)->a - r->a;
		copy(memsrc, r->a, dict_ - memsrc);
		while (r < rules_ - 1) {
			rule *next = r + 1;
			r->id = next->id, r->refs = next->refs;
			r->a = next->a - distance;
			r->b = next->b - distance;
			r++;
		}
	}
	rules_--;
}

static int rewrite(void)
{
	char c, last = 0, *cap, *s = src_;
	while (*s == ' ') s++;
	while ((c = *s)) {
		if (c == '(' || spacer(last)) {
			rule *r;
			/* phase: undefine */
			if (c == '>' && s[1] == '<') {
				s += 2;
				while (*s == ' ') s++;
				cap = walk(s), r = find_rule(s, cap);
				if (r != NULL) {
					if (!quiet) fprintf(stderr, \">< (%s) (%s)\\\\n\", r->a ? r->a : \"\", r->b ? r->b : \"\");
					remove_rule(r);
				}
				while (*cap == ' ') cap++;
				return write_tail(cap, NULL);
			}
			/* phase: define */
			if (c == '<' && s[1] == '>') {
				r = rules_, r->id = rules_ - rules;
				s = parse_frag(&r->b, parse_frag(&r->a, s + 2));
				if (*r->a) {
					if (!quiet) fprintf(stderr, \"<> (%s) (%s)\\\\n\", r->a ? r->a : \"\", r->b ? r->b : \"\");
					rules_++;
				}
				while (*s == ' ') s++;
				return write_tail(s, NULL);
			}
			/* phase: lambda */
			if (c == '?' && s[1] == '(') {
				char *d = dict_;
				cap = walk(s + 1);
				r = rules_, r->id = -1;
				parse_frag(&r->b, parse_frag(&r->a, s + 2));
				s = cap;
				while (*s == ' ') s++;
				if (!(*r->a) || !apply_rule(r, s)) write_tail(s, NULL);
				dict_ = d;
				return 1;
			}
			/* phase: match */
			for(r = rules; r < rules_; r++)
				if (apply_rule(r, s)) return 1;
		}
		*dst_++ = last = c, s++;
	}
	*dst_++ = 0;
	return 0;
}

static char *mem_import(char *src, char *ptr)
{
	unsigned char c, last = 0;
	while ((c = (unsigned char)*src++)) {
		c = c <= 0x20 ? 0x20 : c;
		if (c == ' ' && last == '(') continue;
		if (c == ')' && last == ' ') ptr--;
		if (c == ' ' && last == ' ') ptr--;
		if (c == '(' && last != '?' && !spacer(last)) *ptr++ = ' ';
		if (last == ')' && !spacer(c)) *ptr++ = ' ';
		*ptr++ = last = c;
	}
	return ptr;
}
.
${SEP}vis 2${SEP}wq" $VI -e 'modal.c'

# Patch: vi.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> #include <sys/wait\\\\.h>
#include \"vi\\\\.h\"
#include \"conf\\\\.c\"${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 17\\${SEP}${QF}}${SEP};=
${SEP}+2a #include \"modal.c\"
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index fcab3ea4..c3a84af9 100644
--- a/conf.c
+++ b/conf.c
@@ -258,7 +258,7 @@ return|select|switch|type|var))\\>", A(GR1, BL1 | SYN_BD, YE1)},
 (?:([,;]#?)[ \t]*((?:\\|.*?(?:(?<^\\\\)\\||$)[ \t]*)*(?:(?:<.*?(?:(?<^\\\\)<|$)|>.*?(?:(?<^\\\\)>|$))|\
 (?:'[a-z'`[\\]*])|([.$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*([0-9]+)[ \t]*)*(?:[ \t]*\\|.*?(?:(?<^\\\\)\\||$))*[ \t]*)*)\
 ((pac|pr|ai|ish|err|ic|grp|mpt|rcm|shape|seq|ts|td|order|hl[lwpr]?|left|lim|led|vis)\
-|[@&!=dmj]|\\?\\?\?!?|\\?!|b[psx]?|p[uh]?|ac?|e[f!]?!?|f[-+><tdp]?|inc|i|sc!?|\
+|[@&!=dj]|md?|\\?\\?\?!?|\\?!|b[psx]?|p[uh]?|ac?|e[f!]?!?|f[-+><tdp]?|inc|i|sc!?|\
 (?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czb]?|x!?|ya!?|cm!?|cd?)?",
 		A(BL1 | SYN_BD, RE, RE, RE, RE, WH1, MA1, RE, RE, WH1, RE, GR1, CY1, MA1)},
 	{ex_ft, "\\\\(.)", A(AY1 | SYN_BD, YE)},
diff --git a/ex.c b/ex.c
index a5f4fe52..925a0171 100644
--- a/ex.c
+++ b/ex.c
@@ -1443,6 +1443,101 @@ _EO(left,
 	return NULL;
 )
 
+static void modal_dispatch(char *tape)
+{
+	char *s = tape;
+	sbuf_smake(out, 64)
+	while (*s) {
+		while (*s == ' ') s++;
+		if (!*s) break;
+		char *end = walk(s);
+		if (*s == '(' && s[1] == '$') {
+			char cmd[512];
+			char *p = s + 2;
+			char *cap = end - 1;
+			int len = (int)(cap - p);
+			if (len > 0 && len < (int)sizeof(cmd) - 1) {
+				memcpy(cmd, p, len);
+				cmd[len] = '\0';
+				ex_exec(cmd);
+			}
+		} else {
+			sbuf_mem(out, s, end - s)
+			sbuf_chr(out, ' ')
+		}
+		s = end;
+	}
+	if (out->s_n > 0) {
+		sbufn_null(out)
+		ex_print(out->s, NULL)
+	}
+	free(out->s);
+}
+
+static void *ec_modal(char *loc, char *cmd, char *arg)
+{
+	/* reset modal state */
+	flip = 0;
+	src_ = bank_a; dst_ = bank_b;
+	rules_ = rules; dict_ = dict;
+	stack_ = stack;
+	memset(regs, 0, sizeof(regs));
+	bank_a[0] = bank_b[0] = 0;
+	quiet = 1;
+
+	/* load rules from lbuf range into src_ */
+	char *ptr = src_;
+	if (loc[0] && lbuf_len(xb)) {
+		int beg = 0, end = 0, o1 = 0, o2 = -1;
+		if (ex_region(loc, &beg, &end, &o1, &o2))
+			return xrerr;
+		for (int i = beg; i < end; i++) {
+			char *ln = lbuf_get(xb, i);
+			if (ln) {
+				ptr = mem_import(ln, ptr);
+				if (ptr < src_ + sizeof(bank_a) - 1)
+					*ptr++ = ' ';
+			}
+		}
+	}
+
+	/* append arg (initial tape) to src_ */
+	if (*arg)
+		ptr = mem_import(arg, ptr);
+
+	/* trim trailing whitespace and null-terminate */
+	while (ptr > src_ && *(ptr - 1) <= ' ') ptr--;
+	*ptr = '\0';
+
+	/* set up output capture */
+	sbuf_smake(out, 64)
+	modal_output = out;
+
+	/* rewrite loop */
+	int rw = 0, lim = cycles;
+	cycles = 0x200000;
+	while (rewrite() && ++rw)
+		if (!--cycles) {
+			cycles = lim;
+			modal_output = NULL;
+			free(out->s);
+			return "modal: rewrites exceeded";
+		}
+	cycles = lim;
+	modal_output = NULL;
+
+	/* flush string output captured via device_write */
+	if (out->s_n > 0) {
+		sbufn_null(out)
+		ex_print(out->s, NULL)
+	}
+	free(out->s);
+
+	/* dispatch ($cmd...) forms and print residue */
+	modal_dispatch(src_);
+	return NULL;
+}
+
 #undef EO
 #define EO(opt) {#opt, eo_##opt}
 
@@ -1492,6 +1587,7 @@ static struct excmd {
 	{"g!", ec_glob},
 	{"g", ec_glob},
 	EO(mpt),
+	{"md", ec_modal},
 	{"m", ec_mark},
 	{"q!", ec_quit},
 	{"q", ec_quit},
diff --git a/modal.c b/modal.c
new file mode 100644
index 00000000..a825bd62
--- /dev/null
+++ b/modal.c
@@ -0,0 +1,342 @@
+#include <stdio.h>
+
+static sbuf *modal_output;
+
+typedef struct {
+	unsigned int id, refs;
+	char *a, *b;
+} rule;
+
+static int flip, quiet, debug, cycles = 0x200000;
+static rule rules[0x1000], *rules_ = rules;
+static char dict[0x8000], *dict_ = dict;
+static char bank_a[0x4000], *src_ = bank_a;
+static char bank_b[0x4000], *dst_ = bank_b;
+static char *regs[0x100], stack[0x10], *stack_ = stack;
+
+#define spacer(c) (c <= ' ' || c == '(' || c == ')')
+#define chex(c) (0xf & (c - (c <= '9' ? '0' : 0x57)))
+
+static char *copy(char *src, char *dst, int length)
+{
+	while (length--) *dst++ = *src++;
+	return dst;
+}
+
+static char *walk(char *s)
+{
+	char c;
+	int depth = 0;
+	if (*s == '(') {
+		while ((c = *s++)) {
+			if (c == '(') depth++;
+			if (c == ')') --depth;
+			if (!depth) return s;
+		}
+	}
+	while ((c = *s) && !spacer(c)) s++;
+	return s;
+}
+
+static int sint(char *s)
+{
+	char c = *s, *cap = walk(s);
+	int r = 0, n = 1;
+	if (c == '#') {
+		s++;
+		while ((c = *s) && s++ < cap) r = (r << 4) | chex(c);
+		return r;
+	}
+	if (c == '-') { n = -1, s++; }
+	while ((c = *s) && s++ < cap) r = r * 10 + c - '0';
+	return r * n;
+}
+
+static void device_write(char *s)
+{
+	char **reg = regs + '0';
+	/* phase: ALU */
+	if (*reg) {
+		int hex = **reg == '#', acc = sint(*reg++);
+		/* clang-format off */
+		switch(*s) {
+		case '+': while (*reg) acc += sint(*reg++); break;
+		case '-': while (*reg) acc -= sint(*reg++); break;
+		case '*': while (*reg) acc *= sint(*reg++); break;
+		case '/': while (*reg) acc /= sint(*reg++); break;
+		case '%': while (*reg) acc %= sint(*reg++); break;
+		case '&': while (*reg) acc &= sint(*reg++); break;
+		case '^': while (*reg) acc ^= sint(*reg++); break;
+		case '|': while (*reg) acc |= sint(*reg++); break;
+		case '=': while (*reg) acc = acc == sint(*reg++); break;
+		case '!': while (*reg) acc = acc != sint(*reg++); break;
+		case '>': while (*reg) acc = acc > sint(*reg++); break;
+		case '<': while (*reg) acc = acc < sint(*reg++); break;
+		}
+		/* clang-format on */
+		dst_ += snprintf(dst_, 0x10, hex ? "#%x" : "%d", acc);
+		return;
+	} else {
+		/* phase: string */
+		char *cap = walk(s);
+		if (*s == '(') s++, --cap;
+		while (s < cap) {
+			char c = *s++, hb, lb;
+			if (c == '\\') {
+				unsigned char wc = 0;
+				switch(*s++) {
+				case 't': wc = 0x09; break;
+				case 'n': wc = 0x0a; break;
+				case 's': wc = 0x20; break;
+				case '#': hb = *s++, lb = *s++; wc = (chex(hb) << 4) | chex(lb); break;
+				}
+				if (wc) {
+					if (modal_output) { sbuf_chr(modal_output, wc) } else putc(wc, stdout);
+				}
+			} else {
+				if (modal_output) { sbuf_chr(modal_output, c) } else putc(c, stdout);
+			}
+		}
+	}
+}
+
+static char *file_import(char *path, char *ptr)
+{
+	FILE *f;
+	int pr = 0;
+	if ((f = fopen(path, "r"))) {
+		unsigned char c, last = 0;
+		while (fread(&c, 1, 1, f)) {
+			c = c <= 0x20 ? 0x20 : c;
+			if (c == '(') pr++;
+			if (c == ')') pr--;
+			if (c == ' ' && last == '(') continue;
+			if (c == ')' && last == ' ') ptr--;
+			if (c == ' ' && last == ' ') ptr--;
+			if (c == '(' && last != '?' && !spacer(last)) *ptr++ = ' ';
+			if (last == ')' && !spacer(c)) *ptr++ = ' ';
+			*ptr++ = last = c;
+		}
+		while (*(--ptr) <= ' ') *ptr = 0;
+		fclose(f);
+		if (pr) fprintf(stderr, "Modal program imbalanced.\n");
+		return ptr;
+	}
+	return copy("NAF", ptr, 3);
+}
+
+static void write_reg(char r, char *reg)
+{
+	switch(r) {
+	case ':': device_write(reg); return;
+	case '~': {
+		unsigned char c;
+		while (fread(&c, 1, 1, stdin) && c >= ' ') *dst_++ = c;
+		if (feof(stdin)) dst_ = copy("EOF", dst_, 3);
+		return;
+	}
+	case '_': {
+		char filepath[0x80];
+		copy(reg, filepath, walk(reg) - reg);
+		dst_ = file_import(filepath, dst_);
+		return;
+	}
+	case '^': { /* op: join */
+		char c, *cap = walk(reg);
+		if (*reg == '(') reg++, --cap;
+		while (reg < cap && (c = *reg++))
+			if (!spacer(c)) *dst_++ = c;
+		return;
+	}
+	case '.': { /* op: unwrap */
+		char *cap = walk(reg);
+		if (*reg == '(') reg++, --cap;
+		dst_ = copy(reg, dst_, cap - reg);
+		return;
+	}
+	case '*': { /* op: explode */
+		int i, depth = 0;
+		char c, *cap = walk(reg);
+		if (*reg == '(' && reg[1] != ')') { /* tuple */
+			reg++;
+			while (reg < cap - 1) {
+				while ((c = *reg) && !spacer(c))
+					*dst_++ = c, reg++;
+				*dst_++ = ' ', *dst_++ = '(', reg++, depth++;
+			}
+		} else /* token */
+			while ((c = *reg++) && !spacer(c))
+				*dst_++ = c, *dst_++ = ' ', *dst_++ = '(', depth++;
+		for(i = 0; i < depth; i++) *dst_++ = ')';
+		return;
+	}
+	default: {
+		dst_ = copy(reg, dst_, walk(reg) - reg);
+		return;
+	}
+	}
+}
+
+static int write_tail(char *s, rule *r)
+{
+	while ((*dst_++ = *s++))
+		;
+	*dst_ = 0;
+	if ((flip = !flip))
+		src_ = bank_b, dst_ = bank_a;
+	else
+		src_ = bank_a, dst_ = bank_b;
+	if (r && !quiet) fprintf(stderr, "%02d %s\n", r->id, src_), ++r->refs;
+	return 1;
+}
+
+static int apply_rule(rule *r, char *s)
+{
+	unsigned char rid;
+	char c, *a = r->a, *b = r->b, *origin = dst_, *reg;
+	/* phase: clean regs */
+	while (stack_ != stack) regs[(int)*(--stack_)] = 0;
+	/* phase: match rule */
+	while ((c = *a++)) {
+		if (c == '?') {
+			char *pcap = walk(s);
+			rid = *a++;
+			if ((reg = regs[rid])) { /* reg cmp */
+				char *rcap = walk(reg), *pp = s;
+				while (reg < rcap || pp < pcap)
+					if (*reg++ != *pp++) return 0;
+			} else /* reg set */
+				regs[rid] = s, *stack_++ = rid;
+			s = pcap;
+		} else if (c != *s++)
+			return 0;
+	}
+	c = *s;
+	if (!spacer(c)) return 0;
+	/* phase: write rule */
+	while ((c = *b++))
+		if (c == '?' && (rid = *b) && (reg = regs[rid]))
+			write_reg(rid, reg), b++;
+		else
+			*dst_++ = c;
+	if (dst_ == origin) {
+		while (*s == ' ') s++;
+		if (*s == ')' && *(dst_ - 1) == ' ') dst_--;
+	}
+	return write_tail(s, r);
+}
+
+static char *parse_frag(char **side, char *s)
+{
+	char c, *cap;
+	while ((c = *s) && c == ' ') s++;
+	if (c == ')' || (c == '<' && s[1] == '>') || (c == '>' && s[1] == '<')) {
+		*side = dict_, *dict_++ = 0;
+		return s;
+	} else {
+		cap = walk(s), *side = dict_;
+		if (c == '(')
+			dict_ = copy(s + 1, dict_, cap - s - 2);
+		else
+			dict_ = copy(s, dict_, cap - s);
+		s = cap, *dict_++ = 0;
+	}
+	return s;
+}
+
+static rule *find_rule(char *s, char *cap)
+{
+	rule *r = rules;
+	if (*s == '(') s++, cap--;
+	while (r < rules_) {
+		char *ss = s, *a = r->a;
+		while (*ss++ == *a++)
+			if (!*a && ss == cap) return r;
+		r++;
+	}
+	return NULL;
+}
+
+static void remove_rule(rule *r)
+{
+	if (r < rules_ - 1) {
+		char *memsrc = (r + 1)->a;
+		int distance = (r + 1)->a - r->a;
+		copy(memsrc, r->a, dict_ - memsrc);
+		while (r < rules_ - 1) {
+			rule *next = r + 1;
+			r->id = next->id, r->refs = next->refs;
+			r->a = next->a - distance;
+			r->b = next->b - distance;
+			r++;
+		}
+	}
+	rules_--;
+}
+
+static int rewrite(void)
+{
+	char c, last = 0, *cap, *s = src_;
+	while (*s == ' ') s++;
+	while ((c = *s)) {
+		if (c == '(' || spacer(last)) {
+			rule *r;
+			/* phase: undefine */
+			if (c == '>' && s[1] == '<') {
+				s += 2;
+				while (*s == ' ') s++;
+				cap = walk(s), r = find_rule(s, cap);
+				if (r != NULL) {
+					if (!quiet) fprintf(stderr, ">< (%s) (%s)\n", r->a ? r->a : "", r->b ? r->b : "");
+					remove_rule(r);
+				}
+				while (*cap == ' ') cap++;
+				return write_tail(cap, NULL);
+			}
+			/* phase: define */
+			if (c == '<' && s[1] == '>') {
+				r = rules_, r->id = rules_ - rules;
+				s = parse_frag(&r->b, parse_frag(&r->a, s + 2));
+				if (*r->a) {
+					if (!quiet) fprintf(stderr, "<> (%s) (%s)\n", r->a ? r->a : "", r->b ? r->b : "");
+					rules_++;
+				}
+				while (*s == ' ') s++;
+				return write_tail(s, NULL);
+			}
+			/* phase: lambda */
+			if (c == '?' && s[1] == '(') {
+				char *d = dict_;
+				cap = walk(s + 1);
+				r = rules_, r->id = -1;
+				parse_frag(&r->b, parse_frag(&r->a, s + 2));
+				s = cap;
+				while (*s == ' ') s++;
+				if (!(*r->a) || !apply_rule(r, s)) write_tail(s, NULL);
+				dict_ = d;
+				return 1;
+			}
+			/* phase: match */
+			for(r = rules; r < rules_; r++)
+				if (apply_rule(r, s)) return 1;
+		}
+		*dst_++ = last = c, s++;
+	}
+	*dst_++ = 0;
+	return 0;
+}
+
+static char *mem_import(char *src, char *ptr)
+{
+	unsigned char c, last = 0;
+	while ((c = (unsigned char)*src++)) {
+		c = c <= 0x20 ? 0x20 : c;
+		if (c == ' ' && last == '(') continue;
+		if (c == ')' && last == ' ') ptr--;
+		if (c == ' ' && last == ' ') ptr--;
+		if (c == '(' && last != '?' && !spacer(last)) *ptr++ = ' ';
+		if (last == ')' && !spacer(c)) *ptr++ = ' ';
+		*ptr++ = last = c;
+	}
+	return ptr;
+}
diff --git a/vi.c b/vi.c
index 00929032..aedc522d 100644
--- a/vi.c
+++ b/vi.c
@@ -15,6 +15,7 @@
 #include <sys/wait.h>
 #include "vi.h"
 #include "conf.c"
+#include "modal.c"
 #include "ex.c"
 #include "lbuf.c"
 #include "led.c"
