#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0102-0105 0107-0110 0112-0113 0115-0132 0172 0176-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Command handling readability line breaks
LB="0?"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: vi.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 		break;
	case '\\\\(':
	case '\\\\)':${SEP}??!${DBG:-re p FAIL line 661\\${SEP}p FAIL line 661${INTR}${QF}}${SEP}${LB}
${SEP}+3${SEP}s/\\\\(/)/${SEP}??!${DBG:-re p FAIL line 661\\${SEP}p FAIL line 661${INTR}${QF}}${SEP}.,\$;f> 	case '\\\\}':
	case '\\\\[':
	case '\\\\]':${SEP}??!${DBG:-re p FAIL line 701\\${SEP}p FAIL line 701${INTR}${QF}}${SEP}${LB}
${SEP}+3,#+1c 		dir = mv == '}' || mv == ']' ? 1 : -1;
		var = mv == '[' || mv == ']' ? '{' : '\\\\n';
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/vi.c b/vi.c
index b665a9f3..c0c6942a 100644
--- a/vi.c
+++ b/vi.c
@@ -658,7 +658,7 @@ static int vi_motion(int vc, int *row, int *off)
 		break;
 	case '(':
 	case ')':
-		dir = mv == '(' ? 1 : -1;
+		dir = mv == ')' ? 1 : -1;
 		if (!bre)
 			bre = rset_smake("^[.?!]+['\\])]*(?:[ \t]+\n?|\n)", 0);
 		int subs[2], org;
@@ -698,8 +698,8 @@ static int vi_motion(int vc, int *row, int *off)
 	case '}':
 	case '[':
 	case ']':
-		dir = mv == '{' || mv == '[' ? 1 : -1;
-		var = mv == '[' || mv == ']' ? '\n' : '{';
+		dir = mv == '}' || mv == ']' ? 1 : -1;
+		var = mv == '[' || mv == ']' ? '{' : '\n';
 		for (i = 0; i < cnt; i++)
 			if (lbuf_sectionbeg(xb, dir, row, off, var))
 				break;
