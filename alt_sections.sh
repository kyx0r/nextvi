#!/bin/sh
# Generated by patch2vi from unified diff
# Separator: '@'
set -e

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Patch: vi.c
EXINIT="rcm:|sc! @|vis 6@707,708c 		dir = mv == '}' || mv == ']' ? 1 : -1;
		mark = mv == '[' || mv == ']' ? '{' : '\\n';
.
@vis 4@vis 6@667c 		dir = mv == ')' ? 1 : -1;
.
@vis 4@wq" $VI -e 'vi.c'
