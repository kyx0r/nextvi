#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 0001 0002 0003 0004 0005 0006 0007 0010 0013 0014 0016 0017 0020 0021 0022 0023 0024 0025 0026 0027 0030 0031 0032 0033 0034 0035 0036 0037 0110 0115 0121 0122 0126 0127 0130 0131 0132 0136 0176 0177 0200 0201 0202 0203 0204 0205 0207 0210 0211 0212 0213 0214 0215 0216 0217 0224 0225 0226 0227 0230 0231 0232 0233 0234 0235 0236 0237 0240 0241 0242 0243 0244 0245 0246 0247 0250 0251 0252 0253 0254 0255 0256 0257 0260 0261 0262 0263 0264 0265 0266 0267 0270 0271 0272 0273 0274 0275 0276 0277 0300 0301 0302 0303 0304 0305 0306 0307 0310 0311 0312 0313 0314 0315 0316 0317 0320 0321 0322 0323 0324 0325 0326 0327 0330 0331 0332 0333 0334 0335 0336 0337 0340 0341 0343 0344 0345 0346 0347 0350 0351 0352 0353 0354 0355 0356 0357 0360 0361 0362 0363 0364 0365 0366 0367 0370 0371 0372 0373 0374 0375 0376 0377
set -e

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"
# Uncomment to skip errors (. = silent nop)
#DBG="."
# Set QF=. to continue despite errors (errors are still printed)
#QF=.

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: led.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> static sbuf \\\\*suggestsb;
static sbuf \\\\*acsb;
sbuf \\\\*led_attsb;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 3\\${SEP}${QF}}${SEP};=
${SEP}+2a static int vi_insmov;
.
${SEP}.,\$;f> \\\\}

static void led_printparts\\\\(sbuf \\\\*sb, int pre, int ps,${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 281\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/f\\\\)/f, int print)/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 281\\${SEP}${QF}}${SEP}.,\$;f> 	\\\\}
	if \\\\(pos >= xleft \\\\+ xcols \\\\|\\\\| pos < xleft\\\\)
		xleft = pos < xcols \\\\? 0 : pos - xcols / 2;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 307\\${SEP}${QF}}${SEP};=
${SEP}+3,#+1c 	if (print) {
		syn_blockhl = -1;
		led_crender(r->s, -1, vi_lncol, xleft, xleft + xcols - vi_lncol);
	}
.
${SEP}.,\$;f> 		sbuf_mem\\\\(led_attsb, &la, \\\\(int\\\\)sizeof\\\\(la\\\\)\\\\) \\\\\\\\
	\\\\} \\\\\\\\
	sbuf_str\\\\(sb, buf\\\\) \\\\\\\\${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 367\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/i\\\\)/i, 1)/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 367\\${SEP}${QF}}${SEP}.,\$;f> 	sbuf_free\\\\(led_attsb\\\\) \\\\\\\\
	led_attsb = prev_attsb; \\\\\\\\
	c = term_read\\\\(TK_CTL\\\\('l'\\\\)\\\\); \\\\\\\\${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 372\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/f\\\\)/f, !vi_insmov)/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 372\\${SEP}${QF}}${SEP}.,\$;f> 	char \\\\*cs;
	int len, c, i;
	do \\\\{${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 429\\${SEP}${QF}}${SEP};=
${SEP}+3c 		led_printparts(sb, pre, ps, *post, postn, poff, !vi_insmov);
		vi_insmov = 0;
.
${SEP}.,\$;f> 			else if \\\\(!i\\\\)
				term_clean\\\\(\\\\);
			continue;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 620\\${SEP}${QF}}${SEP};=
${SEP}+2a 		case '\\\\033':;	/* Arrow keys */
			char cbuf[1];
			cbuf[0] = '\\\\0';
			int fl = fcntl(STDIN_FILENO, F_GETFL);
			fcntl(STDIN_FILENO, F_SETFL, fl | O_NONBLOCK);
			read(STDIN_FILENO, cbuf, 1);
			if (*cbuf == '[') {
				read(STDIN_FILENO, cbuf, 1);
				fcntl(STDIN_FILENO, F_SETFL, fl);
				if (ai_max < 0) {
					/* Prompt mode: handle arrows internally */
					int lc, clen, plen;
					char *newpost;
					switch (*cbuf) {
					case 'D':  /* ← */
						if (len > pre) {
							lc = led_lastchar(sb->s + pre) + pre;
							clen = len - lc;
							plen = strlen(*post);
							newpost = malloc(plen + clen + 1);
							memcpy(newpost, sb->s + lc, clen);
							memcpy(newpost + clen, *post, plen + 1);
							free(*postref);
							*postref = *post = newpost;
							postn++;
							sbuf_cut(sb, lc)
						}
						continue;
					case 'C':  /* → */
						if (**post) {
							clen = uc_len(*post);
							sbuf_mem(sb, *post, clen)
							plen = strlen(*post);
							newpost = malloc(plen - clen + 1);
							memcpy(newpost, *post + clen, plen - clen + 1);
							free(*postref);
							*postref = *post = newpost;
							postn--;
						}
						continue;
					case 'A':  /* ↑ - history */
					case 'B':  /* ↓ - history */
						continue;
					}
				}
				vi_insmov = *cbuf;
				return *cbuf;
			}
			fcntl(STDIN_FILENO, F_SETFL, fl);
			return c;
.
${SEP}.,\$;f> int led_prompt\\\\(sbuf \\\\*sb, char \\\\*insert, int \\\\*kmap, ins_state \\\\*is, int ps, int flg\\\\)
\\\\{
	int n = !\\\\(flg & 2\\\\) \\\\? sb->s_n : 0, key, off;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 656\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/ post/ NULL/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 656\\${SEP}${QF}}${SEP}.,\$;f> 			&off, kmap, is, 0, xrow, xtop, flg\\\\);
	restore\\\\(xtd\\\\)
	restore\\\\(xleft\\\\)${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 670\\${SEP}${QF}}${SEP};=
${SEP}+2a 	sbufn_str(sb, post)
	free(postref);
.
${SEP}.,\$;f> 			return key;
		\\\\}
		sbuf_chr\\\\(sb, key\\\\)${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 701\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/f\\\\)/f, 1)/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 701\\${SEP}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'led.c'

# Patch: vi.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 	\\\\*l = ln - pln;
\\\\}

${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 844\\${SEP}${QF}}${SEP};=
${SEP}+2a static int lmodified;

.
${SEP}.,\$;f> 	if \\\\(postn \\\\+ l2 != tlen \\\\|\\\\| memcmp\\\\(ln \\\\+ l1, sb->s \\\\+ l1, tlen - l2 - l1\\\\)\\\\)
		lbuf_edit\\\\(xb, sb->s, r1, r2 \\\\+ 1, o1, xoff\\\\);
	free\\\\(sb->s\\\\);${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 876\\${SEP}${QF}}${SEP};=
${SEP}+2a 	lmodified = 1;
.
${SEP}.,\$;f> 	term_room\\\\(cmdo\\\\);
	sbuf_mem\\\\(sb, ln, l1\\\\)
	key = led_input\\\\(sb, post, postn, row, cmdo << 2, &postn\\\\);${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1047\\${SEP}${QF}}${SEP};=
${SEP}+3${SEP}s/\\\\)/) {/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1047\\${SEP}${QF}}${SEP}.,\$f> 		lbuf_edit\\\\(xb, sb->s, row, row \\\\+ !cmdo, off, xoff\\\\);${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1048\\${SEP}${QF}}${SEP};=
${SEP}.a 		lmodified = 1;
	} else
		lmodified = 0;
.
${SEP}.,\$;f> 					term_back\\\\(xoff != lbuf_eol\\\\(xb, xrow, 1\\\\) \\\\? 'i' : 'a'\\\\);
					break;
				\\\\}${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1517\\${SEP}${QF}}${SEP};=
${SEP}+2a 				switch (k) {
				case 'A':	/* ↑ */
					term_back(!lmodified ? c : 'i');
					if (lmodified)
						vi_col = vi_off2col(xb, xrow, xoff);
					xrow--;
					xrow = xrow < 0 ? 0 : xrow;
					xoff = vi_col2off(xb, xrow, vi_col);
					lmodified = 0;
					goto _break;
				case 'B':	/* ↓ */
					term_back(!lmodified ? c : 'i');
					if (lmodified)
						vi_col = vi_off2col(xb, xrow, xoff);
					xrow++;
					xoff = vi_col2off(xb, xrow, vi_col);
					lmodified = 0;
					goto _break;
				case 'D':	/* ← */
					term_back('i');
					xoff--;
					xoff = xoff < 0 ? 0 : xoff;
					vi_col = vi_off2col(xb, xrow, xoff);
					goto _break;
				case 'C':	/* → */
					term_back(*uc_chr(lbuf_get(xb, xrow), xoff+2) ? 'i' : 'A');
					xoff++;
					if (*uc_chr(lbuf_get(xb, xrow), xoff))
						vi_col = vi_off2col(xb, xrow, xoff);
					goto _break;
				}
.
${SEP}.,\$;f> 				if \\\\(c != 'A' && c != 'C'\\\\)
					xoff--;
				break;${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1520\\${SEP}${QF}}${SEP};=
${SEP}+2a 				_break:
				vi_mod = 0;
				break;
.
${SEP}vis 2${SEP}wq" $VI -e 'vi.c'

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/led.c b/led.c
index e85c962e..e3bf20d3 100644
--- a/led.c
+++ b/led.c
@@ -1,6 +1,7 @@
 static sbuf *suggestsb;
 static sbuf *acsb;
 sbuf *led_attsb;
+static int vi_insmov;
 
 int dstrlen(const char *s, char delim)
 {
@@ -278,7 +279,7 @@ static int led_lastword(char *s)
 }
 
 static void led_printparts(sbuf *sb, int pre, int ps,
-	char *post, int postn, int *poff)
+	char *post, int postn, int *poff, int print)
 {
 	if (!xled) {
 		sbufn_null(sb)
@@ -304,8 +305,10 @@ static void led_printparts(sbuf *sb, int pre, int ps,
 	}
 	if (pos >= xleft + xcols || pos < xleft)
 		xleft = pos < xcols ? 0 : pos - xcols / 2;
-	syn_blockhl = -1;
-	led_crender(r->s, -1, vi_lncol, xleft, xleft + xcols - vi_lncol);
+	if (print) {
+		syn_blockhl = -1;
+		led_crender(r->s, -1, vi_lncol, xleft, xleft + xcols - vi_lncol);
+	}
 	term_pos(-1, led_pos(r->s, pos) + vi_lncol);
 	sbufn_cut(sb, psn)
 	rstate -= 2;
@@ -364,12 +367,12 @@ char *led_read(int *kmap, int c)
 		sbuf_mem(led_attsb, &la, (int)sizeof(la)) \
 	} \
 	sbuf_str(sb, buf) \
-	led_printparts(sb, pre, ps, *post, postn, &i); \
+	led_printparts(sb, pre, ps, *post, postn, &i, 1); \
 	sbuf_cut(sb, len) \
 	sbuf_free(led_attsb) \
 	led_attsb = prev_attsb; \
 	c = term_read(TK_CTL('l')); \
-	led_printparts(sb, pre, ps, *post, postn, poff); \
+	led_printparts(sb, pre, ps, *post, postn, poff, !vi_insmov); \
 	goto noredraw; \
 } \
 
@@ -426,7 +429,8 @@ static int led_line(sbuf *sb, int ps, int pre, char **post, int postn, char **po
 	char *cs;
 	int len, c, i;
 	do {
-		led_printparts(sb, pre, ps, *post, postn, poff);
+		led_printparts(sb, pre, ps, *post, postn, poff, !vi_insmov);
+		vi_insmov = 0;
 		len = sb->s_n;
 		c = term_read(TK_CTL('l'));
 		noredraw:
@@ -618,6 +622,56 @@ static int led_line(sbuf *sb, int ps, int pre, char **post, int postn, char **po
 			else if (!i)
 				term_clean();
 			continue;
+		case '\033':;	/* Arrow keys */
+			char cbuf[1];
+			cbuf[0] = '\0';
+			int fl = fcntl(STDIN_FILENO, F_GETFL);
+			fcntl(STDIN_FILENO, F_SETFL, fl | O_NONBLOCK);
+			read(STDIN_FILENO, cbuf, 1);
+			if (*cbuf == '[') {
+				read(STDIN_FILENO, cbuf, 1);
+				fcntl(STDIN_FILENO, F_SETFL, fl);
+				if (ai_max < 0) {
+					/* Prompt mode: handle arrows internally */
+					int lc, clen, plen;
+					char *newpost;
+					switch (*cbuf) {
+					case 'D':  /* ← */
+						if (len > pre) {
+							lc = led_lastchar(sb->s + pre) + pre;
+							clen = len - lc;
+							plen = strlen(*post);
+							newpost = malloc(plen + clen + 1);
+							memcpy(newpost, sb->s + lc, clen);
+							memcpy(newpost + clen, *post, plen + 1);
+							free(*postref);
+							*postref = *post = newpost;
+							postn++;
+							sbuf_cut(sb, lc)
+						}
+						continue;
+					case 'C':  /* → */
+						if (**post) {
+							clen = uc_len(*post);
+							sbuf_mem(sb, *post, clen)
+							plen = strlen(*post);
+							newpost = malloc(plen - clen + 1);
+							memcpy(newpost, *post + clen, plen - clen + 1);
+							free(*postref);
+							*postref = *post = newpost;
+							postn--;
+						}
+						continue;
+					case 'A':  /* ↑ - history */
+					case 'B':  /* ↓ - history */
+						continue;
+					}
+				}
+				vi_insmov = *cbuf;
+				return *cbuf;
+			}
+			fcntl(STDIN_FILENO, F_SETFL, fl);
+			return c;
 		case TK_CTL('o'): {
 			if (!*postref)
 				*postref = *post = uc_dup(*post);
@@ -653,7 +707,7 @@ static int led_line(sbuf *sb, int ps, int pre, char **post, int postn, char **po
 int led_prompt(sbuf *sb, char *insert, int *kmap, ins_state *is, int ps, int flg)
 {
 	int n = !(flg & 2) ? sb->s_n : 0, key, off;
-	char *post = "", *postref = post;
+	char *post = "", *postref = NULL;
 	ins_state _is;
 	vi_lncol = 0;
 	if (insert)
@@ -668,6 +722,8 @@ int led_prompt(sbuf *sb, char *insert, int *kmap, ins_state *is, int ps, int flg
 			&off, kmap, is, 0, xrow, xtop, flg);
 	restore(xtd)
 	restore(xleft)
+	sbufn_str(sb, post)
+	free(postref);
 	if (key == '\n' && flg & 1) {
 		lbuf_dedup(tempbufs[0].lb, sb->s + n, sb->s_n - n)
 		temp_pos(0, -1, 0, 0);
@@ -698,7 +754,7 @@ int led_input(sbuf *sb, char *post, int postn, int row, int flg, int *pren)
 			return key;
 		}
 		sbuf_chr(sb, key)
-		led_printparts(sb, -1, ps, "", 0, &xoff);
+		led_printparts(sb, -1, ps, "", 0, &xoff, 1);
 		term_chr('\n');
 		term_room(1);
 		crow++;
diff --git a/vi.c b/vi.c
index 167a597e..160c4404 100644
--- a/vi.c
+++ b/vi.c
@@ -842,6 +842,8 @@ static void vi_indents(char *ln, int *l)
 	*l = ln - pln;
 }
 
+static int lmodified;
+
 static int vi_change(int r1, int o1, int r2, int o2, int lnmode)
 {
 	char *post, *ln = lbuf_get(xb, r1);
@@ -874,6 +876,7 @@ static int vi_change(int r1, int o1, int r2, int o2, int lnmode)
 	if (postn + l2 != tlen || memcmp(ln + l1, sb->s + l1, tlen - l2 - l1))
 		lbuf_edit(xb, sb->s, r1, r2 + 1, o1, xoff);
 	free(sb->s);
+	lmodified = 1;
 	return key;
 }
 
@@ -1044,8 +1047,11 @@ static int vc_insert(int cmd)
 	term_room(cmdo);
 	sbuf_mem(sb, ln, l1)
 	key = led_input(sb, post, postn, row, cmdo << 2, &postn);
-	if (postn != l1 || cmdo || !ln)
+	if (postn != l1 || cmdo || !ln) {
 		lbuf_edit(xb, sb->s, row, row + !cmdo, off, xoff);
+		lmodified = 1;
+	} else
+		lmodified = 0;
 	free(sb->s);
 	return key;
 }
@@ -1515,9 +1521,43 @@ void vi(int init)
 					term_back(xoff != lbuf_eol(xb, xrow, 1) ? 'i' : 'a');
 					break;
 				}
+				switch (k) {
+				case 'A':	/* ↑ */
+					term_back(!lmodified ? c : 'i');
+					if (lmodified)
+						vi_col = vi_off2col(xb, xrow, xoff);
+					xrow--;
+					xrow = xrow < 0 ? 0 : xrow;
+					xoff = vi_col2off(xb, xrow, vi_col);
+					lmodified = 0;
+					goto _break;
+				case 'B':	/* ↓ */
+					term_back(!lmodified ? c : 'i');
+					if (lmodified)
+						vi_col = vi_off2col(xb, xrow, xoff);
+					xrow++;
+					xoff = vi_col2off(xb, xrow, vi_col);
+					lmodified = 0;
+					goto _break;
+				case 'D':	/* ← */
+					term_back('i');
+					xoff--;
+					xoff = xoff < 0 ? 0 : xoff;
+					vi_col = vi_off2col(xb, xrow, xoff);
+					goto _break;
+				case 'C':	/* → */
+					term_back(*uc_chr(lbuf_get(xb, xrow), xoff+2) ? 'i' : 'A');
+					xoff++;
+					if (*uc_chr(lbuf_get(xb, xrow), xoff))
+						vi_col = vi_off2col(xb, xrow, xoff);
+					goto _break;
+				}
 				if (c != 'A' && c != 'C')
 					xoff--;
 				break;
+				_break:
+				vi_mod = 0;
+				break;
 			case 'J':
 				vc_join(1, vi_arg <= 1 ? 2 : vi_arg);
 				break;
