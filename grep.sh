#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0003-0010 0013-0014 0016-0023 0025-0037 0110 0112 0120-0121 0126-0130 0132 0172 0177-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
ESC="$(printf '\002')"
# Command that handles readability line breaks
LB="0?"
# Phase 1 (search/mark): errors disabled by default,
# DBG1=1 enables error reporting, QF1=1 quits on failure
# OK1: with DBG1=1 also report fallback anchor successes
[ "$DBG1" = "1" ] && OK1= || OK1="0?"
[ "$DBG1" = "1" ] && DBG1= || DBG1="0?"
[ "$QF1" = "1" ] && QF1="${ESC}${SEP}vis 2${ESC}${SEP}q!1" || QF1=
# Phase 2 (edits): DBG2=1 disables errors, QF2=1 ignores them
[ "$DBG2" = "1" ] && DBG2="0?" || DBG2=
[ "$QF2" = "1" ] && QF2= || QF2="${ESC}${SEP}vis 2${ESC}${SEP}q!1"
# Enters vi at failing code line in this script
# Designed for state inspection mid execution
[ "$INTR" = "1" ] && INTR="${ESC}${SEP}|sc|${ESC}${SEP}vis 2:0reg:e $0:83reg %@47:%f> %@112:&Q:b0:|sc! ${ESC}${ESC}${ESC}${SEP}|:vis 3${ESC}${SEP}q1" || INTR=

# Patch: conf.c ex.c vi.c vi.h
# Body too large for EXINIT/argv: stage it in a file
if ( : > /tmp/p2vi.$$ ) 2>/dev/null; then
    P2VIF=/tmp/p2vi.$$
else
    P2VIF=./p2vi.$$
fi
trap 'rm -f "$P2VIF"' EXIT
printf '%s\n' "|sc! ${ESC}${SEP}|:vis 3${SEP}98reg${SEP}b0${SEP}%ya 98${SEP}?%;f> char n_ft\\[\\] = \"/#\";	/\\* numbers highlight for \\^v \\*/
char nn_ft\\[\\] = \"/##\";	/\\* numbers highlight for # \\*/
char ac_ft\\[\\] = \"/ac\";	/\\* autocomplete dropdown \\*/
char ex_ft\\[\\] = \"/ex\";	/\\* ex mode \\(is never '\\\\n' terminated\\) \\*/
char vs_ft\\[\\] = \"/vs\";	/\\* vi search prompt \\(is never '\\\\n' terminated\\) \\*/
char bar_ft\\[\\] = \"/-\";	/\\* status bar \\(is never '\\\\n' terminated\\) \\*/${ESC}${SEP}0??${ESC}${SEP}0??+2m 0${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> char n_ft\\[\\] = \"/#\";	/\\* numbers highlight for \\^v \\*/
char nn_ft\\[\\] = \"/##\";	/\\* numbers highlight for # \\*/
char ac_ft\\[\\] = \"/ac\";	/\\* autocomplete dropdown \\*/${ESC}${SEP}1??${ESC}${SEP}1??+2m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:15:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f> char n_ft\\[\\] = \"/#\";	/\\* numbers highlight for \\^v \\*/.*?
char nn_ft\\[\\] = \"/##\";	/\\* numbers highlight for # \\*/.*?
(char ac_ft\\[\\] = \"/ac\";	/\\* autocomplete dropdown \\*/)${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:15:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:15${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 	\\{n_ft, NULL},
	\\{nn_ft, NULL},
	\\{ac_ft, NULL},
	\\{ex_ft, NULL},
	\\{vs_ft, NULL},
	\\{bar_ft, NULL},${ESC}${SEP}0??${ESC}${SEP}0??+2m 1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	\\{n_ft, NULL},
	\\{nn_ft, NULL},
	\\{ac_ft, NULL},${ESC}${SEP}1??${ESC}${SEP}1??+2m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:39:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 	\\{n_ft, NULL},.*?
	\\{nn_ft, NULL},.*?
(	\\{ac_ft, NULL},)${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:39:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:39${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 		A\\(NA, SYN_BGMK\\(RE1\\), SYN_BGMK\\(AY1\\), SYN_BGMK\\(AY\\)\\)},
	\\{ac_ft, \"\\[\\^ \\\\t-/:-@\\[-\\^\\{-~\\]\\+\\\$\\|\\(\\.\\+\\\$\\)\", A\\(NA, SYN_BGMK\\(AY1\\)\\)},

	\\{ex_ft, \"\\.\\+\", A\\(AY1 \\| SYN_BD\\), 1},
	\\{ex_ft, \":\\[ \\\\t\\]\\*\\(\\(\\(\\(\\?:\\\\\\\\\\|\\(\\?:\\[\\^\\|\\\\\\\\\\\\\\\\\\]\\|\\\\\\\\\\\\\\\\\\.\\?\\)\\*\\\\\\\\\\|\\?\\[ \\\\t\\]\\*\\)\\*\\(\\?:\\(\\?:<\\(\\?:\\[\\^<\\\\\\\\\\\\\\\\\\]\\|\\\\\\\\\\\\\\\\\\.\\?\\)\\*<\\?\\|>\\(\\?:\\[\\^>\\\\\\\\\\\\\\\\\\]\\|\\\\\\\\\\\\\\\\\\.\\?\\)\\*>\\?\\)\\|\\\\
\\(\\?:'\\[0-9\\]\\+\\)\\|\\(\\[\\.%\\\$\\]\\|\\[0-9 \\\\t\\]\\*\\)\\?\\)\\)\\(\\?:\\(\\[-\\*-\\+/%\\]\\)\\[ \\\\t\\]\\*\\[0-9\\]\\+\\[ \\\\t\\]\\*\\)\\*\\(\\?:\\[ \\\\t\\]\\*\\\\\\\\\\|\\(\\?:\\[\\^\\|\\\\\\\\\\\\\\\\\\]\\|\\\\\\\\\\\\\\\\\\.\\?\\)\\*\\\\\\\\\\|\\?\\[ \\\\t\\]\\*\\)\\*\\)\\[ \\\\t\\]\\*\\\\${ESC}${SEP}0??${ESC}${SEP}0??+2m 2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 		A\\(NA, SYN_BGMK\\(RE1\\), SYN_BGMK\\(AY1\\), SYN_BGMK\\(AY\\)\\)},
	\\{ac_ft, \"\\[\\^ \\\\t-/:-@\\[-\\^\\{-~\\]\\+\\\$\\|\\(\\.\\+\\\$\\)\", A\\(NA, SYN_BGMK\\(AY1\\)\\)},

${ESC}${SEP}1??${ESC}${SEP}1??+2m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:289:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 		A\\(NA, SYN_BGMK\\(RE1\\), SYN_BGMK\\(AY1\\), SYN_BGMK\\(AY\\)\\)},.*?
	\\{ac_ft, \"\\[\\^ \\\\t-/:-@\\[-\\^\\{-~\\]\\+\\\$\\|\\(\\.\\+\\\$\\)\", A\\(NA, SYN_BGMK\\(AY1\\)\\)},.*?
()${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK conf.c:289:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:289${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}${LB}
${SEP}'0a char grep_ft[] = \"/g\";	/* grep buffer */
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:15:m0${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'1a 	{grep_ft, NULL},
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:39:m1${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'2a 	{grep_ft, \"^(.+?):([0-9]+):(.+)\", A(MA, GR1, CY, AY1)},
	{grep_ft, NULL, A(AY | SYN_BGMK(RE1)), 1, 3},

${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL conf.c:289:m2${ESC}${SEP}pr${INTR}${QF2}}${SEP}b1${SEP}%ya 98${SEP}?%;f> int xregs_n;			/\\* allocated register count \\*/
int xdefreg;			/\\* ex default register \\*/
struct buf \\*bufs;		/\\* main buffers \\*/
struct buf tempbufs\\[3\\];		/\\* temporary buffers, for internal use \\*/
struct buf \\*ex_buf;		/\\* current buffer \\*/
struct buf \\*ex_pbuf;		/\\* prev buffer \\*/
static struct buf \\*ex_tpbuf;	/\\* temp prev buffer \\*/${ESC}${SEP}0??${ESC}${SEP}0??+3m 0${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> struct buf tempbufs\\[3\\];		/\\* temporary buffers, for internal use \\*/
struct buf \\*ex_buf;		/\\* current buffer \\*/
struct buf \\*ex_pbuf;		/\\* prev buffer \\*/
static struct buf \\*ex_tpbuf;	/\\* temp prev buffer \\*/${ESC}${SEP}1??${ESC}${SEP}1??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> int xregs_n;			/\\* allocated register count \\*/
int xdefreg;			/\\* ex default register \\*/
struct buf \\*bufs;		/\\* main buffers \\*/${ESC}${SEP}2??${ESC}${SEP}2??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f> ^struct buf tempbufs\\[3\\];		/\\* temporary buffers, for internal use \\*/\$${ESC}${SEP}3??${ESC}${SEP}3??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f> struct buf \\*ex_buf;		/\\* current buffer \\*/
struct buf \\*ex_pbuf;		/\\* prev buffer \\*/
static struct buf \\*ex_tpbuf;	/\\* temp prev buffer \\*/${ESC}${SEP}4??${ESC}${SEP}4??-1m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> i.. ....s_....	/\\*...l.c.... ..gi.te.....n. ..
................. .. de.............. \\*.
...u...bu......s.	./. ..in ..ff.r....
s..u....u..te.............\\*.....o.a.. ......s. .o..i....na. ... ./
.....t ....\\*.x.buf;	./\\*.c..ren....f... \\*/
.t..c. ... ....p.....	.....ev.bu...r...
..a.ic....u.t ......x.......	.............bu.......${ESC}${SEP}5??${ESC}${SEP}5??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f> int xregs_n;			/\\* allocated register count \\*/.*?
int xdefreg;			/\\* ex default register \\*/.*?
struct buf \\*bufs;		/\\* main buffers \\*/.*?
(struct buf tempbufs\\[3\\];		/\\* temporary buffers, for internal use \\*/)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK ex.c:43:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL ex.c:43${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}${LB}
${SEP}'0s/3/4/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL ex.c:43:m0${ESC}${SEP}pr${INTR}${QF2}}${SEP}b2${SEP}%ya 98${SEP}?%;f> 	free\\(sb->s\\);
}

#define fssearch\\(\\) \\\\
len = lbuf_s\\(path\\)->len; \\\\
path\\[len\\] = '\\\\0'; \\\\
ret = ex_edit\\(path, len\\); \\\\${ESC}${SEP}0??${ESC}${SEP}0??+3m 0${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> #define fssearch\\(\\) \\\\
len = lbuf_s\\(path\\)->len; \\\\
path\\[len\\] = '\\\\0'; \\\\
ret = ex_edit\\(path, len\\); \\\\${ESC}${SEP}1??${ESC}${SEP}1??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> 	free\\(sb->s\\);
}

${ESC}${SEP}2??${ESC}${SEP}2??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f> ^#define fssearch\\(\\) \\\\\$${ESC}${SEP}3??${ESC}${SEP}3??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f> len = lbuf_s\\(path\\)->len; \\\\
path\\[len\\] = '\\\\0'; \\\\
ret = ex_edit\\(path, len\\); \\\\${ESC}${SEP}4??${ESC}${SEP}4??-1m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> ...e.....>...
}

...fi...f..e........
..... .....s...t.\\)...e...\\\\
.......n. . .\\\\0....
.e..=......i....t.. .......${ESC}${SEP}5??${ESC}${SEP}5??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f> 	free\\(sb->s\\);.*?
}.*?
.*?
(#define fssearch\\(\\) \\\\)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:439:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:439${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ path\\[len\\] = '\\\\0'; \\\\
ret = ex_edit\\(path, len\\); \\\\
path\\[len\\] = '\\\\n'; \\\\
if \\(ret && xrow\\) \\{ \\\\
	\\*row = xrow; \\*off = xoff; /\\* short circuit \\*/ \\\\
	if \\(!vi_search\\('n', cnt, row, off, 0\\)\\) \\\\
		return 1; \\\\${ESC}${SEP}0??${ESC}${SEP}0??+3m 1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ if \\(ret && xrow\\) \\{ \\\\
	\\*row = xrow; \\*off = xoff; /\\* short circuit \\*/ \\\\
	if \\(!vi_search\\('n', cnt, row, off, 0\\)\\) \\\\
		return 1; \\\\${ESC}${SEP}1??${ESC}${SEP}1??m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ path\\[len\\] = '\\\\0'; \\\\
ret = ex_edit\\(path, len\\); \\\\
path\\[len\\] = '\\\\n'; \\\\${ESC}${SEP}2??${ESC}${SEP}2??+3m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^if \\(ret && xrow\\) \\{ \\\\\$${ESC}${SEP}3??${ESC}${SEP}3??m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ 	\\*row = xrow; \\*off = xoff; /\\* short circuit \\*/ \\\\
	if \\(!vi_search\\('n', cnt, row, off, 0\\)\\) \\\\
		return 1; \\\\${ESC}${SEP}4??${ESC}${SEP}4??-1m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ ...h\\[.e...=...0.;.\\\\
.e....e..e.....a... ...\\)...
.a........=..\\\\..;..
i.....t......o......
..r...=....w; .o..................t..ircui. ....
.i....v.......h...........row..o....0....
	...tu.......${ESC}${SEP}5??${ESC}${SEP}5??+3m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ path\\[len\\] = '\\\\0'; \\\\.*?
ret = ex_edit\\(path, len\\); \\\\.*?
path\\[len\\] = '\\\\n'; \\\\.*?
(if \\(ret && xrow\\) \\{ \\\\)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 1${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:444:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:444${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ if \\(!vi_search\\(\\*row \\? 'N' : 'n', cnt, row, off, 0\\)\\) \\\\
	return 1; \\\\

static int fs_search\\(int cnt, int \\*row, int \\*off\\)
\\{
	char \\*path;${ESC}${SEP}0??${ESC}${SEP}0??+3m 2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ static int fs_search\\(int cnt, int \\*row, int \\*off\\)
\\{
	char \\*path;${ESC}${SEP}1??${ESC}${SEP}1??m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ if \\(!vi_search\\(\\*row \\? 'N' : 'n', cnt, row, off, 0\\)\\) \\\\
	return 1; \\\\

${ESC}${SEP}2??${ESC}${SEP}2??+3m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^static int fs_search\\(int cnt, int \\*row, int \\*off\\)\$${ESC}${SEP}3??${ESC}${SEP}3??m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ \\{
	char \\*path;${ESC}${SEP}4??${ESC}${SEP}4??-1m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ .. \\(.vi.s.......r.w.....'.. .... ....................
.r.t....1...

.ta... ..........r....n..c..........o........of..
\\{
	.....\\*p....${ESC}${SEP}5??${ESC}${SEP}5??+3m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ if \\(!vi_search\\(\\*row \\? 'N' : 'n', cnt, row, off, 0\\)\\) \\\\.*?
	return 1; \\\\.*?
.*?
(static int fs_search\\(int cnt, int \\*row, int \\*off\\))${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 2${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:455:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:455${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ \\{
	char \\*path;
	int again = 0, ret, len;
	wrap:
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];${ESC}${SEP}0??${ESC}${SEP}0??+2m 3${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	int again = 0, ret, len;
	wrap:
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];${ESC}${SEP}1??${ESC}${SEP}1??m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ \\{
	char \\*path;${ESC}${SEP}2??${ESC}${SEP}2??+2m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^	int again = 0, ret, len;\$${ESC}${SEP}3??${ESC}${SEP}3??m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ 	wrap:
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];${ESC}${SEP}4??${ESC}${SEP}4??-1m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ \\{
..........h.
...t.agai........e.. ....
.....:
....l........ < l..._.e.\\(..m.b.........\\)..\\{
	...t.......p....\\[.....-........s....${ESC}${SEP}5??${ESC}${SEP}5??+2m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ \\{.*?
	char \\*path;.*?
(	int again = 0, ret, len;)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 3${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:458:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:458${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 	wrap:
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];
		fssearch\\(\\)
	}
	if \\(fspos == lbuf_len\\(tempbufs\\[1\\]\\.lb\\) && !again\\) \\{
		fspos = 0;${ESC}${SEP}0??${ESC}${SEP}0??+3m 4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 		fssearch\\(\\)
	}
	if \\(fspos == lbuf_len\\(tempbufs\\[1\\]\\.lb\\) && !again\\) \\{
		fspos = 0;${ESC}${SEP}1??${ESC}${SEP}1??m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	wrap:
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];${ESC}${SEP}2??${ESC}${SEP}2??+3m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^		fssearch\\(\\)\$${ESC}${SEP}3??${ESC}${SEP}3??m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ 	}
	if \\(fspos == lbuf_len\\(tempbufs\\[1\\]\\.lb\\) && !again\\) \\{
		fspos = 0;${ESC}${SEP}4??${ESC}${SEP}4??-1m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	.....
.w..l. .f.... . ......e..t......s\\[.\\]\\.......
.	.a.h.=.temp...s..............o.\\+.\\].
.	......c...
.}
	.f....... .=........n.te...uf..1..l.. &..!.....\\) \\{
...s....=...${ESC}${SEP}5??${ESC}${SEP}5??+3m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 	wrap:.*?
	while \\(fspos < lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\) \\{.*?
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\+\\+\\];.*?
(		fssearch\\(\\))${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 4${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:462:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:462${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 	int ret, len;
	while \\(--fspos >= 0\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\];
		fssearch\\(\\)
	}
	return 0;
}${ESC}${SEP}0??${ESC}${SEP}0??+3m 5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 		fssearch\\(\\)
	}
	return 0;
}${ESC}${SEP}1??${ESC}${SEP}1??m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	int ret, len;
	while \\(--fspos >= 0\\) \\{
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\];${ESC}${SEP}2??${ESC}${SEP}2??+3m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^		fssearch\\(\\)\$${ESC}${SEP}3??${ESC}${SEP}3??m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ 	}
	return 0;
}${ESC}${SEP}4??${ESC}${SEP}4??-1m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ .i...r... l.n.
..h......-.sp.s........
..p..h . ..m..u.........>.n....os..
.	.s...r...\\)
	.
......n ..
}${ESC}${SEP}5??${ESC}${SEP}5??+3m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 	int ret, len;.*?
	while \\(--fspos >= 0\\) \\{.*?
		path = tempbufs\\[1\\]\\.lb->ln\\[fspos\\];.*?
(		fssearch\\(\\))${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 5${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:478:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:478${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 				break;
		break;
	case TK_CTL\\('\\]'\\):	/\\* this is also \\^5 on some systems \\*/
	case TK_CTL\\('p'\\):
		#define open_saved\\(n\\) \\\\
		if \\(savepath\\[n\\]\\) \\{ \\\\${ESC}${SEP}0??${ESC}${SEP}0??+2m 6${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 				break;
		break;
	case TK_CTL\\('\\]'\\):	/\\* this is also \\^5 on some systems \\*/${ESC}${SEP}1??${ESC}${SEP}1??+2m 6${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:657:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 				break;.*?
		break;.*?
(	case TK_CTL\\('\\]'\\):	/\\* this is also \\^5 on some systems \\*/)${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 6${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:657:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:657${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 			lkwdcnt = xkwdcnt;
			fspos \\+= fsdir < 0 \\? 1 : 0;
			fspos = MIN\\(fspos, lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\);
			fs_search\\(1, row, off\\);
			fsdir = 1;${ESC}${SEP}0??${ESC}${SEP}0??+3m 7${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 			fs_search\\(1, row, off\\);
			fsdir = 1;${ESC}${SEP}1??${ESC}${SEP}1??m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 			lkwdcnt = xkwdcnt;
			fspos \\+= fsdir < 0 \\? 1 : 0;
			fspos = MIN\\(fspos, lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\);${ESC}${SEP}2??${ESC}${SEP}2??+3m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^			fs_search\\(1, row, off\\);\$${ESC}${SEP}3??${ESC}${SEP}3??m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^			fsdir = 1;\$${ESC}${SEP}4??${ESC}${SEP}4??-1m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a4${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f+ .	....dc.............
......os ...fs....<.....1....;
		....os =....\\(...... ...f......e.p.u..\\[1\\]..b...
.	.f...e..c.\\(.,........f..
		.f..i......${ESC}${SEP}5??${ESC}${SEP}5??+3m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 			lkwdcnt = xkwdcnt;.*?
			fspos \\+= fsdir < 0 \\? 1 : 0;.*?
			fspos = MIN\\(fspos, lbuf_len\\(tempbufs\\[1\\]\\.lb\\)\\);.*?
(			fs_search\\(1, row, off\\);)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 7${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:676:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:676${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 			fsdir = 1;
		} else \\{
			fspos -= fsdir > 0 \\? 1 : 0;
			if \\(!fs_searchback\\(1, row, off\\)\\) \\{${ESC}${SEP}0??${ESC}${SEP}0??m 8${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f+ ^			fsdir = 1;\$${ESC}${SEP}1??${ESC}${SEP}1??m 8${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:677:a1${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}grp 1${ESC}${SEP}%;f+ (			fsdir = 1;)${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 8${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:677:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:677${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 				n = strlen\\(ln\\);
				char buf\\[n \\+ 4\\];
				memcpy\\(buf, \":e \", 3\\);
				memcpy\\(buf\\+3, ln, n\\);
				term_push\\(buf, n \\+ 3\\);
				break; }
			case TK_CTL\\('n'\\):
				vi_cndir = vi_arg \\? -vi_cndir : vi_cndir;${ESC}${SEP}0??${ESC}${SEP}0??+3m 9${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 				memcpy\\(buf\\+3, ln, n\\);
				term_push\\(buf, n \\+ 3\\);
				break; }
			case TK_CTL\\('n'\\):
				vi_cndir = vi_arg \\? -vi_cndir : vi_cndir;${ESC}${SEP}1??${ESC}${SEP}1??m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 				n = strlen\\(ln\\);
				char buf\\[n \\+ 4\\];
				memcpy\\(buf, \":e \", 3\\);${ESC}${SEP}2??${ESC}${SEP}2??+3m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 				memcpy\\(buf\\+3, ln, n\\);
				term_push\\(buf, n \\+ 3\\);${ESC}${SEP}3??${ESC}${SEP}3??m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a3${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 				break; }
			case TK_CTL\\('n'\\):
				vi_cndir = vi_arg \\? -vi_cndir : vi_cndir;${ESC}${SEP}4??${ESC}${SEP}4??-2m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ ..... . ....e....\\).
.	...h.......n.. ..;
.			......\\(.u...\".e... 3.;
			....c.y........l., ...
........_pus...... ..\\+..\\).
	....r.a....
....a.e..K_C.L.'.'\\):
....v..c.... .........\\? .v...n.i..:..._...i.;${ESC}${SEP}5??${ESC}${SEP}5??+3m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 				n = strlen\\(ln\\);.*?
				char buf\\[n \\+ 4\\];.*?
				memcpy\\(buf, \":e \", 3\\);.*?
(				memcpy\\(buf\\+3, ln, n\\);)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 9${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1269:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1269${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 					}
					ln = vi_enprompt\\(\":\", buf, &k, &n\\);
					goto do_excmd; }
				case 'r': \\{
					cs = vi_curword\\(xb, xrow, xoff, vi_arg, 1\\);
					n = cs \\? strlen\\(cs\\) : 0;${ESC}${SEP}0??${ESC}${SEP}0??+2m 10${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 					}
					ln = vi_enprompt\\(\":\", buf, &k, &n\\);
					goto do_excmd; }${ESC}${SEP}1??${ESC}${SEP}1??+2m 10${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1409:a1${SEP}${LB}
${SEP}0;1??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1409${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}?%;f+ 	temp_open\\(0, \"/hist/\", _ft\\);
	temp_open\\(1, \"/fm/\", fm_ft\\);
	temp_open\\(2, \"/sc/\", _ft\\);
	for \\(i = 1; i < argc && argv\\[i\\]\\[0\\] == '-'; i\\+\\+\\) \\{
		if \\(argv\\[i\\]\\[1\\] == '-' && !argv\\[i\\]\\[2\\]\\) \\{
			i\\+\\+;${ESC}${SEP}0??${ESC}${SEP}0??+2m 11${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f+ 	temp_open\\(0, \"/hist/\", _ft\\);
	temp_open\\(1, \"/fm/\", fm_ft\\);
	temp_open\\(2, \"/sc/\", _ft\\);${ESC}${SEP}1??${ESC}${SEP}1??+2m 11${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1840:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f+ 	temp_open\\(0, \"/hist/\", _ft\\);.*?
	temp_open\\(1, \"/fm/\", fm_ft\\);.*?
(	temp_open\\(2, \"/sc/\", _ft\\);)${ESC}${SEP}2??${ESC}${SEP}grp 0${ESC}${SEP}2??m 11${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.c:1840:a2${SEP}${LB}
${SEP}0;1;2??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1840${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}${LB}
${SEP}'0s/\\(\\)/(isbuffer)/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:439:m0${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'1s/ret && xrow/isbuffer/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:444:m1${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'2s/ c/ again, int c/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:455:m2${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'3s/again = 0, //${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:458:m3${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'4s/\\(\\)/(ret && xrow && again != 2)/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:462:m4${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'5s/\\(\\)/(ret && xrow)/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:478:m5${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'6a 	case TK_CTL('x'):
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:657:m6${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'7s/\\(1/(0, 1/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:676:m7${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'8a 		} else if (mv == TK_CTL('x')) {
			term_exec(\"\", 1, '&')
			temp_pos(3, -1, 0, 0);
			temp_switch(3, 0);
			sbuf *gstats; sbuf_make(gstats, 1024)
			char nbuf[100];
			int colpos = 0;
			fspos = 0;
			while (fs_search(2, 1, row, off)) {
				sbuf_str(gstats, xb_path)
				sbuf_chr(gstats, ':')
				colpos = gstats->s_n;
				#define nextrow() \\
				itoa((*row)+1, nbuf); \\
				sbuf_str(gstats, nbuf) \\
				sbuf_chr(gstats, ':') \\
				sbufn_str(gstats, lbuf_get(xb, *row)) \\
				temp_write(3, gstats->s); \\
				(*row)++; \\

				nextrow()
				while (!vi_search('n', cnt, row, off, 0)) {
					sbuf_cut(gstats, colpos)
					nextrow()
				}
				sbuf_cut(gstats, 0)
			}
			sbuf_free(gstats)
			for (i = xbufcur-1; i >= 0 && bufs[i].mtime == -1; i--)
				ex_bufpostfix(&bufs[i], 1);
			temp_switch(3, 0);
			lbuf_jump(xb, '[', row, off);
			*off = 0;
			syn_reloadft(syn_addhl(xregs['/'] ? xregs['/']->s : NULL, 3), xic ? REG_ICASE : 0);
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:677:m8${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'9,#+1c 				memcpy(buf+3, ln, n + 1);
				if (!strcmp(xb_path, \"/grep/\")) {
					int subs[2];
					rset *rs = rset_make(1, (char*[]){\":[0-9]+:\"}, 0);
					if (rset_find(rs, buf, subs, 0) >= 0) {
						buf[subs[0]] = xsep;
						buf[subs[1]-1] = '\\n';
						buf[subs[1]] = '\\0';
					}
					rset_free(rs);
				}
				term_push(buf, strlen(buf));
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1269:m9${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'10a 				case 'x':
					temp_switch(3, 1);
					vi_mod = 1;
					break;
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1409:m10${ESC}${SEP}pr${INTR}${QF2}}${SEP}${LB}
${SEP}'11a 	temp_open(3, \"/grep/\", grep_ft);
${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.c:1840:m11${ESC}${SEP}pr${INTR}${QF2}}${SEP}b3${SEP}%ya 98${SEP}?%;f> extern int xregs_n;
extern int xdefreg;
extern struct buf \\*bufs;
extern struct buf tempbufs\\[3\\];
extern struct buf \\*ex_buf;
extern struct buf \\*ex_pbuf;
#define istempbuf\\(buf\\) \\(buf >= tempbufs && buf < tempbufs \\+ LEN\\(tempbufs\\)\\)${ESC}${SEP}0??${ESC}${SEP}0??+3m 0${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> extern struct buf tempbufs\\[3\\];
extern struct buf \\*ex_buf;
extern struct buf \\*ex_pbuf;
#define istempbuf\\(buf\\) \\(buf >= tempbufs && buf < tempbufs \\+ LEN\\(tempbufs\\)\\)${ESC}${SEP}1??${ESC}${SEP}1??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a1${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> extern int xregs_n;
extern int xdefreg;
extern struct buf \\*bufs;${ESC}${SEP}2??${ESC}${SEP}2??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a2${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP};0${ESC}${SEP}0reg${ESC}${SEP}.,\$f> ^extern struct buf tempbufs\\[3\\];\$${ESC}${SEP}3??${ESC}${SEP}3??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a3${ESC}${ESC}${ESC}${SEP}98reg${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}98reg${ESC}${SEP}%;f> extern struct buf \\*ex_buf;
extern struct buf \\*ex_pbuf;
#define istempbuf\\(buf\\) \\(buf >= tempbufs && buf < tempbufs \\+ LEN\\(tempbufs\\)\\)${ESC}${SEP}4??${ESC}${SEP}4??-1m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a4${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}%;f> .....n ......e.s_.;
.x......nt...e..e..
e.............buf...uf..
.x...n ........u.......u....\\].
e.......t.uc........x.....
.xt.r.......t.......x.pb...
..e..ne .......u..b.f..\\(b.. >=..e...... .......<.te...u.. ...EN...........${ESC}${SEP}5??${ESC}${SEP}5??+3m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a5${ESC}${ESC}${ESC}${SEP}1q${ESC}${SEP}grp 1${ESC}${SEP}%;f> extern int xregs_n;.*?
extern int xdefreg;.*?
extern struct buf \\*bufs;.*?
(extern struct buf tempbufs\\[3\\];)${ESC}${SEP}6??${ESC}${SEP}grp 0${ESC}${SEP}6??m 0${ESC}${ESC}${ESC}${SEP}${OK1}p OK vi.h:449:a6${SEP}${LB}
${SEP}0;1;2;3;4;5;6??!${DBG1:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.h:449${ESC}${SEP}pr${INTR}${QF1}}${SEP}${LB}
${SEP}${LB}
${SEP}'0s/3/4/${SEP}??!${DBG2:-ya!112${ESC}${SEP}prp${ESC}${SEP}p FAIL vi.h:449:m0${ESC}${SEP}pr${INTR}${QF2}}${SEP}vis 2${SEP}b0${SEP}w${SEP}b1${SEP}w${SEP}b2${SEP}w${SEP}b3${SEP}w${SEP}2q" > "$P2VIF"
EXINIT='%ya 97:? %@97' $VI -e 'conf.c' 'ex.c' 'vi.c' 'vi.h' "$P2VIF"

exit 0
=== PATCH2VI DELTA ===
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index c2c5c4b4..8a5ba56b 100644
--- a/conf.c
+++ b/conf.c
@@ -13,6 +13,7 @@ char fm_ft[] = "/fm";	/* file manager */
 char n_ft[] = "/#";	/* numbers highlight for ^v */
 char nn_ft[] = "/##";	/* numbers highlight for # */
 char ac_ft[] = "/ac";	/* autocomplete dropdown */
+char grep_ft[] = "/g";	/* grep buffer */
 char ex_ft[] = "/ex";	/* ex mode (is never '\n' terminated) */
 char vs_ft[] = "/vs";	/* vi search prompt (is never '\n' terminated) */
 char bar_ft[] = "/-";	/* status bar (is never '\n' terminated) */
@@ -37,6 +38,7 @@ struct filetype fts[] = {
 	{n_ft, NULL},
 	{nn_ft, NULL},
 	{ac_ft, NULL},
+	{grep_ft, NULL},
 	{ex_ft, NULL},
 	{vs_ft, NULL},
 	{bar_ft, NULL},
@@ -287,6 +289,9 @@ return|select|switch|type|var))\\>", A(GR1, BL1 | SYN_BD, YE1)},
 		A(NA, SYN_BGMK(RE1), SYN_BGMK(AY1), SYN_BGMK(AY))},
 	{ac_ft, "[^ \t-/:-@[-^{-~]+$|(.+$)", A(NA, SYN_BGMK(AY1))},
 
+	{grep_ft, "^(.+?):([0-9]+):(.+)", A(MA, GR1, CY, AY1)},
+	{grep_ft, NULL, A(AY | SYN_BGMK(RE1)), 1, 3},
+
 	{ex_ft, ".+", A(AY1 | SYN_BD), 1},
 	{ex_ft, ":[ \t]*((((?:\\|(?:[^|\\\\]|\\\\.?)*\\|?[ \t]*)*(?:(?:<(?:[^<\\\\]|\\\\.?)*<?|>(?:[^>\\\\]|\\\\.?)*>?)|\
 (?:'[0-9]+)|([.%$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*[0-9]+[ \t]*)*(?:[ \t]*\\|(?:[^|\\\\]|\\\\.?)*\\|?[ \t]*)*)[ \t]*\
diff --git a/ex.c b/ex.c
index ed899f2d..30eb9529 100644
--- a/ex.c
+++ b/ex.c
@@ -40,7 +40,7 @@ sbuf **xregs;			/* string registers */
 int xregs_n;			/* allocated register count */
 int xdefreg;			/* ex default register */
 struct buf *bufs;		/* main buffers */
-struct buf tempbufs[3];		/* temporary buffers, for internal use */
+struct buf tempbufs[4];		/* temporary buffers, for internal use */
 struct buf *ex_buf;		/* current buffer */
 struct buf *ex_pbuf;		/* prev buffer */
 static struct buf *ex_tpbuf;	/* temp prev buffer */
diff --git a/vi.c b/vi.c
index b674c703..df08eafd 100644
--- a/vi.c
+++ b/vi.c
@@ -436,12 +436,12 @@ void dir_calc(char *path)
 	free(sb->s);
 }
 
-#define fssearch() \
+#define fssearch(isbuffer) \
 len = lbuf_s(path)->len; \
 path[len] = '\0'; \
 ret = ex_edit(path, len); \
 path[len] = '\n'; \
-if (ret && xrow) { \
+if (isbuffer) { \
 	*row = xrow; *off = xoff; /* short circuit */ \
 	if (!vi_search('n', cnt, row, off, 0)) \
 		return 1; \
@@ -452,14 +452,14 @@ if (ret && xrow) { \
 if (!vi_search(*row ? 'N' : 'n', cnt, row, off, 0)) \
 	return 1; \
 
-static int fs_search(int cnt, int *row, int *off)
+static int fs_search(int again, int cnt, int *row, int *off)
 {
 	char *path;
-	int again = 0, ret, len;
+	int ret, len;
 	wrap:
 	while (fspos < lbuf_len(tempbufs[1].lb)) {
 		path = tempbufs[1].lb->ln[fspos++];
-		fssearch()
+		fssearch(ret && xrow && again != 2)
 	}
 	if (fspos == lbuf_len(tempbufs[1].lb) && !again) {
 		fspos = 0;
@@ -475,7 +475,7 @@ static int fs_searchback(int cnt, int *row, int *off)
 	int ret, len;
 	while (--fspos >= 0) {
 		path = tempbufs[1].lb->ln[fspos];
-		fssearch()
+		fssearch(ret && xrow)
 	}
 	return 0;
 }
@@ -655,6 +655,7 @@ static int vi_region(int cmd, int *row, int *off)
 				break;
 		break;
 	case TK_CTL(']'):	/* this is also ^5 on some systems */
+	case TK_CTL('x'):
 	case TK_CTL('p'):
 		#define open_saved(n) \
 		if (savepath[n]) { \
@@ -673,8 +674,42 @@ static int vi_region(int cmd, int *row, int *off)
 			lkwdcnt = xkwdcnt;
 			fspos += fsdir < 0 ? 1 : 0;
 			fspos = MIN(fspos, lbuf_len(tempbufs[1].lb));
-			fs_search(1, row, off);
+			fs_search(0, 1, row, off);
 			fsdir = 1;
+		} else if (mv == TK_CTL('x')) {
+			term_exec("", 1, '&')
+			temp_pos(3, -1, 0, 0);
+			temp_switch(3, 0);
+			sbuf *gstats; sbuf_make(gstats, 1024)
+			char nbuf[100];
+			int colpos = 0;
+			fspos = 0;
+			while (fs_search(2, 1, row, off)) {
+				sbuf_str(gstats, xb_path)
+				sbuf_chr(gstats, ':')
+				colpos = gstats->s_n;
+				#define nextrow() \
+				itoa((*row)+1, nbuf); \
+				sbuf_str(gstats, nbuf) \
+				sbuf_chr(gstats, ':') \
+				sbufn_str(gstats, lbuf_get(xb, *row)) \
+				temp_write(3, gstats->s); \
+				(*row)++; \
+
+				nextrow()
+				while (!vi_search('n', cnt, row, off, 0)) {
+					sbuf_cut(gstats, colpos)
+					nextrow()
+				}
+				sbuf_cut(gstats, 0)
+			}
+			sbuf_free(gstats)
+			for (i = xbufcur-1; i >= 0 && bufs[i].mtime == -1; i--)
+				ex_bufpostfix(&bufs[i], 1);
+			temp_switch(3, 0);
+			lbuf_jump(xb, '[', row, off);
+			*off = 0;
+			syn_reloadft(syn_addhl(xregs['/'] ? xregs['/']->s : NULL, 3), xic ? REG_ICASE : 0);
 		} else {
 			fspos -= fsdir > 0 ? 1 : 0;
 			if (!fs_searchback(1, row, off)) {
@@ -1266,8 +1301,18 @@ void vi(int init)
 				n = strlen(ln);
 				char buf[n + 4];
 				memcpy(buf, ":e ", 3);
-				memcpy(buf+3, ln, n);
-				term_push(buf, n + 3);
+				memcpy(buf+3, ln, n + 1);
+				if (!strcmp(xb_path, "/grep/")) {
+					int subs[2];
+					rset *rs = rset_make(1, (char*[]){":[0-9]+:"}, 0);
+					if (rset_find(rs, buf, subs, 0) >= 0) {
+						buf[subs[0]] = xsep;
+						buf[subs[1]-1] = '\n';
+						buf[subs[1]] = '\0';
+					}
+					rset_free(rs);
+				}
+				term_push(buf, strlen(buf));
 				break; }
 			case TK_CTL('n'):
 				vi_cndir = vi_arg ? -vi_cndir : vi_cndir;
@@ -1407,6 +1452,10 @@ void vi(int init)
 					}
 					ln = vi_enprompt(":", buf, &k, &n);
 					goto do_excmd; }
+				case 'x':
+					temp_switch(3, 1);
+					vi_mod = 1;
+					break;
 				case 'r': {
 					cs = vi_curword(xb, xrow, xoff, vi_arg, 1);
 					n = cs ? strlen(cs) : 0;
@@ -1838,6 +1887,7 @@ int main(int argc, char *argv[])
 	temp_open(0, "/hist/", _ft);
 	temp_open(1, "/fm/", fm_ft);
 	temp_open(2, "/sc/", _ft);
+	temp_open(3, "/grep/", grep_ft);
 	for (i = 1; i < argc && argv[i][0] == '-'; i++) {
 		if (argv[i][1] == '-' && !argv[i][2]) {
 			i++;
diff --git a/vi.h b/vi.h
index ca6cca08..251e995a 100644
--- a/vi.h
+++ b/vi.h
@@ -446,7 +446,7 @@ extern sbuf **xregs;
 extern int xregs_n;
 extern int xdefreg;
 extern struct buf *bufs;
-extern struct buf tempbufs[3];
+extern struct buf tempbufs[4];
 extern struct buf *ex_buf;
 extern struct buf *ex_pbuf;
 #define istempbuf(buf) (buf >= tempbufs && buf < tempbufs + LEN(tempbufs))
