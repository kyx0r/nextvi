#!/bin/sh -e
# Generated by patch2vi from unified diff
# Available separators: 0001-0010 0013-0014 0016-0037 0112-0113 0120-0121 0124 0126 0130 0132 0153 0176-0377

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

VI=${VI:-vi}
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi binary" >&2
    exit 1
fi

SEP="$(printf '\001')"
# Comment to continue despite errors (errors are still printed)
QF="\\${SEP}vis 2\\${SEP}q!1"
# Command handling readability line breaks
LB="0?"
# Uncomment to enter interactive vi on patch failure
#INTR="\\${SEP}|sc|\\${SEP}vis 2:e $0:%f>:@Q:q!1"
# Uncomment to skip errors (0? = silent nop)
#DBG="0\?"

# Patch: conf.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%f> \\\\(\\\\?:'\\\\[a-z'\`\\\\[\\\\\\\\\\\\\\\\\\\\]\\\\*\\\\]\\\\)\\\\|\\\\(\\\\[\\\\.\\\\\$\\\\]\\\\|\\\\[0-9 \\\\\\\\t\\\\]\\\\*\\\\)\\\\?\\\\)\\\\)\\\\(\\\\?:\\\\(\\\\[-\\\\*-\\\\+/%\\\\]\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\(\\\\[0-9\\\\]\\\\+\\\\)\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\(\\\\?:\\\\[ \\\\\\\\t\\\\]\\\\*\\\\\\\\\\\\\\\\\\\\|\\\\.\\\\*\\\\?\\\\(\\\\?:\\\\(\\\\?<\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\)\\\\\\\\\\\\\\\\\\\\|\\\\|\\\\\$\\\\)\\\\)\\\\*\\\\[ \\\\\\\\t\\\\]\\\\*\\\\)\\\\*\\\\)\\\\\\\\${SEP}??!${DBG:-re p FAIL line 295\\${SEP}p FAIL line 295${INTR}${QF}}${SEP}${LB}
${SEP}+3${SEP}s/cd/c[dx]/${SEP}??!${DBG:-re p FAIL line 295\\${SEP}p FAIL line 295${INTR}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'conf.c'

# Patch: ex.c
EXINIT="|sc! \\\\${SEP}|:vis 3${SEP}%;f> 	return val;
\\\\}

${SEP}??!${DBG:-re p FAIL line 1439\\${SEP}p FAIL line 1439${INTR}${QF}}${SEP}${LB}
${SEP}+2a static void *ec_closebuf(char *loc, char *cmd, char *arg)
{
	int idx, ridx = 0;
	int istmp = istempbuf(ex_buf);
	char *s;
	if (!*arg) {
		idx = istmp ? -1 : ex_buf - bufs;
	} else {
		while (*arg == ' ' || *arg == '\\\\t')
			arg++;
		idx = atoi(arg);
		if (idx < 0 && !istmp)
			idx = ex_buf - bufs + 1;
		s = strchr(arg, '-');
		if (s) {
			ridx = atoi(s+1) - idx;
			if (ridx <= 0 && !istmp)
				ridx = (ex_buf - bufs) - idx - 1;
		}
	}
	for (int b = 0; b <= ridx; b++) {
		if (idx < 0 || (!idx && xbufcur < 2) || idx >= xbufcur)
			return \"invalid buffer index\";
		bufs_free(idx);
		for (int i = idx; i < xbufcur; i++)
			bufs[i] = bufs[i+1];
		xbufcur--;
		if (!istmp) {
			ex_buf = idx == ex_buf - bufs ? bufs : ex_buf;
			ex_buf = idx < ex_buf - bufs ? ex_buf - 1 : ex_buf;
			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
		} else {
			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
			ex_tpbuf = idx == ex_tpbuf - bufs ? bufs : ex_tpbuf;
			ex_tpbuf = idx < ex_tpbuf - bufs ? ex_tpbuf - 1 : ex_tpbuf;
		}
		syn_setft(ex_ft);
	}
	return NULL;
}


${SEP}.,\$f> 	\\\\{\"cd\", ec_chdir\\\\},${SEP}??!${DBG:-re p FAIL line 1538\\${SEP}p FAIL line 1538${INTR}${QF}}${SEP}${LB}
${SEP}.a 	{\"cx\", ec_closebuf},
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

exit 0
=== PATCH2VI DELTA ===
=== DELTA conf.c ===
GROUP 1
-(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czbd]|x!?|ya!?|cm!?|cd?)?",
+(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czbd]|x!?|ya!?|cm!?|c[dx]?)?",
pattern:
\(\?:'\[a-z'`\[\\\\\]\*\]\)\|\(\[\.\$\]\|\[0-9 \\t\]\*\)\?\)\)\(\?:\(\[-\*-\+/%\]\)\[ \\t\]\*\(\[0-9\]\+\)\[ \\t\]\*\)\*\(\?:\[ \\t\]\*\\\\\|\.\*\?\(\?:\(\?<\^\\\\\\\\\)\\\\\|\|\$\)\)\*\[ \\t\]\*\)\*\)\\
=== END DELTA ===
=== DELTA ex.c ===
GROUP 2
+	{"cx", ec_closebuf},
pattern:
	\{"cd", ec_chdir\},
edit_cmd_rel:
a 	{"cx", ec_closebuf},
=== END DELTA ===
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index be2bf4a2..a04e732c 100644
--- a/conf.c
+++ b/conf.c
@@ -292,7 +292,7 @@ return|select|switch|type|var))\\>", A(GR1, BL1 | SYN_BD, YE1)},
 (?:'[a-z'`[\\]*])|([.$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*([0-9]+)[ \t]*)*(?:[ \t]*\\|.*?(?:(?<^\\\\)\\||$))*[ \t]*)*)\
 ((pac|pr|ai|ish|err|ic|grp|mpt|shape|seq|ts|td|order|hl[lwpr]?|left|lim|led|vis)\
 |[@&!=dmj]|\\?\\?\?!?|\\?!|b[psx]?|p[uh]?|ac?|e[f!]?!?|f[-+><tdp]?|inc|i|sc!?|\
-(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czbd]|x!?|ya!?|cm!?|cd?)?",
+(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czbd]|x!?|ya!?|cm!?|c[dx]?)?",
 		A(BL1 | SYN_BD, RE, RE, RE, RE, WH1, MA1, RE, RE, WH1, RE, GR1, CY1, MA1)},
 	{ex_ft, "\\\\(.)", A(AY1 | SYN_BD, YE)},
 	{ex_ft, "!(?:[^!\\\\]|\\\\.)*!?|%(?:#|[0-9]+|@([^\\\\]))?", A(WH1 | SYN_BD, CY1)},
diff --git a/ex.c b/ex.c
index d8a2c535..6aa0ddd4 100644
--- a/ex.c
+++ b/ex.c
@@ -1437,6 +1437,50 @@ static int eo_val(char *arg)
 	return val;
 }
 
+static void *ec_closebuf(char *loc, char *cmd, char *arg)
+{
+	int idx, ridx = 0;
+	int istmp = istempbuf(ex_buf);
+	char *s;
+	if (!*arg) {
+		idx = istmp ? -1 : ex_buf - bufs;
+	} else {
+		while (*arg == ' ' || *arg == '\t')
+			arg++;
+		idx = atoi(arg);
+		if (idx < 0 && !istmp)
+			idx = ex_buf - bufs + 1;
+		s = strchr(arg, '-');
+		if (s) {
+			ridx = atoi(s+1) - idx;
+			if (ridx <= 0 && !istmp)
+				ridx = (ex_buf - bufs) - idx - 1;
+		}
+	}
+	for (int b = 0; b <= ridx; b++) {
+		if (idx < 0 || (!idx && xbufcur < 2) || idx >= xbufcur)
+			return "invalid buffer index";
+		bufs_free(idx);
+		for (int i = idx; i < xbufcur; i++)
+			bufs[i] = bufs[i+1];
+		xbufcur--;
+		if (!istmp) {
+			ex_buf = idx == ex_buf - bufs ? bufs : ex_buf;
+			ex_buf = idx < ex_buf - bufs ? ex_buf - 1 : ex_buf;
+			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
+			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
+		} else {
+			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
+			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
+			ex_tpbuf = idx == ex_tpbuf - bufs ? bufs : ex_tpbuf;
+			ex_tpbuf = idx < ex_tpbuf - bufs ? ex_tpbuf - 1 : ex_tpbuf;
+		}
+		syn_setft(ex_ft);
+	}
+	return NULL;
+}
+
+
 #define _EO(opt, inner) \
 static void *eo_##opt(char *loc, char *cmd, char *arg) { inner }
 
@@ -1536,6 +1580,7 @@ static struct excmd {
 	{"cm!", ec_cmap},
 	{"cm", ec_cmap},
 	{"cd", ec_chdir},
+	{"cx", ec_closebuf},
 	{"c", ec_insert},
 	{"j", ec_join},
 	EO(ts),
