#!/bin/sh
# Generated by patch2vi from unified diff
# Available separators: 0001 0002 0003 0004 0005 0006 0007 0010 0013 0014 0016 0017 0020 0021 0022 0023 0024 0025 0026 0027 0030 0031 0032 0033 0034 0035 0036 0037 0112 0113 0120 0121 0124 0126 0130 0132 0153 0176 0177 0200 0201 0202 0203 0204 0205 0206 0207 0210 0211 0212 0213 0214 0215 0216 0217 0220 0221 0222 0223 0224 0225 0226 0227 0230 0231 0232 0233 0234 0235 0236 0237 0240 0241 0242 0243 0244 0245 0246 0247 0250 0251 0252 0253 0254 0255 0256 0257 0260 0261 0262 0263 0264 0265 0266 0267 0270 0271 0272 0273 0274 0275 0276 0277 0300 0301 0302 0303 0304 0305 0306 0307 0310 0311 0312 0313 0314 0315 0316 0317 0320 0321 0322 0323 0324 0325 0326 0327 0330 0331 0332 0333 0334 0335 0336 0337 0340 0341 0342 0343 0344 0345 0346 0347 0350 0351 0352 0353 0354 0355 0356 0357 0360 0361 0362 0363 0364 0365 0366 0367 0370 0371 0372 0373 0374 0375 0376 0377
set -e

# Pass any argument to use patch(1) instead of nextvi ex commands
if [ -n "$1" ]; then
    sed '1,/^=== PATCH2VI PATCH ===$/d' "$0" | patch -p1 --merge=diff3
    exit $?
fi

# Path to nextvi (adjust as needed)
VI=${VI:-vi}

# Uncomment to enter interactive vi on patch failure
#DBG="|sc|vis 2:e $0:@Q:q!1"
# Uncomment to skip errors (. = silent nop)
#DBG="."
# Set QF=. to continue despite errors (errors are still printed)
#QF=.

# Verify that VI is nextvi
if ! $VI -? 2>&1 | grep -q 'Nextvi'; then
    echo "Error: $VI is not nextvi" >&2
    echo "Set VI environment variable to point to nextvi" >&2
    exit 1
fi

# Patch: conf.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 	\\\\{ex_ft, \"\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\(\\\\.\\\\)\", A\\\\(AY1 \\\\| SYN_BD, YE\\\\)\\\\},
	\\\\{ex_ft, \"!\\\\(\\\\?:\\\\[\\\\^!\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\]\\\\|\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\.\\\\)\\\\*!\\\\?\\\\|%\\\\(\\\\?:#\\\\|\\\\[0-9\\\\]\\\\+\\\\|@\\\\(\\\\[\\\\^\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\]\\\\)\\\\)\\\\?\", A\\\\(WH1 \\\\| SYN_BD, CY1\\\\)\\\\},${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 271\\${SEP}${QF}}${SEP};=
${SEP}-2${SEP}s/cd/c[dx]/${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 271\\${SEP}${QF}}${SEP}vis 2${SEP}wq" $VI -e 'conf.c'

# Patch: ex.c
SEP="$(printf '\001')"
QF=${QF-"$(printf 'vis 2\\\001q! 1')"}
EXINIT="rcm:|sc! \\\\${SEP}|vis 3${SEP}%;f> 	return val;
\\\\}

${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1429\\${SEP}${QF}}${SEP};=
${SEP}+2a static void *ec_closebuf(char *loc, char *cmd, char *arg)
{
	int idx, ridx = 0;
	int istmp = istempbuf(ex_buf);
	char *s;
	if (!*arg) {
		idx = istmp ? -1 : ex_buf - bufs;
	} else {
		while (*arg == ' ' || *arg == '\\\\t')
			arg++;
		idx = atoi(arg);
		if (idx < 0 && !istmp)
			idx = ex_buf - bufs + 1;
		s = strchr(arg, '-');
		if (s) {
			ridx = atoi(s+1) - idx;
			if (ridx <= 0 && !istmp)
				ridx = (ex_buf - bufs) - idx - 1;
		}
	}
	for (int b = 0; b <= ridx; b++) {
		if (idx < 0 || (!idx && xbufcur < 2) || idx >= xbufcur)
			return \"invalid buffer index\";
		bufs_free(idx);
		for (int i = idx; i < xbufcur; i++)
			bufs[i] = bufs[i+1];
		xbufcur--;
		if (!istmp) {
			ex_buf = idx == ex_buf - bufs ? bufs : ex_buf;
			ex_buf = idx < ex_buf - bufs ? ex_buf - 1 : ex_buf;
			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
		} else {
			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
			ex_tpbuf = idx == ex_tpbuf - bufs ? bufs : ex_tpbuf;
			ex_tpbuf = idx < ex_tpbuf - bufs ? ex_tpbuf - 1 : ex_tpbuf;
		}
		syn_setft(ex_ft);
	}
	return NULL;
}


.
${SEP}.,\$f> 	\\\\{\"cd\", ec_chdir\\\\},${SEP}??!${DBG:--5,+5p\\${SEP}p FAIL line 1529\\${SEP}${QF}}${SEP};=
${SEP}.a 	{\"cx\", ec_closebuf},
.
${SEP}vis 2${SEP}wq" $VI -e 'ex.c'

exit 0
=== PATCH2VI DELTA ===
=== DELTA conf.c ===
--- /tmp/patch2vi_OPBNlo_conf.c.diff.orig
+++ /tmp/patch2vi_OPBNlo_conf.c.diff
@@ -6,13 +6,7 @@
 #relc
 === SEARCH COMMAND ===
 %;f>
-=== SEARCH PATTERN (offset: 3) ===
-\(\?:'\[a-z'`\[\\\\\]\*\]\)\|\(\[\.\$\]\|\[0-9 \\t\]\*\)\?\)\)\(\?:\(\[-\*-\+/%\]\)\[ \\t\]\*\(\[0-9\]\+\)\[ \\t\]\*\)\*\(\?:\[ \\t\]\*\\\\\|\.\*\?\(\?:\(\?<\^\\\\\\\\\)\\\\\|\|\$\)\)\*\[ \\t\]\*\)\*\)\\
-\(\(pac\|pr\|ai\|ish\|err\|ic\|grp\|mpt\|rcm\|shape\|seq\|ts\|td\|order\|hl\[lwpr\]\?\|left\|lim\|led\|vis\)\\
-\|\[@&!=dmj\]\|\\\\\?\\\\\?\\\?!\?\|\\\\\?!\|b\[psx\]\?\|p\[uh\]\?\|ac\?\|e\[f!\]\?!\?\|f\[-\+><tdp\]\?\|inc\|i\|sc!\?\|\\
---- extra (delete to include) ---
-\(\?:g!\?\|s\)\[ \\t\]\?\(\.\)\?\|q!\?\|reg\?\\\\\+\?\|rd\?\|w\(\?:q!\|\[q!\]\)\?\|u\[czb\]\?\|x!\?\|ya!\?\|cm!\?\|cd\?\)\?",
-		A\(BL1 \| SYN_BD, RE, RE, RE, RE, WH1, MA1, RE, RE, WH1, RE, GR1, CY1, MA1\)\},
+=== SEARCH PATTERN (offset: -2) ===
 	\{ex_ft, "\\\\\\\\\(\.\)", A\(AY1 \| SYN_BD, YE\)\},
 	\{ex_ft, "!\(\?:\[\^!\\\\\\\\\]\|\\\\\\\\\.\)\*!\?\|%\(\?:#\|\[0-9\]\+\|@\(\[\^\\\\\\\\\]\)\)\?", A\(WH1 \| SYN_BD, CY1\)\},
 === END GROUP ===
=== DELTA ex.c ===
--- /tmp/patch2vi_PFaBMn_ex.c.diff.orig
+++ /tmp/patch2vi_PFaBMn_ex.c.diff
@@ -64,9 +64,7 @@
 #offset
 === SEARCH COMMAND ===
 .,\$;f>
-=== SEARCH PATTERN (offset: 3) ===
-	\{"cm!", ec_cmap\},
-	\{"cm", ec_cmap\},
+=== SEARCH PATTERN (offset: 1) ===
 	\{"cd", ec_chdir\},
 --- extra (delete to include) ---
 	\{"c", ec_insert\},
=== PATCH2VI PATCH ===
diff --git a/conf.c b/conf.c
index 3ad1249c..ad010a18 100644
--- a/conf.c
+++ b/conf.c
@@ -268,7 +268,7 @@ return|select|switch|type|var))\\>", A(GR1, BL1 | SYN_BD, YE1)},
 (?:'[a-z'`[\\]*])|([.$]|[0-9 \t]*)?))(?:([-*-+/%])[ \t]*([0-9]+)[ \t]*)*(?:[ \t]*\\|.*?(?:(?<^\\\\)\\||$))*[ \t]*)*)\
 ((pac|pr|ai|ish|err|ic|grp|mpt|rcm|shape|seq|ts|td|order|hl[lwpr]?|left|lim|led|vis)\
 |[@&!=dmj]|\\?\\?\?!?|\\?!|b[psx]?|p[uh]?|ac?|e[f!]?!?|f[-+><tdp]?|inc|i|sc!?|\
-(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czb]?|x!?|ya!?|cm!?|cd?)?",
+(?:g!?|s)[ \t]?(.)?|q!?|reg?\\+?|rd?|w(?:q!|[q!])?|u[czb]?|x!?|ya!?|cm!?|c[dx]?)?",
 		A(BL1 | SYN_BD, RE, RE, RE, RE, WH1, MA1, RE, RE, WH1, RE, GR1, CY1, MA1)},
 	{ex_ft, "\\\\(.)", A(AY1 | SYN_BD, YE)},
 	{ex_ft, "!(?:[^!\\\\]|\\\\.)*!?|%(?:#|[0-9]+|@([^\\\\]))?", A(WH1 | SYN_BD, CY1)},
diff --git a/ex.c b/ex.c
index 105545c5..3488dba4 100644
--- a/ex.c
+++ b/ex.c
@@ -1427,6 +1427,50 @@ static int eo_val(char *arg)
 	return val;
 }
 
+static void *ec_closebuf(char *loc, char *cmd, char *arg)
+{
+	int idx, ridx = 0;
+	int istmp = istempbuf(ex_buf);
+	char *s;
+	if (!*arg) {
+		idx = istmp ? -1 : ex_buf - bufs;
+	} else {
+		while (*arg == ' ' || *arg == '\t')
+			arg++;
+		idx = atoi(arg);
+		if (idx < 0 && !istmp)
+			idx = ex_buf - bufs + 1;
+		s = strchr(arg, '-');
+		if (s) {
+			ridx = atoi(s+1) - idx;
+			if (ridx <= 0 && !istmp)
+				ridx = (ex_buf - bufs) - idx - 1;
+		}
+	}
+	for (int b = 0; b <= ridx; b++) {
+		if (idx < 0 || (!idx && xbufcur < 2) || idx >= xbufcur)
+			return "invalid buffer index";
+		bufs_free(idx);
+		for (int i = idx; i < xbufcur; i++)
+			bufs[i] = bufs[i+1];
+		xbufcur--;
+		if (!istmp) {
+			ex_buf = idx == ex_buf - bufs ? bufs : ex_buf;
+			ex_buf = idx < ex_buf - bufs ? ex_buf - 1 : ex_buf;
+			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
+			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
+		} else {
+			ex_pbuf = idx == ex_pbuf - bufs ? bufs : ex_pbuf;
+			ex_pbuf = idx < ex_pbuf - bufs ? ex_pbuf - 1 : ex_pbuf;
+			ex_tpbuf = idx == ex_tpbuf - bufs ? bufs : ex_tpbuf;
+			ex_tpbuf = idx < ex_tpbuf - bufs ? ex_tpbuf - 1 : ex_tpbuf;
+		}
+		syn_setft(ex_ft);
+	}
+	return NULL;
+}
+
+
 #define _EO(opt, inner) \
 static void *eo_##opt(char *loc, char *cmd, char *arg) { inner }
 
@@ -1527,6 +1571,7 @@ static struct excmd {
 	{"cm!", ec_cmap},
 	{"cm", ec_cmap},
 	{"cd", ec_chdir},
+	{"cx", ec_closebuf},
 	{"c", ec_insert},
 	{"j", ec_join},
 	EO(ts),
